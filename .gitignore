node_modules
xianbaoku_cache*
push_config.local.js
.git.corrupt_backup
.tools
.stryker-tmp
# mutation.yml 的「变异测试」step 带 --fileLogLevel info ⇒ stryker 把 INFO 日志写到 cwd 的 stryker.log
# （本机手工跑同一命令也会产生）。它是复用状态的判定源、只进 artifact 旁的 reuse.json，本身不入库。
stryker.log
reports
coverage
mutation-progress.json
mutation-report.json
cleanup_rootfs_junk.sh
.tmp_parallel_*.json

# AI 审查运行时产物不入库
.ai

# AGENTS.md 要求的"破坏性 Git 操作前快照"产物，属本机文件不入库
backup.bundle
