# V3 P1/P2 深度 Bug 审计记录

> 审计目标：优先寻找会导致消息永久丢失、重复轰炸、进程挂死、敏感信息泄露、缓存/状态损坏或主流程崩溃的问题。
> 审计原则：先确认触发路径，再评估影响面与修复风险；修复风险大于收益的项目记录为设计边界，不强行修改。
> 当前 HTTP 客户端为官方 `got@11.8.6`；早期“自制 HTTP/got”描述仅保留为历史审计背景。

## 审计范围

- 主流程：配置校验、拉取、重试、判重、过滤、截断、推送、缓存、日志、告警、日报、退出码。
- 推送模块：各通道请求构造、API 业务失败、异常响应、密钥脱敏、超时和部分成功。
- 官方 got：重定向、响应错误、超时、JSON 解析和 POST 语义。
- 输入与安全：脏类型、路径逃逸、符号链接、原型污染、危险 URL、HTML/Markdown 注入、ReDoS、Unicode。
- 状态与并发：原子写、内存缓存、并行测试隔离、日报/告警状态和失败重试。

## 已确认并修复

### P1：HTTP 200 + JSON null 被当作成功

部分通道对 `null.code`/`null.errcode` 缺少防御，异常被捕获后仍 resolve，导致消息写缓存并永久漏推。

处理：通道响应统一判空，异常结构按失败处理。

### P2：HITOKOTO 开关误判

`0`、`'0'`、空值和非法值误触发一言请求，增加不必要延迟。

处理：仅显式 `true`/`'true'` 启用，并补回归和变异验证。

### P2：路径相对重定向错误与非法 Location 异常

旧实现直接拼接 origin，路径相对 Location 可能失败；非法 Location 在异步回调内直接抛异常。

处理：使用 `new URL(location, currentUrl)`，解析失败转 Promise reject。

### P2：saveBatch 非数组输入崩溃

对象、数字、布尔值、Symbol 等输入进入 `for...of` 后抛异常。

处理：入口统一 `Array.isArray()` 防御。

### P2：cache.dir 路径逃逸

`..` 和绝对路径可能将缓存写到项目根目录之外。

处理：解析后限制在项目根目录内，越界回退默认目录。

### P2：危险 URL 内部控制空白绕过

`java\\nscript:`、`java\\tscript:` 等形式可能生成 Markdown 链接。

处理：协议判断前清理 ASCII 控制空白。

### P2：过滤 hash 时序错误

过滤缓存清理失败后仍推进 `filter.hash`，旧 `_f` 条目可能永久不重评。

处理：只有缓存清理成功才推进 hash，失败下次重试。

### P2：告警/日报状态文件半写

状态文件直接覆盖写入，进程中断可能留下损坏 JSON。

处理：统一临时文件 + rename 原子写。

### P2：异常响应完整序列化导致敏感字段进入日志

WxPusher、息知等异常路径可能完整输出未知响应对象；白名单字段自身也可能回显已配置 token/key。

处理：只输出白名单错误字段，未知字段不进入日志；摘要字段继续对所有已配置密钥脱敏。

### P2：组合配置脏值导致 filterHash 崩溃

过滤校验和规则编译已能处理嵌套 Symbol 等脏值，但联合到 App.run 的 `filterHash` 字符串化路径仍可能抛异常。

处理：hash 使用安全字符串归一化，与其他配置入口保持同一防御口径。

### P2：最终推送出口 HTML 形态检测漏掉非白名单元素

最终出口原先只对有限标签白名单触发主动 HTML 清理；自定义模板生成 `<input>`、`<form>` 等非白名单元素时，事件属性可能绕过清理并进入 HTML 渲染通道。

处理：改为识别一般字母开头的 HTML 标签形态后统一清洗，并补端到端回归测试；纯文本字面量不带真实标签时不触发过度清洗。

### P2：批量审查发现的脏输入与兼容性问题

- 正则过滤、分类限定、只看它字段的异常字符串转换可能中断流程：统一安全转换，失败保守放行。
- `validateConfig()` 的 `pingbitime` 异常转换可能抛错：统一捕获并返回警告。
- 模板占位符对象循环引用导致 `JSON.stringify()` 抛错：序列化失败安全降为空。
- 合法无引号图片属性未转换，图片随后被标签清理丢失：补 `src/alt` 无引号解析。
- Server 酱字符串 `errno` 与数字 `errno` 语义不一致：统一兼容字符串成功码/重复码。
- WxPusher HTML 检测与主流程有限白名单分裂：统一一般 HTML 标签检测，同时保留 Markdown autolink 不误判。

### v3.195 批量审查新增项

- 数值配置空值的 JavaScript 隐式转换可能改变限频/配置语义：已统一回退默认。
- `compileRules()` 对无法字符串化的 `pingbitime` 配置可能抛错：已安全忽略。
- HTTP 客户端 `.json()` 对 number/boolean 原始 JSON 的兼容性：迁移官方 got 后已支持所有已解析 JSON 原始值。
- `safeErr()` 读取异常 `message` getter 可能二次抛错：已隔离 getter 异常。
- Bark 绝对地址判断区分大小写：已改为大小写不敏感。
- 多个通道业务成功码数字/字符串类型不一致：已统一兼容。

## 深挖后暂不修的项目

### POST 301/302 重定向方法语义

官方 got 负责处理重定向；本项目不额外把 POST 强制改写为 GET。推送 API 通常不返回这类重定向，改变方法语义可能破坏接口兼容性，缺乏真实触发证据，暂不改。

### 设计边界：符号链接、状态和实体 HTML 的处理

符号链接路径已增加 realpath 校验；损坏日报状态已归一化；实体解码后主动 HTML 已二次清理。仍保留 POST 重定向语义不变，因为改变它的兼容性风险高于当前可确认收益。

## 验证要求

- 正常路径回归：全量测试、并行集成测试、通道测试。
- 每个修复至少一个反例测试；高风险修复执行“改回旧逻辑”的变异验证。
- 修复后重新扫描所有调用点，避免只修单一入口。
- 最终提交前确认工作区、远程分支和备份状态。

## 资源生命周期与内存验证记录（2026 年 8 月 5 日）

本轮针对 V3 的资源泄漏风险补充了静态、动态和回归三层验证：

### 静态检查

扫描 `xbk_function_v3.js`、`xbk_http.js`、`xbk_sendNotify_slim.js`、`xbk_agents.js`、`test_app_p.js` 和青龙入口中的资源 API，重点检查：

- `setTimeout` / `setInterval` 是否有明确生命周期；
- `Promise.race` 超时定时器是否在成功、失败两条路径都清理；
- HTTP Agent、响应流和超限销毁是否有释放路径；
- 本地 server、worker、文件写入是否有关闭/清理路径。

### HTTP 连接与响应流测试

使用本地 HTTP server 驱动 `xbk_http.fetchJson()`，分别验证正常 JSON、响应中断、响应体超限和慢速响应：

- 响应中断必须 reject，不能让 Promise 永久挂起；
- 超过响应体上限必须销毁 stream 并返回 `EBODYLIMIT`；
- 正常请求完成后关闭本地 server；
- 等待 Keep-Alive 空闲连接回收后检查 Agent 的活动 socket、空闲 socket 和请求队列；
- 检查进程活动 handle/request，确认没有由请求测试遗留的连接或请求。

本轮验证未发现活动 HTTP socket、Agent 请求队列或未结束响应流残留。

### 堆内存稳定性测试

使用 `node --expose-gc` 重复调用模板替换、HTML 转换等高频纯函数，并在阶段之间主动执行 GC，对比执行前后的 `process.memoryUsage().heapUsed`：

- 观察是否出现持续单调增长；
- 检查大批临时字符串、实体解码结果和模板对象是否可回收；
- 确认内存缓存只保留设计范围内的数据，不因格式化调用无限增长。

本轮执行后堆内存没有持续增长趋势。

### 全量回归

最后运行 `npm test`，覆盖单元、集成、通道和并行调度路径；结果以该命令的实际输出为准，并确认并行测试临时名单文件和 worker 缓存目录没有正常退出残留。

### 验证边界

以下情形仍属于异常终止边界，不等同于已确认的内存泄漏：

- `kill -9`、断电或进程被强制终止时，清理代码可能来不及执行；
- Keep-Alive 连接在仍有请求或处于空闲回收窗口时暂时存在，这是预期行为；
- 多实例并发的全局锁和跨进程资源协调不在当前单实例设计范围内。

## 当前结论

已确认的 P1/P2 项按影响面评估后修复；剩余项目均属于触发条件苛刻、行为契约不明确或兼容性风险高的设计边界，不强行修改。

> 本文件记录审计过程和取舍，后续发现的新问题继续追加，不用易过时的测试数量作为当前状态依据。
