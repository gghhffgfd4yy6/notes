# 项目规则

xbk-push：个人青龙**单实例**线报推送脚本。

## 必守

- 不提交密钥、缓存或本地配置。
- 破坏性 Git 操作前：`git bundle create backup.bundle --all && git bundle verify backup.bundle`；不得手动删除 `.git/objects`，清理使用 `git gc --prune=now`。
- 修改后先验证再称完成；提交前至少运行受影响测试，常规改动跑 `npm run check`。
- 一次提交只做一件事；改版本时同步**四处**：主文件头（`xbk_function_v3.js` 顶部）、`CHANGELOG.md`、`package.json`、`package-lock.json`（顶层 `version` 与 `packages[""].version` 两处都要），由 `node check-version.js` 硬门禁（pre-commit、CI、release.yml 的 `npm run check` 三处都跑，任一处漂移即红）。
- 推送前确认分支和远程；只需推送 `origin`（2026-09-14 起 Gitee 镜像与 `.workflow/master-pipeline.yml` 已废弃移除，不再双推）。禁止浅克隆处理远程历史。

## 常用命令

```bash
npm run check                 # = lint → 版本闸门 → 变异行段校验 → npm test
npm test
npm run test:filter
npm run test:app
npm run test:notify
npm run test:mutation
npm run test:mutation-ranges  # 校验矩阵行段覆盖（改带行段的文件后必跑）
```

## 大文件与变异行段（改动前必读）

`.github/workflows/mutation.yml` 的 `mutate` 行段是硬编码的；v3.270 曾因文件增长静默漏测尾部 430 行。改动带行段的文件（当前为 `xbk_function_v3.js`、`xbk_sendNotify_slim.js`）后：

1. 同步重拆该文件在 matrix `include` 中的行段：连续、不重叠、覆盖到实际行数。
2. 运行 `node scripts/check-mutation-ranges.js`（或 `npm run test:mutation-ranges`）校验：行段全覆盖与连续性、`name`/`src`/`mutate` 三字段齐全且 `src` 与 mutate 目标一致、运行链上的生产文件全部列为 mutate 目标、段名与 `scripts/mutation-report.js` 的 `EXPECTED_SEGMENTS` 双向一致、`stryker.config.js` 与矩阵一致。

## 测试与文档门禁

- `npm run hooks:install` 注册 `.githooks`：`pre-commit` 跑 lint / 版本闸门 / 变异行段校验（约 20s），`pre-push` 跑 `npm run test:filter`（约 60s，推送前拦截）——但**只对被推提交的内容**跑（PR #156 返工）：`local_sha == HEAD` 且**整棵工作树干净（未跟踪文件也算脏；被 ignore 的文件不算）**时走快路径（此时工作树内容 ≡ 被推提交内容），否则（推别的分支 / 有未提交修改 / 有未跟踪文件）在 detached 临时 worktree 里检出那个 sha 后跑同一条门禁、跑完强制清理，隔离环境建不起来即 fail-closed——**绝不拿当前工作树的结果冒充被推提交的验证**（旧实现正是这样给出两类假绿灯：推 A 测 B、未提交的修复掩盖提交里的失败）；未跟踪文件同样会让快路径失效并改走隔离校验——它们虽不属于被推提交，却会被门禁扫描（`test_filter.js` 会 `readdirSync` 全部 `xbk_*.js` 做 helper 可达性断言），可能把「提交该红」变成「工作树绿」。`commit-msg` 要求首行以 `fix: feat: refactor: docs: chore: style: test: perf: revert: build: ci:` 之一开头且不超过 100 字符。装完用 `npm run hooks:verify` 自检（只读：生效 exit 0，未生效 exit 1 并说明是配置缺失、钩子文件缺失还是无执行位）——npm 不会自动注册仓库钩子，新克隆不跑 `hooks:install` 就是**静默没有门禁**，而 `hooks:install` 对「跳过/未覆盖」场景按设计仍 exit 0，不能当作「装好了」的证据。
- 增删 `test_*.js` 或改 `.github/workflows/test.yml` 的显式步骤：同步 `test_suites.js` 与 `SKIP_SUITES`（由 `test_suite_registry.js`、`test_ci_skip_suites.js` 对账）。
- CI 变异**分数门禁**：**当前为 `stryker.config.js` 的 `thresholds.break = null`**（分数已不是门禁）。停用理由：同配置同段三轮 `storage` 实测 **79.08% / 82.92% / 71.02%**，极差 **11.90pp**（`coveredBy` 285/285 三轮一致 ⇒ 抖动来自测试自身），任何阈值都会随噪声误判；且历史「最低段 `message-store` 69.38%」依据的是**含陈旧复用的虚高值**（真实全量 v3-entry 47.81%、storage 59.30%——⚠️ 这两个数**不是 main 现 sha 的诚实基线**：实测出自 `06e62cc`（main 现停在 `9a094b6`）、**修复前配置**（`--concurrency 2` + 共享 `xianbaoku_cache`）、含**假 Killed 风险**，**不得作为 `break` 的标定依据**，详见 `.local/tap-vs-command-diff.md` §7），该基线**已被证伪**，据它定的 65 从来不是可用判据。**待去 flake + 按 TAP 口径的诚实基线重新标定**（TAP runner 迁移已由 PR-1（= PR #162，TAP 迁移落地提交 `b05e323`）落地，见下条）。分数取消后，`NaN` 假绿通道由 PR #158 新增的 **fail-closed 守卫**堵住：位置在 `mutation.yml`「变异测试」step **之后**，读本段报告并断言 `runtimeErrors > 0` **或** `totalValid === 0`（即 `mutationScore = NaN`）**或**报告缺失 ⇒ `exit 1`。为什么必须：`calculateMetrics` 里 `mutationScore = totalValid > 0 ? … : NaN`，而 `determineExitCode` 是 `if (mutationScore < breaking)` ⇒ `NaN < 65 === false` ⇒ 一个全程 RuntimeError / 零有效变异体的段会**静默变绿**。`mutation.yml` 每个矩阵 job 各自 `npx stryker run <matrix.config> --mutate "<段>" --concurrency 8`（PR-1 起**逐段选 runner**：15 段 TAP、`v3-entry`/`storage`/`qinglong-push`/`check-deps` 四段 command，见下条），stryker 按**本段**报告判定，其余段照跑（`fail-fast: false`），产物上传与 `report` job 都是 `if: always()`，日报照发；`--concurrency` 2→8 依据同 commit 的 CI 实测（2→4 = 1.88×、4→8 = 1.84×，2→8 共 **3.46×**），4 vCPU 远未饱和（C=8 时整机仅 ~37% 忙、单链只吃 ~15% CPU）。**「按测试指纹强制全量重跑」是错的解法，已判定不采用**：它拦不住真根因（污染发生在变异体运行期、基线全程是绿的，且测试没变 ⇒ 指纹不变 ⇒ 主 key 仍命中，复用照旧），且在这套 runner 上**跑不完**（真正全量 `app` 2651 个变异体 ≈ 7–11 小时、`utils` 2402、`message-store` 1692，平台 6h/job 硬上限 + 本仓 step 330min ⇒ 9 段已被杀，而 `actions/cache` 是 `post-if: success()`、**失败段不保存进度** ⇒ 那几段每轮从零开始、永久红）——**该「跑不完」论证针对 command 档**：PR-1 切 TAP 后全 19 段 TAP 基线（head `cb247c9`）≈71min，⚠️ 该 run 整体为 **failure**、唯一失败 job `storage` 按设计触发 fail-closed 守卫（**非 timeout**，勿读成「19 段 TAP 全绿」）；并已实测证伪「⇒ 墙钟不变」的预估 ⇒ 这个前提已不成立（完整墙钟对照见「CI 变异已切 TAP 档」条目的「TAP 墙钟对照」）；但 PR-1 按保守路径**仍保留增量**，去增量留待后续 PR。真根因是**共享缓存的并发串扰**：`test_filter.js` 的缓存目录曾是共享的 `<root>/xianbaoku_cache`，多个用例（如「`init`/`save` 在目录不存在时自动创建」）会 `rmSync` 整目录，而 Stryker 一次 run 只建一个沙箱、两个 worker 在同一份副本里互删 ⇒ `ENOTEMPTY … sandbox-*/xianbaoku_cache` 把同族「持久化/墓碑」用例整族拖红 ⇒ **与该变异体无关的假 Killed**，再被 `coverageAnalysis:'off'` 下**无条件复用**的 `incremental-differ`（`if (!testCoverage.hasCoverage) return true`）冻结 11 天（43.4MB 原始 inc 里 `sandbox-yi6WZh` 出现 5021 次、`ENOTEMPTY` 86 次；78 个「旧 Killed→干净全量 Survived」翻转里 57 个带该沙箱名）；修复＝缓存目录按进程唯一（`xianbaoku_cache_p<XBK_PARALLEL_ID|pid>`，与 `Config.cache.dir` 同源），独立复现：并发 2 份时 base 各 3 失败 → fixed **0 失败、无残留**。分数门禁**不在** `scripts/mutation-report.js` 里：该脚本 `main()` 是先 `validateSegments`/`validateFreshness` 再发 Issue，加分数 throw 会在最需要看分数时把日报一起吞掉——它对「缺段/多余段/重复段/陈旧报告」的 throw（完整性真门禁）保持不变，不许放宽。将来按 TAP 口径重新标定后若恢复分数门禁，回退＝把 `break` 改回 `null`。
- 变异**复用状态可见**（v3.276 续，PR #158 评审 · Qodo Medium / Correctness；**沿用 v3.276 不 bump 版本**）：增量缓存 key 有意不含测试侧指纹（见上条），而 `coverageAnalysis:'off'` 下 `incremental-differ` 拿不到覆盖信息时**无条件复用**旧结果（PR-1 后 `coverageAnalysis:'off'` **只剩 command 档那 4 段**（`v3-entry` / `storage` / `qinglong-push` / `check-deps`）；TAP 档是 perTest、有覆盖信息，不走这条分支——**已在 CI 实测**：`d34cf5b`（源与测试均未变、命中 TAP 档 inc）实测 `133/133`、`2651/2651`、`2402/2402`、`391/391` 全部复用，无「本该重算却重算」的例子（该轮出现全量只因缓存 key 形态变更导致未命中，与 perTest 无关） ⇒ 被复用的段描述的是**旧测试状态**。当前无分数门禁（`break = null`），故这不构成任何门禁风险；但**日报/artifact 会把旧结果呈现为「本 commit 的结果」**这一层是真实缺陷，PR #158 的处理是让它**可见**——**不恢复测试指纹、不强制全量、不改分数口径**。链路：`mutation.yml` 的「变异测试」step 带 `--fileLogLevel info`（stryker 默认 `fileLogLevel=off`，**不写**日志文件），紧随其后由「记录本段增量复用状态」step 调 `node scripts/mutation-report.js --reuse --segment "${{ matrix.name }}" --log stryker.log --out reports/mutation/reuse.json` 落盘，`reuse.json` 随 artifact 到汇总 job；日报逐段标注「本轮全量 / 复用 N/M」，复用比例 **≥50%** 时显式提示「该段未全部重算，分数/存活清单可能来自旧测试状态」，**读不到元信息的段显示「未记录」并另给「不要假定它们是全量重算」的提示**（绝不静默当成全量）。判定源（**已实测**，非推测）＝stryker 的两条 INFO 日志：`No incremental result file found at …, a full mutation testing run will be performed.`（全量）与 `Result:\t\tN of M mutant result(s) are reused.`（复用）；**报告本身没有复用维度**（`mutation-testing-report-schema` 的 `MutantResult` 无任何 reuse 字段），所以日志是唯一来源，必须在**段 job 里**落盘才能到达汇总 job。**PR #168 追加溯源三态（纯显示层交叉校验）**：「恢复增量缓存」step 新增 `id: inc-cache`，「记录本段增量复用状态」step 把 `steps.inc-cache.outputs.cache-hit` 改经 `env` 注入（`CACHE_HIT: ${{ steps.inc-cache.outputs.cache-hit || 'none' }}`），run 行改用 `--cache-hit "$CACHE_HIT"`——**不内联进 `run`**（本仓 `test.yml` 的 zizmor template-injection 约定：上游 step output 一律经 `env` 注入；内联模板形态由 `test_mutation_report_cli.js` 的反例 F（靶向 · R1-02：把活动 run 行的 `"$CACHE_HIT"` 换回内联模板即变红）与对活动 run 行的 `--cache-hit "$CACHE_HIT"` 正断言一同守住；同族靶向反例还有 D 删活动 run 行的 `--cache-hit`、E 删 env 行的 `|| 'none'` 归一——**E 锁的是归一，不是内联**，内联回潮即变红），把 `actions/cache` 的 `cache-hit` 三态落进 `reuse.json` 的 `cacheHit` 字段：`true` = 主 key 精确命中（`hashFiles` 覆盖的输入未变）、`false` = 主 key 未命中、由 `restore-keys` **兜底复原**（结果 = 旧缓存 + 按文件内容差分复用）、空 ⇒ 用 `|| 'none'` 归一为 `none` = 主 key 与兜底都没恢复 ⇒ 本轮该段必为真全量。为什么必须归一：上游 `actions/cache` issue #1466——恢复失败时它**有意不设置** `cache-hit` output（无恢复 ≠ `cache-hit=false`），故「空值」与「参数漏传」是两件事。日报据此逐段标注溯源（主 key 命中 / **兜底复原** / **其余（含 `none` 与字段缺失）并入「复用来源未记录」**），`cacheHit=false` 且日志含复用 ⇒ 给「**兜底复原 + 含复用**」专用结论（分数/存活清单来自**旧缓存**，不等于本 commit 的结果）；`cacheHit=none` 却记录了「复用 N/M」⇒ 自相矛盾、点名为 `none+partial` 矛盾（接线可能坏了）。**边界（不得越界）**：这是**纯显示层**交叉校验，**不新增第四态**（仍只有「全量 / 复用 N/M / 未记录」三态 + 溯源标注）、**不加 throw**、**不改分数口径**。两个已防的实测坑：① 日志以 `flags:'a'` 追加 ⇒ 解析取**最后一次**事件（否则上一轮的「全量」会盖住本轮的「复用」）；② 消息里的 ANSI 着色**不进文件日志**（**已实测核实；前任注释写反了，PR #158 已更正**）：`chalk.yellowBright` 在**消息构造时**着色，CI 的 `chalk.level>0` 时 stdout 上确实是 `ESC[93m…ESC[39m`（本机 `FORCE_COLOR=1` 跑真 stryker 复现），但**写文件的 `LoggingEvent.format()` 显式 `.replace(ansiRegex,'')`**（`@stryker-mutator/core` 的 `logging/logging-event.js`，只有写 stdout 的 `formatColorized()` 保留颜色）⇒ 同一实验里 `stryker.log` 全文件 **0 个 ESC 字节**。故 `stripAnsi` 是**纵深防御**（防 stryker 改掉 `format()`、或有人把 stdout 文本喂进解析器），**不是**当前文件格式的必要条件；**不得**再宣称「CI 下文件日志带 ANSI」。**残余边界（PR #158 补，前任漏掉；已实测复现）**：`Result: N of M` 的 `M` 只统计**当前 `--mutate` 范围**内的变异体，而 incremental-differ 的 sticky 分支（`!currentMutantKeys.has(key) && !isInMutatedScope(...)`）会把旧增量报告中**已不在当前范围**的变异体 `{...oldResult}` **原样并入**报告——status 沿用旧运行，却**既不计入 N 也不计入 M** ⇒ `N/M` 只是「旧结果占比」的**下界**。实测：同一份 src.js 不改内容，先 `--mutate "src.js:1-4"` 再 `--mutate "src.js:1-3"`（缓存 key **不含 mutate 范围**；`restore-keys` 是**不含源指纹**的兜底前缀（PR-1 评审后由裸前缀 `stryker-<段>-` 收窄：裸前缀会让 TAP 段恢复 command 档遗留 inc，而 core 对 inc 零校验）⇒ 主 key 未命中时也恢复**同段同档配置**的最近一条同前缀缓存；当前 key / 兜底形态见 `mutation.yml` 缓存 step 的 `key:` / `restore-keys:`（本 PR 落地时 `:405-407`）与下一段）⇒ 日志 `6 of 6 reused` 而报告有 **8** 个变异体，多出的 2 个是第 4 行的旧变异体；对照组删 inc 全量跑恰好 6 个。该场景在本仓可达（本文件要求大文件增长时**重拆 mutate 行段**）。处理＝**只呈现、不改口径**：日报「♻️ 复用状态」小节把「报告变异体数 vs 日志口径 M」不一致的段单独点名（`reuseUnaccountedCount`，纯显示层交叉校验，**不加 throw、不新增第四态**），并明说比例只是下界；`0 of M` 这类「标为全量但计数对不上」的段**不再**被并进「全部全量重算 ⇒ 对应本 commit 的测试状态」这句结论（由 `test_mutation_report.js` 的靶向反例锁定）。**硬约束（不得放宽）**：`--reuse` **只在 `reports/mutation/mutation.json` 已存在时**才写 `reuse.json`——否则崩溃段因多出文件让上传步的 `if-no-files-found: error` 失效，「stryker 没产出报告 ⇒ 段 job 响亮变红」被降级成「汇总 job 缺段才暴露」（由 `test_mutation_report_cli.js` 的「无报告不得写 reuse.json」用例锁定）。另外三处**不得顺手改**：日报分数口径仍是 `(killed+timeout)/total`、`EXPECTED_SEGMENTS` 对账不变、日报仍**不加**分数 throw。真正的诚实全量重跑留到下一个 PR（**TAP runner 迁移已由 PR-1 落地**：全 19 段 TAP 基线（head `cb247c9`）≈71min，⚠️ 该 run 整体为 **failure**、唯一失败 job `storage` 按设计触发 fail-closed 守卫（**非 timeout**，勿读成「19 段 TAP 全绿」）；完整墙钟对照（含 16 TAP + 3 command **86.0min** → 15 TAP + 4 command **90.8min**）见下条「CI 变异已切 TAP 档」的「TAP 墙钟对照」⇒ 去增量已可负担，但 PR-1 按保守路径**不动增量链路**），届时再按 TAP 口径的诚实基线重新标定 `break`。
- CI 变异已切 **TAP 档**（PR-1；**沿用 v3.276 不 bump 版本**）：`mutation.yml` 的矩阵每条新增 `config` 字段 ⇒ **逐段可选 runner**——15 段用新增的 `stryker.tap.config.js`（`testRunner: 'tap'` + `coverageAnalysis: 'perTest'`，报告仍落 **stryker 默认**的 `reports/mutation/mutation.json`/`.html`，守卫/清理步/reuse 落盘闸门/artifact/analyze-artifacts 全按默认路径对齐），**`v3-entry` / `storage` / `qinglong-push` / `check-deps` 四段保留 `stryker.config.js`**（command 档）。**分叉登记（有意为之）**：本地 `npm run test:mutation` 仍走 command 档 `stryker.config.js`；两档测试集由 `stryker.tap.config.js` 的**加载期断言**强制对齐（`tap.testFiles` 显式清单 ↔ `test_suites.js` 推导清单，逐元素含顺序，不一致即 throw）。**这 4 段为何不迁移**：`xbk_storage.js` 有 `let fd = -1` 这类哨兵，UnaryOperator 变异体把它改成 `+1` 后 finally 里 `closeSync(fd)` 变成 `closeSync(1)`——**直接关掉 stdout**；TAP 档下 `#81` 触发 libuv 断言 `uv__close: fd > STDERR_FILENO` 直接 SIGABRT（不可捕获）、`#134` 的 TAP 点落到 stderr，而 tap-runner 的硬规则是「进程非零退出且解析不到失败测试 ⇒ **RuntimeError**」⇒ **TAP 档下「变异体导致被测进程崩溃」被记为 RuntimeError 而非 Killed，是该 runner 的语义损失**。**不用 shim 包装 `closeSync` 来掩盖**——那会改变被测系统的可观测行为、把真击杀变成 Survived。代价如实登记：这 4 段（`v3-entry`/`storage`/`qinglong-push`/`check-deps`）score 与其余 15 段**不同口径**（`coverageAnalysis:'off'`，未覆盖变异体计入 Survived 而非 NoCoverage），**跨段比较分数必须排除**。另有 ② `v3-entry`/`qinglong-push`/`check-deps` 的独立根因（同一手法）：其变异目标只在**子进程**里被执行（`test_utils_pure.js:449` 的 `execFileSync(node,['-e',fallbackProbe])` 在子进程里 `require('./xbk_function_v3')` 且断言只读**子进程 stdout**、`test_qinglong_utils.js` spawn `qinglong/xbk_push.js --status`、`test_check_deps.js` spawn `scripts/check-deps.js`），而覆盖 hook 只预加载在被 tap-runner spawn 的测试进程里 ⇒ perTest 归因不到 ⇒ TAP 档判 NoCoverage 且**不运行任何测试**，静默丢检出；A5 逐变异体 replay 独立量化：`v3-entry` **19** 个（15 NoCoverage + 4 Survived，全部在 command 档是 Killed）、`qinglong-push` **15**、`check-deps` **3**。迁移前置＝让子进程执行也能被覆盖归因或改为进程内调用。工程件：`scripts/tap-shim.js` 从 `spike/tap-baseline` 移植，**除 1 处等价改写外代码行与基线一致**（注释已按 W1 实测更正——见下条与 §3.4；唯一代码差异是 `scripts/tap-shim.js:101` 的 `require('../test_suites.js')` 取代基线 `require(path.join(__dirname, '..', 'test_suites.js'))`——为规避 Codacy 的「非字面量 require」告警，**语义与解析结果实测完全相同**，故**不再声称「逐字节一致」**：全文件 140 vs 121 行、剥离注释后 67 vs 67 行且有 1 行不同）；缓存 key 改为**档位 + 配置指纹 + 依赖指纹 + 源指纹**（`stryker-<段>-cfg-<matrix.config 档位>-<hashFiles(stryker.config.js, stryker.tap.config.js, scripts/tap-shim.js)>-deps-<hashFiles(package-lock.json)>-src-<hashFiles(run_mutation.js, matrix.src)>`，兜底前缀 = **缓存身份里除源指纹以外的全部段**（`stryker-<段>-cfg-<同一档位>-<同一配置指纹>-deps-<同一依赖指纹>-`，**不含 src**）——`tap-shim.js` **必须**在配置指纹里：TAP 档的覆盖归因结果由它决定，不进指纹会让按旧归因算出的 inc 被复用；档位名必须同时在 key 与兜底前缀里，否则某段只改 `config:` 而配置指纹不变时跨档继承会从两条路径一起复活；**`package-lock.json` 必须同时进主 key 与兜底前缀**：旧形态只把它放在主 key 里，dependabot 升 fast-check 4.10.0→4.10.1 后主 key miss、兜底却把旧增量复原，而 stryker 的 incremental-differ 只按**文件内容** diff、不认识依赖版本 ⇒ 19/19 段 100% 复用、依赖升级从未重算（实测 issue #167 对应的 main `schedule` run（head `4c4a433`，2026-09-22T19:45:19Z→19:47:28Z），见 `mutation.yml` 同处注释）。新不变式＝**兜底前缀含除源指纹以外的全部身份段 ⇒ 兜底只允许跨源文件变化**，配置 / `tap-shim` / 依赖任何变化都必须同时换掉主 key 与兜底身份；但测试侧文件（`test_suites.js` / `run_unit_tests.js` / `scripts/mutation-child.js` / `test_*.js`）**有意不入身份**（PR #158 回退测试指纹时的取舍：测试指纹变化频繁、写进身份会让兜底失效且拦不住真根因）⇒ 只改它们时不换身份，4 段 command 档（`coverageAnalysis:'off'`）仍会复用旧结果——这是**已登记的有意取舍**，不是本不变式覆盖的对象。`run_mutation.js` 是**本地**运行器（CI 的 command runner 走 `scripts/mutation-child.js` → `run_unit_tests.js`，**不经它**，见 `stryker.config.js:27` 与 `run_unit_tests.js:27`），它留在 src 段、只进主 key。**一次性代价**：key 形态变更 ⇒ 合并后首次运行 19 段全部未命中 ⇒ **一次真全量**；**#168 合并后首跑** = head `01c0ef89` 的 main push run（15 TAP + 4 command）**19 段全部未命中、真全量**（`run_started_at` 06:30:47Z → `updated_at` 08:01:34Z；瓶颈按 per-job started→completed 计）；此处原先引用的 **86.0min** 实为 PR-1 的 key 变更 head `ca63e71` 的**分支 push** run（16 TAP + 3 command、**非**合并后首跑）；墙钟全表见本条末尾「TAP 墙钟对照」）⇒ `test_ci_skip_suites.js` 的逐字断言同步（**靶向反例未放宽**，另加 key 去 deps / 兜底去 deps / 兜底塞回 src 三个反例）；`@stryker-mutator/tap-runner@^10.0.0` 进 devDependencies（lockfile-only 生成，未走 CI 内联安装回退）。**复用/增量链路一字未改**（cache、`inc-*.json`、`reuse.json`、日报复用小节原样保留——保守路径），`break` 仍 `null`，fail-closed 守卫判据未动。**TAP 墙钟对照（本簇权威归属，其余 bullet 的指针均指向此块）**：全 19 段 TAP 基线（`spike/tap-baseline`，run head `cb247c9`）墙钟 ≈71min（瓶颈=utils 70.25min）、**0 段被 timeout 杀死**（⚠️ 该基线 run 整体结论为 **failure**：唯一失败 job 是 `storage`——按设计触发 fail-closed 守卫判红、**非 timeout**，勿读成「19 段 TAP 全绿」）；PR-1 当时的 16 TAP + 3 command 矩阵实测墙钟 86.0min（head `ca63e71` 的**分支 push** run，2026-09-21T19:47:10Z→21:13:12Z；瓶颈=command 档 qinglong-push 85.3min）；改 15 TAP + 4 command 后 v3-entry 走 command 档、A5 预估 ≈34min < 现瓶颈 `qinglong-push` 85.3min，**但「⇒ 墙钟不变」的预估已被实测证伪**：该 15 TAP + 4 command 矩阵首跑（head `01c0ef89` 的 main push run，2026-09-23T06:30:47Z→08:01:34Z）墙钟 **90.8min**、瓶颈 command 档 `qinglong-push` **90.1min**（较 PR-1 的 85.3min 多 5.6%，成因未归因：该 job 的源文件在两个 head 之间未变、仅 `package-lock.json` 变化），v3-entry 实测 **35.1min**（与 A5 预估 ≈34min 相符）；证据见 `.local/tap-baseline-REPORT.md`。**⚠️ 本机验证盲区（2026-09-22 审查登记，勿把本机全绿当契约已验证）**（这两个测试文件在评审中一直被改、行号反复平移，故下列行号按 **本 PR 落地时的树**给出、并逐处附字面量锚点——按锚点找，别只钉数字）：`test_ci_skip_suites.js:633` 那条「`SKIP_SUITES` 含未知套件须点名」断言（字面量 `错误应点名未知套件`）位于 `try {`(:629) … `} finally {`(:672) 块内——**只有 finally、没有 catch ⇒ 该失败是致命的**；而 Android/Termux 本机 `process.execPath` 指向 `linker64`（不是 node），凡 `execFileSync(process.execPath, …)` 形式的子进程调用必失败——**`spawnSync` 同样失败**（`:599` 的 `runEntry`（定义 `:598`）用的正是 `spawnSync(process.execPath, […/run_unit_tests.js])`），实测文本随参数形态而变：传 `.js` 文件 ⇒ `error: "…/run_unit_tests.js" has bad ELF magic: 27757365`，传 `-e` ⇒ `error: expected absolute path: "-e"`；换用真 node 也无效（node 本体是可执行 ELF shared object ⇒ `/proc/self/exe` 仍指向 linker64），可行绕过是**子进程层替换**（`NODE_OPTIONS=--require <shim>` 预加载替换 `child_process` 的起动路径），`:633` 因此必然炸、进程当场退出。**后果**：该文件里更靠后的这三处纯字符串契约断言——`:1312`（TAP 报告路径 = stryker 默认，`assertReportPaths`）、`:1346`（tap runner 契约，`assertTapRunnerContract`）、`:1440`（矩阵 `config:` 声明，`assertMatrixConfigs`）——**在本机不可达、零覆盖**；但**缓存身份门禁已不在盲区内**：本 PR 把两条不 spawn 子进程的纯字符串门禁（`assertCacheStep` 定义 `:40`、调用 `:92`；`assertCacheConfigIdentity` 定义 `:107`、调用 `:159`）**前移到致命断言之前**，本机实测跑过并在 `:161` 打印 `✅ 缓存身份门禁…正向通过`。实证（本 PR 落地时的树）：把该文件整份复制到**仓库根**的副本（其余内容一字不改）、给 `:159` 与上述三处断言块各注入一个标记后以真 node 实跑，进程仍在 `:633` 以 `AssertionError: 错误应点名未知套件`（`actual: error: "…/run_unit_tests.js" has bad ELF magic: 27757365`）退出；`assertCacheConfigIdentity` 的标记出现 **1 次**（该门禁本机真跑），另外三处标记各出现 **0 次**（`:1312` 的 `assertReportPaths`、`:1346` 的 `assertTapRunnerContract`、`:1440` 的 `assertMatrixConfigs` 全部未执行）。故本机 `npm test` **根本跑不起来**（`run_tests.js:99` 用 `execFileSync(process.execPath, [file], …)` 逐套件拉起子进程，每个套件都必红于该子进程调用），这些契约只能靠 CI（CI 的 `execPath` 是真 node，致命断言不炸，上述三处断言真实生效），或按上条的 shim 绕过法局部实跑。**⚠️ 同理，`npm run test:filter`（CI `quality-gate` 的显式步骤）在本机也不可作判据**：本机未装 `re2`（`require('re2')` 直接 MODULE_NOT_FOUND），而 `xbk_function_v3.js:52` 明确**不回退 V8**、用户过滤正则一律被跳过 ⇒ 2026-09-23 实测 `node test_filter.js` = **771 ✅ / 126 ❌**（失败集中在 listfilter / 过滤解释 / compileRules 等 regex 相关断言，形态**以**「应屏蔽却保留」为主（带屏蔽/拦截语义 **47**/126）；其余按关键词可再分出「应有告警」**8**、「口径一致性/UTC」**4**、「应正常匹配」**9**、FUSE 符号链接环境失败 **2**（与环境有关，见下条）——**这些关键词分类互不互斥**（「应屏蔽却保留」与「应有告警」有 1 条双计），**五类合计只覆盖 69/126**；未覆盖的 **57** 条多为 `compileRules` / `matchesCompiled` 的形态契约（大小写不敏感 / 分支覆盖 / 多重分隔符 / 压力 / ReDoS 跳过嵌套量词等））；换上按上条造的 re2 存根则 filter 段恢复、**formatter 段**（`htmlToMarkdown` / `sanitizeDecodedHtml` / `decodeHtmlEntities`）反成 **719 ✅ / 178 ❌**（存根语义不等价）——两个方向都不是诚实判据，`test:filter` 只认 CI（CI 会「安装并验证 re2 原生模块」）。同一套件总数恒为 **897**（895+2 = 771+126 = 719+178）⇒ 本文件别处引用的 `895 ✅ / 2 ❌` 出自 **re2 可用的环境**，不是现环境的可复现数字。同理 `test_mutation_report_cli.js` 也因同一 `execFileSync(process.execPath, …)` 在本机失败（`runCli` 的调用点**定义处**（`:37`）；本机症状首现于场景 1 的用法文案断言 `:84`（字面量 `应输出具体用法提示`）——该断言已按 R4-F7 收紧到要求 `用法[:：]`，场景 1 因此**不再假通过**；场景 2 的 `code === 1`（`:90`）在本机仍是「假通过」形态（linker64 直接以退出码 1 结束即满足），但场景 1 已先红、它不会被执行）——两者均**非回归**：这两处实现在上一提交 `9a094b6` 中同位置已存在，在 #168 相对 `9a094b6` 的 diff 里输出为空。绕过限制做局部验证的可行做法：把断言函数（纯字符串断言、不 spawn 子进程）**复制到副本**后独立实跑——已据此确认 `assertCacheConfigIdentity` 正向通过，且 7 个反例（key 去档位段 / 兜底去档位段 / key 指纹去 `tap-shim.js` / 兜底指纹去 `tap-shim.js` / key 去 deps 段 / 兜底去 deps 段 / 兜底塞回 src 段）**全部被抓住** ⇒ 该门禁确实有效。
- **TAP 档已知限制（PR-1 登记，证据 `.local/storage-tap-RE-findings.md`）**：① **`storage` 段留在 command 档不是「没问题」，而是把同一个崩溃记成 `Killed`**——W1 实测：屏蔽 SIGABRT 后跑完整 `test_filter.js`，变异 `#81`（`let fd = -1` → `+1`）与未变异对照组**逐条相同（895 ✅ / 2 ❌，两条为 FUSE 符号链接环境失败）**，即**没有任何断言检出它**；它在 command 档被判 `Killed` 完全来自 libuv abort（`uv__close: fd > STDERR_FILENO`，`run_unit_tests.js` 的 `execFileSync` 拿到非零退出）⇒ 该段分数含 **1 个假击杀（约 +0.35pp）**，是用「看起来绿」换掉「诚实地红」。TAP 档的 `RuntimeError` 反而是 fail-closed 的诚实信号（§3.5）。② **一条尚未量化的假绿通道**：TAP 点若因 stdout 被关而落进 stderr，且该测试文件**退出码为 0**，tap-parser 对空 stdout 判 `{ok:true, skipReason:"no tests found"}` ⇒ 该变异体静默变 **Survived**（基线里 `#111`/@L124 是实例，§3.5）；影响面**无法从现有 artifact 量化**（报告里看不出「TAP 流是空的」）⇒ 已列为 **PR-2 候选**：shim 侧**中继子进程**修法（加载期 spawn 一个持有同一 stdout 管道的中继，退出时同步写其 stdin，不动被测系统行为）——**必须**在 stdin EOF 后立即退出并带超时，否则 `parseTap` 会挂死（比现在的 RE 更糟），另需「中继不可用即回退」；以及「TAP 写不进 stdout 即强制非零退出」。两条都**不改变**「不放松守卫、不隐藏红色」的硬约束（§4.1–§4.3）。③ **未回退的同类损失，A5 独立审查逐变异体 replay 量化**（证据 `.local/audit-A5-migration.md`；A5 枚举「不产生覆盖的检出路径」共 9 条命中，证伪了「只有 3 段受影响」的覆盖面判断）——同一手法（变异目标只在**子进程**执行、父进程无覆盖 ⇒ perTest 判 NoCoverage 且不跑测试）：`v3-entry` **19** 个击杀被静默丢弃（15 个 TAP NoCoverage + 4 个 TAP Survived，全部在 command 档是 Killed），机制＝`test_utils_pure.js:449` 的 `execFileSync(node,['-e',fallbackProbe])` 在子进程里 `require('./xbk_function_v3')`，其断言只读**子进程 stdout**（A5 定位到唯一失败用例 `❌ P1-04 … 首次回落必须告警且只告警一次（实际 0 次）`）⇒ **处理＝回退 command 档**，该回退的墙钟代价与「⇒ 墙钟不变」预估被证伪的完整对照见上条「CI 变异已切 TAP 档」的「TAP 墙钟对照」；`message-store` **3** 个击杀（`test_message_store_utils.js:130` 的 `execFileSync -e CHILD_PREAMBLE`，preamble `require('./xbk_message_store')` 再调 `_saveTombstones/_evictTombstonesToSize`）走 command 档需 ≈3.5h（1692 变异体）⇒ **不回退，登记为已知损失**；`app` 段经该子进程路径（`test_ci_skip_suites.js` 里**按字面量 `--only=空数据` 锚定**的那处 `spawnSync`（本 PR 落地时 `:1535`；其上方 `:1523` 是 `const FILTER = '空数据'`）spawn `test_app.js --only=空数据`——它**只跑 1 条 `--only` 过滤契约探针、不是集成套件**）丢失的击杀 **= 13**（A7 已 census 完成，证据 `.local/audit-A7-app-loss.md`：可达集 R=568 个 NC **全量逐个 replay** ⇒ 13 可击杀、R 外抽 305 个 ⇒ **0**（与「未执行代码不可能被击杀」一致），13/2086=0.62%，**仍是下界**；⚠️ 方法学：均匀抽样（每 5 取 1、n=418）只命中 k=1 ⇒ 外推 **5.0**，**错 2.6 倍**——击杀高度聚集，13 个里 6 个挤在同一 `numConfig` 数组字面量上，故此类量化**必须用 census/分层**），且 app 走 command 档 ≈5.5h **超出 step 330min 上限** ⇒ **结构性不可回退**，只能靠「子进程覆盖归因」修复；**⚠️ 但 `app` 段那 2086 个 NC 与上面两类缺口**（`v3-entry`/`check-deps` 的子进程归因失败）**不同源**：其主因是**测试集缺口**——`xbk_app.js` 的测试用例**只有 `test_app.js` 一个来源**（**166 条用例**；其并行调度版 `test_app_p.js` 是 `integration: true`、内部 `fork(test_app.js)` 执行**同一批**用例，`test_app_p.js:25/67`（`:25` 是 `const SRC = path.join(__dirname, 'test_app.js')`，`fork(SRC, …)` 在 `:67`）），而**两者都不进变异测试集**：`test_app.js` 未被 `test_suites.js` 注册（`test_suite_registry.js` 把它列进豁免白名单并注明「由 CI 显式步骤覆盖」），`test_app_p.js` 虽在 `SUITES` 里但 `integration: true` ⇒ 被 `run_unit_tests.js:45` 的 `UNIT_SUITES = SUITES.filter(s => !s.integration && !s.mutationSkip && !skipSuites.has(s.file))`（此前引文截断了尾部条件）过滤掉（`stryker.tap.config.js` 的 `tap.testFiles` 同样不含它）⇒ **TAP 档与 command 档都不跑它**，故这 2086 个在 command 档会被记成 **Survived**（`coverageAnalysis:'off'` 下未覆盖不计 NoCoverage）**而非 Killed** ⇒ **它们不是「TAP 迁移丢失的击杀」**，当成损失会**高估约 160 倍**（2086 vs 实测 13）——**两类缺口必须分开登记**（A7 §8；`app` 的真实覆盖只来自 `test_filter.js` 等 3 个文件在模块加载期 `require` 装配代码时的顺带覆盖：552+13=565=Killed 199+Survived 364+Timeout 2，完全吻合）；`loop`（`test_loop_utils.js:102` 同手法）因该段 NC=0 ⇒ 0 损失。④ **A5 方法学自校（量化纪律）**：同一套 replay 在 `check-deps` 得 **3**，与该段 PR run 的 fresh command 全量（**3**）**逐位一致**；而上一轮脚本 `a5-chk.tsv` 得 15、与任何 command artifact 都不符 ⇒ 判为**坏数据、不可复用**（其余 `a5-ql.tsv`/`a5-ms-msgutils.tsv`/`a5-v3-utilspure.tsv` 经独立复现一致，可用）。⑤ **陈旧 command artifact 是量化陷阱（反例教训）**：`.local/cmd-arts/qinglong-push`（100% 复用）会给出 **254** 个「TAP-NC → Killed」，是真值 **15** 的 **17 倍**，绝不可用于量化；段损失必须取自 fresh command 全量（head `ca63e71` 的 PR run 的 `reuse.json` 为 `mode:"full"`）。**app / message-store 迁移前置**＝让子进程执行也能被覆盖归因，或把相应断言改为进程内。
- 变异报告 artifact 保留期与跨 run 依赖（v3.276 起）：`mutation.yml`「上传变异报告」的 `retention-days` 为 **7 天**（原 14，经批准下调）。唯一跨 run 消费者是 `.github/workflows/analyze-artifacts.yml`——它取 `gh run list --workflow=mutation.yml --status=success --limit=1`（最近一次**成功**运行）的 run-id，再按 `pattern=mutation-report-*` 下载，故**连续 7 天无成功变异运行**即失去输入；两层都是硬失败：`download-artifact` 报 `Unable to find any artifacts for the associated workflow`，`.github/analyze-artifacts.js` 对空目录 `exit 1`（该 fail-loud 行为由 `test_analyze_artifacts.js` 的 A1/A2 锁定，**不得**改成静默降级）。该工作流无 `schedule`（仅自身变更 push 与手动 `workflow_dispatch`），且 `run_id` 输入可显式指定，故恢复成本低：先重跑 mutation 再触发。再下调保留期前必须复核这一窗口，或把选 run 逻辑改为「取仍存有 artifact 的最新成功运行」。`test.yml` 的 `coverage-report`（同为 14 天）无任何下载方，不改。
- 行为、配置或契约变化：同步更新 `README.md`、`SYSTEM_CONTRACT.md`、`CHANGELOG.md`，不让文档落后于代码。**自动化现状（不要误以为有门禁）**：只有 `CHANGELOG.md` 被间接覆盖——`check-version.js` 只校验它的最新版本标题与其余三方一致，**不校验内容是否描述了本次改动**；`README.md` / `SYSTEM_CONTRACT.md` 的同步**完全没有门禁**，靠评审与约定兜。故 PR 说明须点明「动了哪些文档 / 为什么不需要动」，评审逐条核对。
- 文档里**不要写 GitHub Actions 的 11 位 run id**（形如一串 11 位数字）：Codacy 的 PII 规则会判成 `Potential PII: phone number pattern found`（Security/high），而 `Codacy Static Code Analysis` 是 main 的**必需检查**、阈值 **0 个新问题** ⇒ 引用某次 run 一律写 `head <sha>` + 事件（push / schedule）+ 起止时间（UTC），需要 run id 时用 `gh run list --commit <sha>` 反查。

高风险区域：判重/缓存、推送结果、配置兼容、网络请求、正则防护、文件存储与符号链接防御。改动须补回归测试。完整行为约束见 [SYSTEM_CONTRACT.md](SYSTEM_CONTRACT.md)；运行方式见 [README.md](README.md)。
