# 系统契约

适用范围：个人青龙**单实例**部署。代码和测试是实现依据；以下规则改动前必须评估并补回归测试。

## 核心原则

1. 宁可重复推送，不可因失败提前写缓存而漏推。
2. 仅成功推送的消息进入缓存；失败消息下轮可重试。
3. 外部脏输入、安全风险和资源异常应防御；不为多实例等超出单实例边界的场景增加复杂度。

## 不变量

- **身份与去重**：有效 ID 优先；无 ID 时使用归一化后的有效 URL；两者皆无才使用稳定匿名键。批内、缓存和成功写入必须使用同一规则。
- **缓存**：原子写；写成功后才更新内存；规则变更使过滤标记失效并重评；目录可清空后重建。
- **过滤与配置**：用户正则必须经 RE2；新数值配置统一走 `Utils.num`；新增过滤字段同步更新 filter hash。
- **推送**：无通道配置不得静默成功；部分通道成功即视为本轮成功；模板和链接必须走统一安全入口；敏感信息不得进入日志。
- **错误处理**：可重试错误保留重试机会；永久配置/权限/参数错误直接失败；告警、日报、日志失败不得影响主流程。
- **时间**：无时区日期按 UTC 解析。
- **资源**：定时器可清理；异步任务有错误处理；临时文件失败清理；响应体有大小限制。

## 边界

- 多实例不保证最多推送一次；这是单实例项目的明确限制。
- 超时结果不确定时允许重推，优先避免漏推。
- 内存缓存不感知外部修改；单实例中进程内状态为准。
- 顺序推送末条等待等微小开销不为优化而改变行为。

## 修改检查

- 改判重、缓存、过滤、推送、网络或配置：补针对性测试并运行 `npm test`。
- 改版本：同步主文件头、`CHANGELOG.md`、`package.json`。
- 破坏性 Git 操作先备份；密钥与本地配置绝不提交。
