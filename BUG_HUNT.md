# 🐛 真实 Bug 记录（BUG_HUNT）

> 只收录**真实、有意义、可复现**的 bug——真实运行会触发、影响实际用户（收不到推送 / 推送内容错误 / 配置失效 / 状态失真）。
> **明确排除**：边缘型（罕见输入）、罕见类型（真实数据不出现）、难触发（需特定时序/环境）、企业级（大规模并发/多租户）、理论风险（哈希碰撞概率）。

---

## 一、已确认并修复（真实触发，按版本）

### v3.145（告警/统计）
| Bug | 触发场景 | 影响 |
|---|---|---|
| 告警「已发送」误报 | 接口失败时（告警默认开启）| 通道挂时日志显示"已发送"，实际未送达 |
| maxPerRun 截断统计缺口 | 接口异常返回海量数据（>100 条）| 截断的 N 条不计入任何统计，差值凭空消失 |

### v3.146（日报）
| Bug | 触发场景 | 影响 |
|---|---|---|
| 日报「已发送」误报 | **每天跨天**（日报默认开启）| 日报失败仍显示"已发送" |

### v3.147（推送）
| Bug | 触发场景 | 影响 |
|---|---|---|
| wxpusher summary 切断 emoji | 长标题 + emoji | 摘要显示半个乱码字符 |

### v3.148（Server酱）
| Bug | 触发场景 | 影响 |
|---|---|---|
| Server酱 换行翻倍 `\n\n`→`\n\n\n\n` | **每次 Server酱 推送**（Markdown 段落）| 段落间大段空白 |

### v3.149（mdToPlain）
| Bug | 触发场景 | 影响 |
|---|---|---|
| HTML 残留属性 `a href="..."` | `{Html内容}` 模板 + Push+/Bark | 推送内容被标签属性垃圾淹没 |
| 斜体误伤 `5*3*2cm`→`532cm` | 规格/尺寸格式内容 | **数字丢失**（规格错误）|

### v3.151（一言）
| Bug | 触发场景 | 影响 |
|---|---|---|
| 一言 15s 延迟 | 启用 HITOKOTO + 一言 API 慢/挂 | **每次推送延迟 15s** |

### v3.152（长内容）
| Bug | 触发场景 | 影响 |
|---|---|---|
| 原文链接被截断 | 长内容 desp 超 contentMax | 用户看不到原文链接 |

### v3.153（mdToPlain）
| Bug | 触发场景 | 影响 |
|---|---|---|
| 原文链接 `url (url)` 重复 | Bark/Push+ 每次推送 | URL 显示两次 |

### v3.154（wxpusher 单通道）
| Bug | 触发场景 | 影响 |
|---|---|---|
| wxpusher API 失败静默 | 只保留 wxpusher + key/topic 错误 | **消息永久丢失**（主流程写缓存，下次去重跳过）|

### v3.155（时区/残留）
| Bug | 触发场景 | 影响 |
|---|---|---|
| 日报 UTC 日期错位 | 中国时区凌晨 cron | 日报日期错一天 |
| 测试残留乱码文件 | 每次跑测试 | 缓存目录堆积垃圾文件 |

### v3.156（配置空白/状态）
| Bug | 触发场景 | 影响 |
|---|---|---|
| 空白正则编译成 `/   /i` | 复制粘贴配置带空格 | **假过滤**（标题含 3+ 空格被误拦）|
| 空白 pingbitime 静默 value:0 | 复制粘贴带空格 | **时间过滤静默关闭** |
| 告警/日报状态先写 | 通道挂时 | 失败也限频，重试被挡，信息丢失 |
| normUrl 不去 query/hash | 无 id 数据带跟踪参数 | 同内容重复入库、重复推送 |
| saveBatch 更新必误报 | 每次命中更新分支 | 日志失真，误导排查 |

### v3.157（判重/统计）
| Bug | 触发场景 | 影响 |
|---|---|---|
| anonKey 缺价格/品牌字段 | 同标题+内容不同价格 | 不同商品误合并 |
| 非法 pingbitime（'abc'）落 value:0 | 配置写错 | 时间过滤静默关闭 |
| readMessages 数组元素混入 | 缓存损坏/旧缓存 | 判重异常 |
| fetchData 重试门槛不一致 | retry 非法类型 | 实际重试次数与预期不符 |

### v3.158（配置类型）
| Bug | 触发场景 | 影响 |
|---|---|---|
| 数值配置字符串全部失效 | **环境变量/配置文件部署**（cron 常见）| retry/limit/titleMax 等 7 处静默回退默认 |
| enabled 字符串 'false' 不生效 | 环境变量 `enabled='false'` | 告警/日报无法关闭 |
| domain 前后空格 | 配置复制带空格 | pushUrl/外链错误 |
| 408/409 不重试 | 接口临时 408/409 | 瞬时失败直接终态 |

---

## 二、真实验证状态（v3.158，2026-08-03）

| 验证项 | 结果 |
|---|---|
| 真实接口拉取（20 条）| ✅ 正常 |
| 真实过滤（屏蔽分类 + 只看它）| ✅ 逻辑正确（当前数据无命中属正常波动）|
| 真实 wxpusher 推送 | ✅ **20 条全部成功**（9.9s）|
| 缓存去重（二次运行）| ✅ 20 条全去重 |
| 推送格式 | ✅ 无 [object Object]/undefined/空分类 |

**结论：真实运行全链路正常，当前无新的真实 bug。**

---

## 三、待观察（非 bug，真实运行需留意）

| 项 | 说明 |
|---|---|
| 接口数据内容波动 | 每次拉取不同（如某时段无"京东"标题），只看它命中数会变——正常 |
| 过滤条件变更需清缓存 | 改宽过滤后旧条目已写缓存（视为已处理），需 `rm xianbaoku_cache/push.json` |
| 单通道依赖 | 只保留 wxpusher 时，wxpusher 故障=无推送（API 失败已不静默，会重试）|

---

## 四、方法论（怎么找真实 bug）

1. **真实数据驱动**：拉真实接口数据跑完整链路，不用构造数据
2. **真实配置**：用用户实际会写的配置（复制粘贴带空格、环境变量字符串、模板占位符）
3. **真实通道**：wxpusher 单通道真实推送验证
4. **每次验证可复现**：命令 + 输出留档（见 git 提交历史）
5. **判定标准**：真实触发 → 影响用户 → 修；边缘/罕见/理论/企业级 → 记录不修（理由见 REVIEW_DECISIONS）

> 累计：**真实有意义的 bug 修复 30+ 点**（v3.145-v3.158），全部有测试 + 变异锁定。当前真实层面已收敛。
