# Changelog

完整历史以 Git 提交记录为准。

## v3.269

- 增强告警留痕、可重试失败退避与通道即时重试。
- 修正相关评审问题并通过完整测试。

## v3.270

- 常驻模式：可重试错误不再连续 3 轮退出，改为指数退避持续重试（默认 30 分钟封顶，`XBK_RETRY_BACKOFF_CAP_MS` 可调），仅不可恢复错误退出，消除对外部重启器的依赖。
- 变异测试：V3 重拆 5 段、sendnotify 补齐尾部，修复 4495-4924 约 430 行静默漏测；新增 `scripts/check-mutation-ranges.js` 行段覆盖校验并纳入 `npm run check`，文件增长后 CI 自动拦截。

## v3.271

- 判重 URL 归一化修正：去 hash，只忽略 `utm_*`/`fbclid` 等跟踪参数，业务 query 保留（此前整段 query 被去掉，带业务参数的同一资源被判为不同而重复推送）。
- HTML 转 Markdown 只读取标签真实属性层：`title`/`data-*` 等属性值里形似 `href`/`src` 的文本不再被误当链接或图片。
- `push_config.local.js` 加载失败不再回显原始异常信息（可能带密钥片段），改为固定提示。
- 日报状态 `date` 字段非法时保留原文件并跳过更新（避免累计状态被重置）。
- WxPusher profile 日志不再泄露短 appToken。

## v3.272

- 日报新增当天运行轮数统计与展示，继续采用原子状态写入和跨天重试语义。
- 新增按通道记录的连续失败、限频异常告警与恢复提醒；健康告警不参与健康统计，且失败不会影响主推送、缓存和重试。

## v3.273

- 新增 `--status` 只读运行状态命令：汇总最近运行、日报、通道健康与过滤诊断；不加载推送依赖、不抓取、不推送、不写文件（缓存目录可用绝对路径 `XBK_CACHE_DIR` 指定）。
- 新增过滤诊断日志 `filter-diagnostics.ndjson`：每轮一条 `type: "run"` 汇总，外加被过滤/被强制展现保护的明细；默认每轮最多 100 条、日志超 1 MiB 时保留最新尾部，可在 `diagnostics.filterLog` 调整。
- 青龙入口先检查/恢复依赖再加载应用：`got`/`re2` 缺失时不再在加载阶段失败，并给出部署命令；仅在显式设置 `XBK_AUTO_INSTALL_DEPS=1` 时自动安装与重建 re2。
- 推送安全：PushPlus 正文与 wxpusher 等 HTML 渲染通道统一共用出口清洗（实体编码的主动 HTML 不再还原成活标签）；模板占位符改为单趟替换（数据值中的字面占位符不再被二次替换）；Markdown 链接目标含 `<`/`>` 时按 URL 编码，避免链接被提前截断。
- 通道配置：Bark 扩展参数（archive/group/sound/icon/level/url）与 `QYWX_ORIGIN` 支持环境变量覆盖（此前只认 `push_config.local.js`）；「已配置通道」判定统一（空白值/`0`/`false` 不再计为可用通道，消除自检报有通道而主流程零通道漏推）。
- 稳定性：一言请求关闭 got 内置重试（不再吃满 10 秒推送预算导致整轮取消）；一言文本拼接后再清洗孤立代理（避免通道每轮 URIError）；WxPusher 取消不再被误判为限频逐个重试；emoji 修饰符截断与主实现对齐。
- 可观测性：dry-run 不再报「N 条失败，下次运行重试」（未推送条数另记）；身份无效条目单独计数（终端与 `run.log`）；`run.log` 回显清洗控制字符，防止接口字段伪造日志行；一言获取失败的日志改经 `safeErr` 脱敏（不再原样打印异常 message）；日报/通道健康整体异常补 WARN 留痕；告警留痕的版本号改为入口注入（缺 `package.json` 的部署不再静默失效）。
- 性能：HTML 转 Markdown 的未知标签闭合探测与 URL 归一化改为线性算法/单次预扫描，消除单条长消息可触发的 O(n²) 阻塞。

## v3.274

- 时间口径：运行日志与台账时间戳固定为 `Asia/Shanghai`（格式不变），不再随部署时区变化，与日报日界统一（此前 CI/容器以 UTC 运行时，日志行日期可能比日报早一天）。
- 接口超时钳制：`api.timeout` 统一取整并钳到 `[1, 2^31-1]` 毫秒（小数向上取整），非正或非法值回落默认 5000；此前 `0.5` 或超界值会被 Node 定时器归一到约 1ms，导致每次请求瞬间超时。
- 缓存安全：空字符串路径显式判为不安全并拒绝写入（此前会在进程 CWD 落一个含内容的临时文件）；`readSafeText(filePath, maxBytes)` 支持透传读取上限。
- 过滤：白名单过滤对非字符串字段值统一 `String()` 化（与 `matchesCompiled` 同口径，不再按对象 JSON 化/函数置空）；`checkFields`/`checkCategory`/`checkRegisterTime` 收到未编译或错形状参数时显式告警并保守放行，规则编译失败同样补告警；配置校验对含零宽字符的正则与超上限的 `pingbitime` 补告警，并改为逐字段安全读取（抛错 getter 只告警并跳过该字段，不再中断整轮校验）。
- 推送：HTML 正文超过 100000 字符改用 UTF-16 安全截断（不再按码元切出半个 emoji）并打告警；整体超时的错误对象顶层补 `code='PUSH_TIMEOUT'`，配置通道清单不可得时告警留痕。
- 模板与清洗：`{Html内容}` 在无 url 时不再输出悬空的「原文链接：」，url 被 `safeUrl` 过滤但原始值非空时保留纯文本提示、不生成 href；Markdown 链接/图片的闭合点按 `(…)` 配平扫描（URL 内含 `)` 不再残留 `.jpg)`），配平失败回退「首个 `)`」而不是放弃整段。
- 常驻循环与失败归类：未传 `onError`/`onIntervalError` 时分别由独立默认处理器打诊断日志（单轮失败与性能预热失败不再互相误标、也不再静默吞错）；`summarizeError` 递归加祖先环守卫与深度上限（自引用结构不再抛 `RangeError`），`failureInfo` 透传前同样脱敏/折叠换行/截断，全部通道可重试时聚合原因细分标为 `ALL_CHANNELS_RETRYABLE`。
- 预热与墓碑缓存：`prewarmTls` 的 `count` 非有限值钳为 1（此前 NaN 静默报成功、Infinity 抛 `RangeError`）；墓碑淘汰在无法达标时返回 `null` 并放弃持久化（此前死循环挂起进程），墓碑读取失败与写入门径失败补告警。
- 清理：移除死代码——`xbk_agents` 的 `DNS_CACHE` 死导出、`xbk_rules` 未被调用的正则编译包装、`xbk_utils._parseFallback` 不可达的 `/` 分隔日期分支、`xbk_message_store` 的死状态 `_nowInc`。
- 接口响应：带 UTF-8 BOM 的合法 JSON 不再被判 `ERR_BODY_NOT_JSON`（此前会让常驻循环按永久错误停推）；`XBK_PROFILE=3` 的请求日志 URL 只保留 origin（非末段密钥不再进日志）；响应体非法时不再回显响应体内容、只报长度。
- 入口与状态：青龙入口未识别参数、`XBK_CACHE_DIR` 非绝对路径、Node 低于 `engines` 要求均补告警（不再静默），依赖恢复改用现行等价的 `--omit=dev`，`--check` 失败项带上原因、DNS/TLS 预热统计按任务类型绑定（TLS 不再计入 DNS 计数）；`--status` 的 `run.log` 摘要解析锚定行首（噪声行不再误命中）、`report.state` 缺字段不再整表判 invalid，输出补「待推送（截断）」「截断」「耗时」。
- 门禁与工具链：单元入口新增每套件硬超时 `XBK_UNIT_TIMEOUT`（默认 10 分钟，超时 SIGKILL 并按失败结算）；`check-version.js` / `run_unit_tests.js` 改用 `process.exitCode` 收尾（管道场景不丢错误详情）；变异日报对预期之外/重复分段 fail-loud、报告顶层非对象时报带上下文的错误；行段校验补 `start > end`、非末段越过 EOF 与全文件形式的幽灵目标三类 fail-loud，对 `MUTATION_WORKFLOW_TEXT` 注入打告警（区分注入与真实文件读取），变异报告 JSON 读取失败补路径上下文；`install-hooks.js` 读取 `core.hooksPath` 出错时 exit 1 且不写配置（保留 git 的 stderr），空串值按未配置处理并写入 `.githooks`；`isValidVersion` 补 `typeof` 守卫；`xbk_sendNotify_slim.js` 段二行段随行数同步为 `751-1505`。

## v3.275

- 常驻循环 `sleep`：`signal` 加 `typeof` 守卫（此前传入非 AbortSignal 的真值对象会先抛 `TypeError`，定时器回调里再抛一次成为未捕获异常终止进程）；毫秒值统一经 `clampTimerMs` 钳到 `[0, 2^31-1]` 再交给 `setTimeout`（此前 `1e12` 被 Node 静默降为 1ms，「等一天」变成立即返回）。
- 失败归类：`classifySummary` 的「本轮有无失败」只由 `failed` 决定（此前 `total` 缺失/非数字时 `Number(total)||0` 使有失败的摘要返回 `null`，被调度器读成成功）；企业微信 `93000`（invalid webhook key）与文本兜底 `invalid [webhook|access|api] token|key|parameter` 判为永久配置错误（此前落 `UNKNOWN`，常驻对已失效 webhook 无限退避重试）。
- 脱敏：`redact` 补齐 `Authorization: Bearer|basic <凭据>`、JSON 引号形态 `"appToken":"…"`、单引号/带引号关键字/非字符串 JSON 值/URL query 形态与裸 `Bearer|Basic <凭据>`，关键字表补 `password/pwd/session/cookie/credential`（此前这些形态的凭据原样进日志与告警，只抹掉 scheme）。凭据值以 `\s,;}&]` 为边界；裸 scheme 要求凭据 ≥8 字符，避免误抹普通英文句子（`the bearer of good news`）。
- TLS 预热：`prewarmTls` 的 `count` 补上界（钳到 64）——此前 `1e10`/`2^32` 让 `Array.from({length})` 抛 `RangeError` 整体 reject，略小的值则真的发起海量并发连接。
- 时间口径：运行异常告警正文的时间、RE2 缺 re2 提醒标记的保留期 cutoff 统一 `Asia/Shanghai`（此前前者随进程时区，后者与标记名基准被拆开，CI 的 UTC 下与 run.log/日报错开一天）。
- 变异工具链：报告 JSON 的 V8 字符串上限判定移到 `Buffer.concat` 之前（此前先付出分配峰值才发现放不下，且该护栏长期无法被测试触及）；变异日报段内容校验补 `files` 映射缺失/类型非法、`mutants` 非数组、零变异体三类 fail-loud（此前缓存回填的陈旧 artifact 会被当成 0 变异体的满分日报发布），`validateSegments` 的错误逐段带上原因；CLI 集成测试夹具改为真 stryker schema（补齐 `schemaVersion`/`thresholds`/`source`，并用 ajv 对齐厂商 schema 逐条自校），断言收紧到合计/分段统计值。
- 独立对抗审查跟进：脱敏再补引号包裹/带引号关键字/单引号/非字符串 JSON 值/URL query 等漏抹形态与关键字表（`password/pwd/session/cookie/credential`），并收掉本批新引入的 `the bearer of good news` 假阳性；变异体 `status` 改按厂商 schema 的 `MutantStatus` enum 校验（此前只要求「非空字符串」，`'Bogus'` 会被静默计入 `total`）；`refreshTimeoutError` 的归因按实际分支改写（NaN→回落默认、负值→按下界，不再一律说成「超出上限钳制」）；`sleep` 摘除侧守卫补断言（此前后变异掉也不会变红）；APP-05 用例改为快照-恢复，不再删掉共享缓存目录里的既有 `re2warn.state.*` 标记。

## v3.276
- 钩子分层：`test:filter`（本机 ~60s，占原 pre-commit 耗时 72%）从 `pre-commit` 迁到新增的 `pre-push`，提交前阻塞由 ≈82s 降到 ≈23s；**门禁覆盖未减少**（同命令、同退出码、仍在推送前强制拦截）。`pre-commit` 保留 lint / 版本闸门（四方）/ 变异行段校验三道，并统一为「成功安静、失败原样回显 + 逐门禁计时」；新增 `npm run hooks:verify` 只读自检门禁是否真的生效（`hooks:install` 对跳过场景按设计仍 exit 0，不能当装好的证据）。

- 清洗链按 HTML5 词法状态判定（`xbk_utils._htmlTagSpans`）：只在 `<` 后紧跟字母（开始标签）或 `/`（结束标签）时才开启标签区间——此前未配对的 `<`（`1 < 2`、`价格 <100 元`）会把区间一直延伸到串尾，把后方纯文本里的 `name="…"` 误判成「标签内属性」；同时增记 `valueQuotes`（正扫时处于引号外状态遇到的引号位置），只有真正开启属性值的引号才允许整段占位，杂散引号拼出的伪属性对不再屏蔽其后的 `on*` 事件属性（安全缺口）。
- 清洗链明确 HTML5 数据态下「哪些前缀不进属性状态机」（`<!`/`<?` 一律按文本跳过）（同上 `_htmlTagSpans`）：标签起始集合由 `[A-Za-z/!?]` 收窄为 `[A-Za-z/]`——`</` 后跟字母走标签名态，`</` 后跟非字母按 HTML5 bogus comment 吃到下一个 `>`，`<!`、`<?`、空白、数字、`=` 一律按文本处理；这三类区域**一律不记 `valueQuotes`**。此前它们被当成普通起始标签跑属性状态机，`</x='><img src=x onerror=alert(1)>'` 这类「伪注释/端标签前缀」里的杂散引号因此被记成「属性值开启引号」，回扫出的伪属性对被整段占位、段内真实 `<img>` 的 `onerror` 逃过 `_stripEventAttrs` 原样出网（`</x=`、`</ x=`、`<! x=`、`<? x=` 四条载荷同源；修复后四条在 domino 与 parse5 双解析器下均已关闭，定向差分 207,360 条存活由 810 降到 0，良性语料 200,015 条仅该恶意载荷一条输出改变）。
- 属性值引号只在 `=` 之后定界（`xbk_formatter._quotedAttrSpans`，与 `_readTagAttrValue` 同口径）：未加引号的属性值里的撇号（`<img alt=it's>`）不再被当成引号起点、吞掉其后的 `<a>`/`<h1>`；`=` 之后（可含空白）的引号仍是属性值区间。
- 清洗链内部正则 RE2 兼容：href/src、`_cleanNavAttrs`、`_cleanSrcsetAttrs`、`_cleanStyleAttrs` 四处含反向引用（`\1`/`\2`）的成对引号正则改写为「双引号支 | 单引号支」等价形态（href/src 一并改经 `safeRe` 编译），`sanitizeSurrogates` 去掉 lookaround——此前这些内部模式在 RE2 下不被支持，清洗会在启用 re2 的部署上降级。
- `safeRe` 回落留痕：`new RE2(...)` 编译失败回落原生 `RegExp` 时按**进程一次**告警（模式串与失败原因都经既有 `summarizeError` 脱敏/截断）——此前该分支是空 `catch`，「RE2 静默降级」与「线性防护对该模式不成立」这件事在日志里没有任何痕迹。同一进程只报一次（热路径内部正则都过 `safeRe`，逐条打印会刷屏）。
- ReDoS 防护 `hasNestedQuantifier`：取反字符类 `[^...]` 改为扫描到未转义的类结束符（此前只判 `^` 后一个字符，类体被当普通模式解析——`[^(a+)+]x` 被误拦、`(x[^)]+)+` 因 `)` 被吞进类体而漏检；`[^]` 非空字符类语义不变）。
- 出口清洗时序：一言文本拼接后再补一次出口清洗，消除「清洗作用于 append 之前、`contentType` 判定作用于 append 之后」的不同步——此前 slim 追加的一段一言 HTML 未经清洗即出网。
- 推送出口响应体上限：slim 的 `$.post`/`$.get` 补 20MB 流式响应体上限（复用 `xbk_http.DEFAULT_MAX_BODY`：官方 got 走流式限长读取、超限报 `EBODYLIMIT` 并销毁流，无 `stream` 的 got 替身仍走原 promise 路径），此前会把整段响应体读进内存；流式请求入口 `streamRequest` 补 method 白名单闸门（只放行 `post`/`get` 并静态分派，其余 fail-closed 抛错，不再按非静态数据取 `got.stream[method]`）。
- `WX_pusher_channels` 丢弃告警：多应用分流配置被丢弃（非法 JSON、数组项同时缺 `appToken`/`topicIds`、形状不是数组）时显式告警并给出原因，此前静默回退旧字段，用户看到「推送成功」却少推其余应用；空串与显式空数组不告警。
- 失败归类 · 终态 3xx 判永久：HTTP 层对终态 3xx/304 带真实状态码抛出（此前只看 `>= 400`，3xx 落进 JSON 解析分支报 `ERR_BODY_NOT_JSON`，错误码既误导又随响应体形态漂移），失败策略在数字 `code`、`providerCode`、`HTTP_` 前缀与 `statusCode` 四种承载字段上按 300–399 区间统一判 permanent——此前同一 3xx 在错误码路径落 UNKNOWN（可重试）、在状态码路径却判永久，分类相反。
- 失败归类 · 空响应体单列 `ERR_EMPTY_BODY`：2xx 空体、剥离 BOM 后的空体与纯空白体不再与「返回了内容但不是 JSON」共用一个码，改判**可重试**（上游连上后未写体即结束是典型瞬时故障），并显式列入 `RETRYABLE_CODES`（归类理由即本码本身，不靠 UNKNOWN 兜底）；非空但非 JSON 仍是 `ERR_BODY_NOT_JSON`（永久），两者互不放松。
- 失败归类 · 4xx 与子级仲裁：408/409/425/429 在数字 `code`、`providerCode`、`HTTP_` 前缀与 `statusCode` 四种形态下同判可重试（此前数字 `code`/`providerCode` 路径一律 permanent，与状态码路径相反）；子级仲裁同时认 `error.failures` 与 `failureInfo.failures`（青龙形状），父级标了 `failureKind: 'permanent'` 也不再压掉子级的 retryable。
- 请求层不可重试判定补错误码维度：`PERMANENT_CODES`（JSON 契约/证书/URL 参数类）与请求层单列的确定性失败码 `EBODYLIMIT`（响应体超限）命中即首次失败即抛，不再退避重试满 `api.retry`（此前只认 HTTP 状态码，这类错误白跑 1s+2s+… 退避）；未知错误码保持保守重试口径。
- 请求超时口径：`api.timeout` 只接受 ≥100ms 的整数并钳到 `[100, 2^31-1]`，非整数、亚 100ms（单位误填，如按毫秒写「5 秒」）与非正值一律回落默认 5000 并告警（越上界同样留痕）——此前小数被向上取整、小值原样传出，会让每次请求瞬间超时，且现象是「请求超时」而非「配置有问题」；`xbk_http.fetchJson` 在调用方未显式传 `timeout` 时注入默认 30000 ms（显式值优先），此前 got@11 默认 `timeout:{}` 且共享 Agent 不带超时，服务端半开会让请求永久挂起。
- 重试退避遵守 `Retry-After`：只认 RFC 9110 §10.2.1 的合法形态——`delta-seconds`（非负整数、上界 2^31−1）与三种 HTTP-date（IMF-fixdate、rfc850-date、asctime-date；后两者为 obs-date，asctime 无时区标记故显式按 GMT 解析，IMF-fixdate/asctime 再交叉校验 day-name 与日期一致——rfc850 因 2 位年的 50 年切点口径不做该校验，也不校验真实日历日，属既定遗留工单 T2），空串/`1.5`/`-5`/非日期文本/ISO-8601 等一律回落指数退避且日志不得标注「按 Retry-After」；命中时按服务端指示等待、不叠加抖动，与指数退避两条路径统一受 30s 上限保护。此前 `Date.parse` 的接受面过宽，会把「已过期」的非法头钳成 0 后谎报来源，而外层退避曾完全不读该头、在限流窗口内按固定指数退避反复撞墙。
- DNS 与预热：`prewarmDns` 与真实请求共用同一缓存条目，key 为 `hostname|family|hints|order`（`all` 只决定回调形状、不入 key）——此前把 Node 传入的 `hints`/`all`/`verbatim` 全文编进 key，预热写进一个永不被读的条目、默认配置下 DNS 预热完全无效；只收敛到 `hostname|family` 又反向出错：缓存的是「解析器按调用方选项筛选/排序后」的地址，带 `hints`（如 `ADDRCONFIG` 剔除 AAAA）或自定义 `order` 的真实请求会在 TTL 内复用不匹配的地址，故 `hints`/`order` 重新入 key，且 `prewarmDns` 经 `productionLookupOptions()` 与生产**同源取值**（未指定 family 时 `hints=dns.ADDRCONFIG`、指定时 `hints=0`）；底层统一按 `all:true` 解析、按各调用方 `all` 适配回调形状；`ETIMEDOUT` 计入 DNS 失效码（重试窗口远短于 60s TTL），证书主机名不匹配不再计入；预热结果补 `kind:'dns'|'tls'` 可按字段区分；got 替身缺 `stream` 时 `prewarmTls` 报 `ok:false`，不再未建连却报成功。
- 推送超时按在飞通道归因（可选契约 `params.inFlightTracker`）：Pusher 把追踪器交给投递层，slim 在启动通道任务前写入 pending（未结算通道名数组）、每通道 settle（含同步抛错）时移除，**不传该参数零副作用**。10s 整体超时归因改为优先取在飞清单——此前一律按静态配置清单把**所有**通道标成 `PUSH_TIMEOUT`（含已送达通道），污染通道健康统计与下轮重试判断；空数组是有效状态（全部已结算），与「无该能力」严格区分，两者都拿不到时才补「failures 为空」的告警。
- 通道健康告警：失败告警改为按「告警尝试」计时（排入告警时即落盘 `lastAlertAt`，发送成功与否不影响 `intervalMs` 限频）——此前只在发送成功后回填，告警通道不可用时限频完全失效、每轮重发；恢复告警改为确认送达后才清零（新增 `recoverAlertPending`），发送失败保留 pending、下一轮重发，此前发送失败即永久丢失恢复通知；恢复告警的发送改为在健康状态锁内**原子认领**（唯一令牌 `recoverAlertClaim` + 60s 租约），重叠运行的第二轮看到仍在租约内的认领只跳过、不再各发一条重复通知，只有令牌匹配的送达才清零（迟到的成功不会清掉后继尝试已重新认领的 pending），失败在锁内释放认领、下一轮无需等租约即可重发，超租约的陈旧认领可被接管。
- 单条推送渲染异常按单条失败：pushOne 的渲染段（对象安全复制、链接提取、模板替换、代理对清洗、UTF-16 安全截断、链接保留与 dry-run 预览）纳入 try，渲染期异常按「单条推送失败」处理（警告 + 不写缓存 + 下轮重试），不再冒泡中止整轮并跳过缓存写入。
- 缓存与判重：`_identityIndex` 加失效检查（数组引用 + 首元素 + 长度，陈旧即重建并复检当前元素），消除 `has` 因索引陈旧而漏推；未命中路径再补两条**有界旋转抽查窗**（引用层 32 个位置 / 身份层 2 个位置，每数组版本首次未命中同样重建一次），闭合 `missVerified` 粘滞——此前索引一旦标为「已核实未命中」就不再自愈，被调用方原地改写的数组会让 `has()` 恒判 false、同一批消息每轮重复推送；`_tombstoneLoaded` 改为「读到内容」或「确认缺失」才置位（ioError/unsafe/tooLarge 下次重试，此前一次读故障即永久停用墓碑）；启动清理只按精确锁形状匹配（覆盖 `.seen.cleanup.lock`/`.reclaim`），不再误删 `.seen.cleanup.lock.json`；`saveBatch` 全部出口返回布尔（`true` = 已落盘/无需落盘，`false` = 拒绝或落盘失败）并把落盘结果进摘要与 `run.log`；缓存文件名/路径单射化（`getFileName` 按来源分派：`.` 开头的末段 → `url_`+原名（不再生成隐藏文件）；`url_` 开头且已带 `.json` → `url_`+原名；`url_` 开头但未带 `.json` → `url_`+原名+`#` 再补后缀——三支像集互不相交，`#` 又不可能出现在清洗后的末段里，`https://x/.json`、`https://x/url_`、`https://x/url_.json` 三个不同 pushUrl 因此不再互相覆盖判重记录（上一版对两类统一前置 `url_`，`url_` 与 `url_.json` 仍撞名）；`getFilePath` 的 200 字节截断改为附全名摘要，消除「仅第 200 字节之后不同」的两个长名互相覆盖）；`filterHash` 折入「规则是否实际编译生效」维度，re2 从不可用到可用（或反之）不再让已过滤记录跳过重评。
- 存储与文件安全：`writeAtomic` 在 rename 前 fsync 临时文件、rename 后**尽力** fsync 父目录（目录 fsync 不被文件系统支持时只告警，文件 fsync 失败 fail-closed）；`writeAtomicIfAbsent` 写失败清理本次调用创建的半写残骸（不删他人文件）——先 `closeSync` 再 `unlinkSync`、关完置 `fd = -1`（Windows 上文件句柄未关时 unlink 抛 EPERM/EBUSY，残骸留在缓存路径会被消费侧 `existsSync` 当成「已初始化」永久不自愈；置 -1 避免对复用的 fd 号二次 close）；内容读取与大小判定走同一 fd（`readFdRange` 单次分配的有界读取，读取字节数不超过 `fstat` 观测值）；`maxBytes` 只接受正的安全整数，其余值沿用「不设限」语义但必须告警；`ensureParent` 新建目录显式 `0o700`，已存在目录不 chmod。
- 状态、锁与青龙入口：跨进程锁（`run.log` 与通道健康）的陈旧回收改为「mtime 超龄**且**持有进程已确认退出」，锁内改写 `pid:starttime` 令牌供存活复核与 PID 复用判别——此前只看 mtime，被暂停 >10s 的存活进程锁会被第二个进程抢占；通道健康告警的网络 await 移出临界区（锁内只读改写、锁外发送），告警发送失败不再让其他轮次跳过健康更新。`--status` 复用生产的 `resolveCacheDirInRoot` 解析并打印生效目录（含根内校验与多级回退），此前硬编码默认目录，生产会拒绝并回退的状态被静默读成「缺失」；`--status` 对超限 `run.log`/诊断日志读尾部（不再整体不可见）、保留最近一轮时间戳并对中断轮标记「未正常结束」、`report.state` 的 `date` 按真实日期校验、`pending` 段按生产 `_loadReportState` 同口径校验（非法即判「状态损坏」，此前完全不看 `pending`，生产已判损坏并跳过日报更新的文件在 `--status` 里被读成健康）、通道健康逐条容错。青龙入口 `--dry-run` 改为一次性单轮执行（跑完即退出，0/1 对应本轮成败），依赖探测按 Node 解析口径、恢复改用冻结 `npm ci`、依赖名经 npm 包名白名单清洗后才拼入固定 `node_modules` 路径（非法名 fail-closed 抛错，消除把输入拼进 `path.join` 的形态），`--check` 的 Node 闸门按 `engines` 完整数值比较且常驻路径同源告警；单次入口的失败分类异常补诊断输出，8 个本地模块 `require` 纳入 `XBK_PROFILE=3` 画像。
- 变异与 CI 工具链：`scripts/mutation-json.js` 改为**单次 `open` + `fstat` 按 fd** 预检大小与文件类型再读（超限/非普通文件带路径拒绝，消除 `statSync`→`readFileSync` 的 check-then-use 竞态；此前尺寸守卫落在 `Buffer.concat` 分配峰值之后），预读上限改为生产策略值 2 GiB 并由 `XBK_MUTATION_REPORT_MAX_BYTES` 覆盖（`off` = 只留 Buffer 边界）；`scripts/mutation-report.js` 补陈旧（缓存回填）闸门（以本批报告最新 mtime 为基准，偏差超过 `MUTATION_REPORT_MAX_SKEW_MS`（默认 12h）的段判陈旧并拒绝发布，另加 wall-clock 年龄与本轮运行起点两层判定）与去重列表查询整体容错（含 15s `AbortSignal` 超时）；`run_mutation.js` 断点绑定指纹、缺失/不等即响亮丢弃——指纹覆盖「变异集 + 被变异源文件原文 + 参与判定的测试文件内容」三块输入（后两者本轮补入：只改测试文件、或等长改源码同样会让断点失效，避免旧断点的 killed/survived 被错记到别的候选；清单排序用显式码元序比较器而非随宿主 locale 变序的 `localeCompare`，指纹跨机器稳定）、未判定变异体计入非零退出、超时改杀整个进程组、默认变异目标补齐到与 CI 矩阵一致并加入三方对账门禁、沙箱注入 `PERF_MS=3000`；`scripts/check-deps.js` 探测清单改由 `package.json` 运行时声明派生、判定改「先 resolve 后 load」（区分「缺少」与「已安装但不可用」并输出根因）、补 Node 版本闸门（仓库与 re2 两处 `engines`，含 X-range 语义）与 `require.main` 守卫、`load` 与 `resolve` 统一以项目根为基准；`run_tests.js` 每套件硬超时（套件以 `detached` 自成进程组，超时按 `kill(-pid)` **整组杀伤**、派生后代一并清掉——此前只杀直接子进程，孤儿后代继续跑；超时按失败结算）、测试入口前置门纳入 `devDependencies`、两个入口汇总行补三数字格式；`scripts/install-hooks.js` 新增只读自检 `--verify`；`xbk_function_v3.js`/`xbk_sendNotify_slim.js` 变异行段同步重拆至 442 行（1 段全覆盖 `1-442`）/1634 行（`1-750` + `751-1634`，与 `.github/workflows/mutation.yml` 一致），行段回归用例改从矩阵自取行数。
- 变异**分数门禁**（此前没有任何分数门禁）：`stryker.config.js` 的 `thresholds.break` 由 `null` 改为 `65`（按**段**判定——`mutation.yml` 每段独立 `npx stryker run --mutate "<段>"`，`@stryker-mutator/core@10.0.0` 的 `determineExitCode` 在 `mutationScore < 65` 时以退出码 1 结束，故某段跌破 65 时该矩阵 job 红、其余段照跑、日报照发）。取值依据为实测基线（2026-09-18 日报：合计 81.27%，最低段 `message-store` 69.38%），阈值低于最低段 4.38 个百分点 ⇒ 按当前基线不会误红，而单段真实退化**超过 4.38 个百分点**（即分数严格跌破 65，`determineExitCode` 用的是 `<`：69.38→65.00 不红、→64.99 才红）即被拦下；且日报脚本的计分分母（`total`，含 RuntimeError/CompileError/Ignored/Pending）不小于 stryker 的 `totalValid`，同一份报告在 stryker 侧的分只会更高。`scripts/mutation-report.js` **有意**不加分数 throw（其 `main()` 先验证后发 Issue，会在最需要看分数时连日报一起吞掉），只保留既有的缺段/多余段/重复段/陈旧报告四类 `throw`（完整性真门禁，未放宽）。「只删/弱化测试」这条边界已在 PR #156 返工中收口（缓存 key 与 `restore-keys` 前缀纳入测试侧指纹，见下条），残余边界只剩分数门禁的固有性质：**弱化测试后新跑一轮的分数仍 ≥65 就照样绿**（它拦的是分数真的掉下来，不是测试被人改过）。（**本条依据已于本轮证伪**：`break` 已由 65 改为 `null`，见下方「变异测试假 Killed 根因定位与门禁重定标」条。）
- 文档与契约：版本闸门 `check-version.js` 升级为**四方一致**——基准取主文件头，`CHANGELOG.md` 最新、`package.json` 与新增的 `package-lock.json` 两处根元数据（顶层 `version` 与 `packages[""].version`）任一不符即红；锁文件两处字段缺失/形态异常 fail-closed（不给「字段读不到就跳过」留口），并显式要求补丁段为 `.0`（主文件头/CHANGELOG 处在 `x.y` 而 `package.json` 写成 `x.y.5` 这类「同 base 不同 patch」的漂移必须红）；「CHANGELOG 最新」改取版本号**最大值**（数值比较），倒序/顶插不再误红；订正 DNS 缓存 key 注释为 `hostname|family|hints|order`、`xbk_app` 不可达分支契约注释、`stryker.config.js` 里 html 报告的 Stryker 默认路径（`reports/mutation/mutation.html`）；README 同步 `--dry-run` 一次性语义、`--status` 缓存目录口径、Node 版本闸门与依赖预检口径、`api.timeout` 新口径与变异工具链环境变量。AGENTS.md / CONTRIBUTING.md 同步记录变异分数门禁的判定范围、取值依据与回退方式。
- 变异报告 artifact 保留期：`mutation.yml`「上传变异报告」的 `retention-days` 由 14 天改为 **7 天**（用户批准）。唯一跨 run 消费者是 `.github/workflows/analyze-artifacts.yml`：它按 `gh run list --workflow=mutation.yml --status=success --limit=1` 取**最近一次成功运行**的 run-id，再以 `pattern=mutation-report-*` 下载，因此「连续无成功运行」的容忍窗口随保留期由 14 天缩到 7 天；`mutation.yml` 本 run 内的 `report` job 是同 run 下载，不受影响。该工作流**无 `schedule`**（仅自身变更 push 与手动 `workflow_dispatch` 触发），且 `run_id` 输入可显式指定，故暴露面小、恢复路径明确（先重跑 mutation 再触发）。两侧失败都是**硬失败、非优雅降级**：`download-artifact` 报 `Unable to find any artifacts for the associated workflow`；`.github/analyze-artifacts.js` 对缺失/空目录 `exit 1`（由 `test_analyze_artifacts.js` 的 A1/A2 锁定，不改）。`test.yml` 的 `coverage-report`（同为 14 天）**未改**：该 artifact 无任何下载方，纯 UI 留档。策略与回退口径记入 `AGENTS.md`「测试与文档门禁」。
- PR #156 返工（外部评审 7 条全部落地，逐条附「回退即真红」实测；无一条靠解释/注释结案）：① **变异增量缓存 key 与 `restore-keys` 前缀纳入测试侧指纹**（`scripts/mutation-child.js` / `run_unit_tests.js` / `test_suites.js` / `test_suite_registry.js` / 全部 `test_*.js`，即 commandRunner 真正执行的那条测试链），并**删除**原来无条件的裸兜底前缀 `stryker-<段>-`——`coverageAnalysis:'off'` 下 incremental-differ 感知不到测试变化，此前「源码没动、只改/只删测试」会命中旧基线、把按旧测试算出的 killed/survived 喂给本轮新增的 `thresholds.break=65` 分数门禁（该阈值已于本轮停用为 `null`，见下条）（Qodo High / Correctness）；测试段必须排在源段之前，因为 actions/cache 的 `restore-keys` 只有前缀匹配语义。② **剥离范围收窄**：只剥 `reports/mutation/mutation.json`，`reports/inc-*.json` **不再剥离**——它是下一次运行 incremental-differ 的复用输入，`statusReason` 会被原样透传进新产出的 JSON/HTML，置空等于让被复用变异体「为什么存活/报错」永久丢失（与既有「不动 mutation.html」同一条理由）；artifact `path` 同步由 `reports/` 收窄为 `reports/mutation/` 并补 `if-no-files-found: error`（inc 在 artifact 侧零消费方：`scripts/mutation-report.js` 的 `findReportJson`/`analyzeSegment` 与 `.github/analyze-artifacts.js` 都只认 `mutation.json`/`mutation-report.json`），代价如实写明：缓存里的 inc 回到未剥离体积（Qodo Medium / Observability）。③ **超时分层**：`mutation` job 的 `timeout-minutes` 由 350 改为 **360**（仅平台 6h 硬上限的显式化），330 加到「变异测试」**step** 上——job 级超时会连坐杀掉同 job 的剥离/上传/清理，该段 artifact 永久丢失并让汇总 job 因缺段硬失败，旧注释「留 10 分钟给上传」的说法本身就是错的（Qodo Medium / Reliability）。④ **`pre-push` 改为按被推提交内容验证**：`local_sha == HEAD` 且已跟踪文件干净时走快路径（工作树内容 ≡ 被推提交内容），否则在 detached 临时 worktree 里检出该 sha 后跑同一条门禁、跑完无论成败强制清理，隔离环境建不起来即 fail-closed——旧实现把 `local_sha` 只用于打印、无条件测当前工作树，「推 A 测 B」「未提交的修复掩盖提交里的失败」两类假绿灯都出自这里（Qodo Medium / Correctness）。⑤ **CodeQL `js/file-system-race` ×3 high 清零**：`test_mutation_report.js` 三处「按路径 `statSync` 检查 → 按路径 `readFileSync` 使用」改为一次 `openSync` + 只对 fd `fstatSync`/`readSync`（与 `scripts/mutation-json.js` 的 STG-01 修法同口径）。回归与证据：`test_ci_skip_suites.js` 新增三条靶向断言（只读**非注释 YAML 行**、剥离断言只取 `run: |` 的 shell 正文，注释骗不过去）；`test_mutation_report_cli.js` 场景 17 同步改为只看 shell 正文——它原先的 `text.includes('reports/inc-*.json')` 在新剥离范围下**只由注释满足**，属「断言被注释糊过去」的假锁定，必须一并修；`test_install_hooks.js` 新增端到端夹具用例。文档同步两处既有失真：`stryker.config.js` 的「体积收敛必须改 workflow、本次未落地」（本 PR 已落地）与 `AGENTS.md` 的「增量模式拦不住弱化测试」。
- 变异测试**假 Killed 根因定位与门禁重定标**（v3.276 续，沿用本段不 bump 版本）：① **真根因（已实证）**——`test_filter.js` 的缓存目录是**共享**的 `<root>/xianbaoku_cache`，多个用例（如「`init`/`save` 在目录不存在时自动创建」）会 `rmSync` 整目录，而 Stryker 一次 run **只建一个沙箱**、`mutation.yml` 每段 `--concurrency 2` ⇒ 两个并发 worker 在同一份副本里互删，触发 `ENOTEMPTY … sandbox-*/xianbaoku_cache`，把同族「持久化/墓碑」用例整族拖红 ⇒ **与该变异体无关的 Killed（假 Killed）**；`incremental-differ` 在 `coverageAnalysis:'off'` 下**无条件复用**（`if (!testCoverage.hasCoverage) return true`）⇒ 假判决被**冻结 11 天**。实证：43.4MB 原始 inc 里 `sandbox-yi6WZh` 出现 5021 次、`ENOTEMPTY` 86 次；78 个「旧 Killed→干净全量 Survived」的翻转里 57 个带该沙箱名。**修复**：缓存目录改为**按进程唯一**（`xianbaoku_cache_p<XBK_PARALLEL_ID|pid>`，复用生产已有分片口径），并令 `Config.cache.dir` 与 `XBK_PARALLEL_ID` 同源；独立复现：并发 2 份时 base 各 3 失败 → fixed **0 失败、无残留**。② **`thresholds.break` 由 65 改为 `null`**（分数门禁停用）：同配置同段三轮 `storage` 实测 **79.08% / 82.92% / 71.02%**（极差 **11.90pp**，`coveredBy` 285/285 三轮一致 ⇒ 抖动来自测试自身），任何阈值都会随噪声误判；且历史「最低段 69.38%」依据的是**含陈旧复用的虚高值**（真实全量 v3-entry 47.81%、storage 59.30%），该基线**已被证伪**；**待去 flake + TAP 迁移后按 TAP 口径的诚实基线重新标定**。③ **新增 fail-closed 守卫**：`mutation.yml` 在「变异测试」step 之后加一步读段报告，`runtimeErrors > 0` **或** `totalValid === 0`（即 `mutationScore = NaN`）**或**报告缺失 ⇒ `exit 1`；必要性：`calculateMetrics` 里 `mutationScore = totalValid > 0 ? … : NaN`，而 `determineExitCode` 是 `if (mutationScore < breaking)` ⇒ `NaN < 65 === false` ⇒ 该段**假绿**。④ **并发 `--concurrency` 2→8**：同 commit 的 CI 实测 2→4 = 1.88×、4→8 = 1.84×（2→8 共 **3.46×**）；4 vCPU 远未饱和（C=8 时整机仅 ~37% 忙、单链只吃 ~15% CPU）。⑤ **「按测试指纹强制全量重跑」结论：不采用，且已在本轮回退**——它拦不住真根因（污染发生在变异体运行期、基线全程是绿的；测试没变 ⇒ 指纹不变 ⇒ 主 key 仍命中，复用照旧），且在这套 runner 上**跑不完**：真正全量 `app` 2651 个变异体 ≈ 7–11 小时、`utils` 2402、`message-store` 1692，平台 6h/job 硬上限 + 本仓 step 330min ⇒ 9 段已被杀，而 `actions/cache` 是 `post-if: success()`、**失败段不保存进度** ⇒ 那几段每轮从零开始、永久红。该改动（PR #156 把测试侧指纹写进增量缓存 key 与 `restore-keys` 前缀，见上条）**已由本轮的 `mutation.yml` 变更回退**（key/`restore-keys` 恢复为不含测试指纹的形态，且兜底前缀带**配置指纹**：key `stryker-<段>-cfg-<两份 stryker 配置的 hashFiles>-src-<源指纹>`、兜底 `stryker-<段>-cfg-<同一配置指纹>-`）——**当时形态**：既无 `matrix.config` 档位、亦无 `-deps-` 段（现行形态见 `mutation.yml` 缓存 step 的 `key:`/`restore-keys:`），`test_ci_skip_suites.js` 里那条「key 必须含测试指纹」的靶向断言已同步**反转为**「必须逐字等于回退后形态」，并配了回退反例（改回含指纹形态即真红）。TAP runner（`@stryker-mutator/tap-runner@10.0.0` + 6 行 TAP shim，可拿 perTest：`Ran 1.13–1.38 tests per mutant` vs command 恒 1.00，实测 **34–37×**——http 73min→2min、storage 154min→4min、rules ≥12.3×）**已由本段 PR-1（= PR #162，TAP 迁移落地提交 `b05e323`）落地**（CI 走 TAP 档；保守路径：15 段 TAP + `v3-entry`/`storage`/`qinglong-push`/`check-deps` 四段保留 command、增量/复用链路与 `break = null` 原样）：分数会双向漂移（http −6.77pp、storage +11.72pp），需先定分数口径（`mutationScoreBasedOnCoveredCode` 或先做 TAP 档全量基线）。
- 变异**复用状态可见**（v3.276 续，沿用本段不 bump 版本；PR #158 评审 · Qodo Medium / Correctness）：增量缓存 key **有意不含测试侧指纹**（见上条），而 `coverageAnalysis:'off'` 下 `incremental-differ` 拿不到覆盖信息时**无条件复用**旧结果（`if (!testCoverage.hasCoverage) return true`）⇒ 被复用的段描述的是**旧测试状态**。当前 `thresholds.break = null`（无分数门禁）⇒ 这不构成门禁风险；但 Qodo 指出的另一层是**真的**：日报（`scripts/mutation-report.js` 发的 Issue）与变异 artifact 会把复用来的旧结果**当作本 commit 的结果**呈现，读者无法分辨。本 PR 的处理是让复用**可见**（不恢复测试指纹、不强制全量、**不改分数口径**）：① `mutation.yml` 的「变异测试」step 追加 `--fileLogLevel info`（stryker 默认 `fileLogLevel=off`，不写日志文件），新增「记录本段增量复用状态」step 调 `node scripts/mutation-report.js --reuse --segment "${{ matrix.name }}" --log stryker.log --out reports/mutation/reuse.json` 落盘元信息（段名 / 全量·复用 / 复用数·总数 / 比例 / 日志原文证据），随 artifact 到汇总 job；② 日报新增「复用」列（`本轮全量` / `复用 N/M` / `未记录`）与独立的「♻️ 复用状态」小节：复用比例 ≥50% 的段显式点名并提示「该段未全部重算，分数/存活清单可能来自旧测试状态」，**读不到元信息的段显示「未记录」并另给「不要假定它们是全量重算」的提示**（绝不静默当成全量）；③ 判定源**已实测**（本机最小 stryker 工程 + 同版本 core，`--incremental --fileLogLevel info` 连跑三次）：全量分支 `INFO ProjectReader No incremental result file found at inc.json, a full mutation testing run will be performed.`、复用分支 `INFO IncrementalDiffer Incremental report:` + `\tResult:\t\t10 of 13 mutant result(s) are reused.`；**报告本身没有复用维度**（`mutation-testing-report-schema` 的 `MutantResult` 无任何 reuse 字段，已核对 schema）⇒ 日志是唯一判定源，必须在段 job 落盘才能到达汇总 job。两个实测坑已防：① 日志以 `flags:'a'` 追加 ⇒ 解析取**最后一次**事件；② **消息里的 ANSI 着色不进文件日志**（**已实测核实；交接时前任的注释把这一条写反了，本轮更正**）——`chalk.yellowBright` 在消息构造时着色，CI 下 stdout 上确实是 `ESC[93m…ESC[39m`（本机 `FORCE_COLOR=1` 跑真 stryker 复现），但**写文件的 `LoggingEvent.format()` 显式剥掉 ANSI**（`logging-event.js`），同一实验里 `stryker.log` 全文件 0 个 ESC 字节 ⇒ `stripAnsi` 是纵深防御、不是当前格式的必要条件（注释/文档/用例已同步更正，不再宣称「CI 下文件日志带 ANSI」）。**④ 残余边界（本轮补，前任漏掉；已实测复现）**：`Result: N of M` 的 `M` 只统计**当前 `--mutate` 范围**内的变异体，而 incremental-differ 的 sticky 分支会把旧增量报告中**已不在当前范围**的变异体 `{...oldResult}` **原样并入**报告（status 沿用旧运行），且**既不计入 N 也不计入 M** ⇒ `N/M` 只是「旧结果占比」的**下界**。实测：同一份 src.js 不改内容，先 `--mutate "src.js:1-4"` 再 `--mutate "src.js:1-3"`（该最小复现跑在配置指纹收窄**之前**；**当时**（PR-1 形态）的缓存 key 是 `stryker-<段>-cfg-<两份 stryker 配置的 hashFiles>-src-<hashFiles(…, matrix.src)>`、不含 mutate 范围，兜底前缀 `stryker-<段>-cfg-<同一配置指纹>-`；#168 起已变为带档位名 + `-deps-` 段、兜底同带 deps 的三段形态，见下条 ⇒ 主 key 未命中时也恢复**同档配置**的最近一条同前缀缓存）⇒ 日志 `6 of 6 reused` 而报告有 **8** 个变异体（多出的 2 个是第 4 行的旧变异体）；对照组删 inc 全量跑恰好 6 个；该场景在本仓可达（大文件增长时会**重拆 mutate 行段**）。处理＝只呈现不改口径：日报「♻️ 复用状态」小节把「报告变异体数 vs 日志口径 M」不一致的段单独点名（`reuseUnaccountedCount`，纯显示层交叉校验，**不加 throw、不新增第四态**）并明说比例只是下界，且 `0 of M` 这类「标为全量但计数对不上」的段**不再**被并进「全部全量重算 ⇒ 对应本 commit 的测试状态」这句结论。**硬约束**：`--reuse` 只在 `reports/mutation/mutation.json` 已存在时才写 `reuse.json`——否则崩溃段因多出文件让上传步的 `if-no-files-found: error` 失效，「stryker 没产出报告 ⇒ 段 job 响亮变红」被降级成「汇总 job 缺段才暴露」（PR #156 有意前移的故障信号）；该闸门与整条链路（含两条靶向反例）由 `test_mutation_report_cli.js` 场景 18/19 锁定，判定与渲染（含 sticky 口径不符的靶向反例）由 `test_mutation_report.js` 的复用状态用例锁定。分数口径（`(killed+timeout)/total`）、`EXPECTED_SEGMENTS` 对账、日报「不加分数 throw」三条**均未改动**。真正的诚实全量重跑留到下一个 PR（TAP runner 实测 34–37×）——**已由本段 PR-1 落地**：全 19 段 TAP 基线（spike/tap-baseline，run head `cb247c9`）墙钟 ≈71min（瓶颈=utils 70.25min）（⚠️ 该基线 run 整体结论为 **failure**：唯一失败 job 是 `storage`——按设计触发 fail-closed 守卫判红、**非 timeout**，勿读成「19 段 TAP 全绿」）；PR-1 当时的 16 TAP + 3 command 矩阵实测墙钟 86.0min（head `ca63e71` 的**分支 push** run，2026-09-21T19:47:10Z→21:13:12Z；瓶颈=command 档 qinglong-push 85.3min）；改为 15 TAP + 4 command 后 v3-entry 走 command 档、A5 预估 ≈34min < 瓶颈，**但「⇒ 墙钟不变」的预估已被实测证伪**：该 15 TAP + 4 command 矩阵首跑（head `01c0ef89` 的 main push run，2026-09-23T06:30:47Z→08:01:34Z）墙钟 **90.8min**、瓶颈 command 档 `qinglong-push` **90.1min**，v3-entry 实测 **35.1min**（≈预估 34min）；超出部分来自瓶颈 job 自身 85.3→90.1min（+5.6%，成因未归因：该 job 的源文件在两个 head 之间未变、仅 `package-lock.json` 变化）。每段改为按 TAP 口径重算（4 段 command 档除外）。
- 变异测试 **CI 切 TAP runner**（v3.276 续，沿用本段不 bump 版本；保守路径 PR-1）：`mutation.yml` 的变异矩阵改用 `@stryker-mutator/tap-runner@10.0.0` + `scripts/tap-shim.js` + `coverageAnalysis: 'perTest'`（新配置 `stryker.tap.config.js` **`require` 了 `stryker.config.js`** 以继承阈值/`mutate`/`ignorePatterns`——方向是 tap 配置 → command 配置，依据 `stryker.tap.config.js:112` 的 `const base = require('./stryker.config.js')`；并**显式**把报告路径写回 Stryker 默认 `reports/mutation/mutation.json|html`——fail-closed 守卫、`rm -rf reports/mutation`、`reuse.json` 落盘闸门、artifact 与 `analyze-artifacts.yml` 全按它对齐，已由新增契约用例锁定）。动机：command 档真全量在 4 vCPU runner 上**跑不完**（app/utils/message-store ≈7–11h，平台 6h 上限 + step 330min 已杀 9 段，而失败段不存缓存 ⇒ 永久红），只能依赖增量复用，而 `coverageAnalysis:'off'` 下 `incremental-differ` 拿不到覆盖信息时**无条件复用** ⇒ 旧测试状态被当成本轮结果；全 19 段 TAP 基线（spike/tap-baseline，run head `cb247c9`）真全量实测 **run 墙钟 ≈71min**（瓶颈=utils 70.25min）（⚠️ 该基线 run 整体结论为 **failure**：唯一失败 job 是 `storage`——按设计触发 fail-closed 守卫判红、**非 timeout**，勿读成「19 段 TAP 全绿」）；PR-1 当时的 16 TAP + 3 command 矩阵实测墙钟 86.0min（head `ca63e71` 的**分支 push** run，2026-09-21T19:47:10Z→21:13:12Z；瓶颈=command 档 qinglong-push 85.3min）；改 15 TAP + 4 command 后 v3-entry 走 command 档、A5 预估 ≈34min < 瓶颈，**但「⇒ 墙钟不变」的预估已被实测证伪**：该 15 TAP + 4 command 矩阵首跑（head `01c0ef89` 的 main push run，2026-09-23T06:30:47Z→08:01:34Z）墙钟 **90.8min**、瓶颈 command 档 `qinglong-push` **90.1min**，v3-entry 实测 **35.1min**（≈预估 34min）；超出部分来自瓶颈 job 自身 85.3→90.1min（+5.6%，成因未归因：该 job 的源文件在两个 head 之间未变、仅 `package-lock.json` 变化），同 sha 双档对拍 **9.4×**（http 段 22min09s→2min20s，逐变异体仅 1/133 真翻转）。矩阵因此新增**逐段 `config:` 字段**：15 段走 TAP 档，**`v3-entry`/`storage`/`qinglong-push`/`check-deps` 四段保留 command 档**——该段 `xbk_storage.js` 的 `fd = -1` 哨兵被 `UnaryOperator` 变异成 `+1` 后 `finally` 的 `closeSync(fd)` 变成 `closeSync(1)`（关 stdout），触发 libuv 断言 `uv__close: fd > STDERR_FILENO` 直接 SIGABRT（JS 层不可捕获），或让 TAP 点落进 stderr，而 tap-runner 的规则是「进程非零退出且解析不到失败测试 ⇒ RuntimeError」⇒ 该段在 TAP 档必然触发 fail-closed 守卫判红（本地已用真实 stryker+tap-runner 端到端复现）。**不用 shim 包装 `fs.closeSync` 掩盖**：那会改写被测系统的可观测行为（`test_storage.js` 的 fd 序列断言正是靠真实 close 才失败），属于篡改测量。**`v3-entry`/`qinglong-push`/`check-deps` 留在 command 档是同一条独立根因**：变异目标只在**子进程**里被执行（`test_utils_pure.js:449` 的 `execFileSync(node,['-e',fallbackProbe])`、`test_qinglong_utils.js` spawn `qinglong/xbk_push.js --status`、`test_check_deps.js` spawn `scripts/check-deps.js`），perTest 覆盖图看不见 ⇒ 判 NoCoverage 且不跑测试；A5 逐变异体 replay 量化丢 **19**（v3-entry：15 NoCoverage + 4 Survived）/ **15**（qinglong-push）/ **3**（check-deps）个击杀，`message-store` 另有 **3** 个未回退（走 command 需 ≈3.5h）、`app` **未量化且结构性不可回退**（≈5.5h 超出 step 330min 上限；判据与证据见 `AGENTS.md` 的「TAP 档已知限制」③–⑤ 与 `.local/audit-A5-migration.md`）。**刻意不动**：增量/复用链路（cache key **当时（PR-1 形态、#168 前）**为 `stryker-<段>-cfg-<matrix.config 档位>-<hashFiles(stryker.config.js, stryker.tap.config.js, scripts/tap-shim.js)>-src-<hashFiles(package-lock.json, run_mutation.js, matrix.src)>`（`package-lock.json` 尚在 src 段、无 `-deps-` 段；见 `git show 01c0ef8^:.github/workflows/mutation.yml` 缓存 step）、兜底前缀同带**档位名 + 同一配置指纹**（同样不带 deps 段）；#168 起为带 `-deps-` 段的三段形态（`-cfg-…-deps-…-src-…`），兜底同带 deps、不含 src——权威形态见 `mutation.yml` 缓存 step 的 `key:`/`restore-keys:`（本 PR 落地时 `:405-407`）；`inc-*.json`、`reuse.json`、日报「♻️ 复用状态」小节原样）与 `thresholds.break = null` 全部原样；本地 `stryker.config.js`（command 档）不动——CI 与本地两档的**测试集**由 `stryker.tap.config.js` 的加载期断言强制对齐（不一致即配置加载失败）。**已知限制（据实登记，证据见 `.local/storage-tap-RE-findings.md`）**：① `storage` 段在 command 档拿到的 1 个 `Killed` 是**假击杀**——屏蔽 SIGABRT 后跑完整 `test_filter.js`，该变异与未变异对照组**逐条相同**（895 ✅），即没有任何断言检出它，其 `Killed` 纯来自 libuv abort ⇒ 该段分数含约 +0.35pp 虚高，退路是「把同一个崩溃记成 Killed」而非「没有问题」；② TAP 点若因 stdout 被关而落进 stderr 且该文件**退出码为 0**，tap-parser 对空 stdout 判 `{ok:true, skipReason:"no tests found"}` ⇒ 该变异体**静默变 Survived**（基线实例 `#111`，静态上界 6/15302；其余无法从现有 artifact 量化），修法（shim 中继子进程 / TAP 写不进 stdout 即强制非零退出）留待后续 PR。日报新增 **`covered 口径 = (被杀+超时)/(被杀+超时+存活)`** 列，与既有 `分数 = (被杀+超时)/全部`（含 NoCoverage，语义一字未改）并列；合计行补 NoCoverage 汇总；整段 NoCoverage 时该列显示 `—`，不产出 `NaN`/`Infinity`/`0%`。
- 变异**复用溯源 + 依赖进兜底前缀**（v3.276 续，沿用本段不 bump 版本）：① **缺陷与实测证据**——`package-lock.json` 原先只在主 key 里，dependabot 升 fast-check 4.10.0→4.10.1 后主 key 未命中，兜底却把旧增量复原，而 stryker 的 `incremental-differ` 只按**文件内容** diff、不认识依赖版本 ⇒ **19/19 段 100% 复用**、依赖升级从未重算（实测 issue #167 对应的 main `schedule` run（head `4c4a433`，2026-09-22T19:45:19Z→19:47:28Z），见 `mutation.yml` 同处注释）。② **修法一（key 三段形态）**：key 改为 `-cfg-<matrix.config 档位>-<配置指纹>` + `-deps-<hashFiles('package-lock.json')>` + `-src-<hashFiles('run_mutation.js', matrix.src)>`，兜底前缀同带配置指纹与 deps 段、**不含 src**；新不变式＝**兜底前缀 = 缓存身份里除源指纹以外的全部段** ⇒ 兜底只允许跨**源文件**变化（`incremental-differ` 按内容 diff），配置 / `scripts/tap-shim.js` / 依赖任何变化都必须同时换掉主 key 与兜底身份（`run_mutation.js` 是**本地**运行器、留在 src 段只进主 key；测试侧文件 `test_suites.js` / `run_unit_tests.js` / `scripts/mutation-child.js` / `test_*.js` **有意不入身份**，理由见 AGENTS.md 同条）。③ **修法二（复用溯源三态）**：「恢复增量缓存」step 新增 `id: inc-cache`，「记录本段增量复用状态」step 追加 `--cache-hit ${{ steps.inc-cache.outputs.cache-hit || 'none' }}`（`actions/cache` 的 `cache-hit` 三态：`true` 主 key 命中 / `false` 兜底复原 / 空归一为 `none`＝主 key 与兜底都没恢复，依据上游 `actions/cache` issue #1466），三态落进 `reuse.json` 的 `cacheHit` 字段并在日报**逐段标注溯源**，给「**兜底复原 + 含复用**」专用结论，并把 `none` 却记录复用的段点名为 `none+partial` 矛盾；**这是纯显示层交叉校验，不新增第四态、不加 throw、不改分数口径**。④ **一次性代价**：key 形态变更 ⇒ 合并后**首次运行 19 段全部未命中** ⇒ 一次真全量；**≈86.0min 是 PR-1 的 key 变更 head `ca63e71` 的分支 push run，2026-09-21T19:47:10Z→21:13:12Z（16 TAP + 3 command）的墙钟，不是合并后首跑**；#168 合并后首跑 head `01c0ef89` 的 main push run，2026-09-23T06:30:47Z→08:01:34Z（15 TAP + 4 command）墙钟 **90.8min**、瓶颈 command 档 `qinglong-push` **90.1min**；此后每次 `package-lock.json` 变化各一次全量，源码变化仍走兜底复用，`package.json` 单改不失效。⑤ **不动**：日报分数口径（`(killed+timeout)/total` 与 `covered` 列）、`EXPECTED_SEGMENTS` 对账、`thresholds.break = null`、fail-closed 守卫、`--reuse` 落盘闸门（无 `mutation.json` 不写 `reuse.json`）全部原样；`test_ci_skip_suites.js` 的逐字断言同步并**新增三个靶向反例**（key 去 deps / 兜底去 deps / 兜底塞回 src），未放宽既有断言。
- 文档与门禁收口（#168 审查发现；**沿用 v3.276 不 bump 版本**）：① 订正 `AGENTS.md` / `CHANGELOG.md` / `README.md` 里 #168 遗留的失真——缓存 key / 兜底前缀被写成 #167 修复前的旧形态（缺档位名、缺 `-deps-`）且冠以「当前」，与 `mutation.yml` 缓存 step 的 `key:`/`restore-keys:`（#168 提交态 `:387-389`；本 PR 落地后 `:405-407`）及本段末条直接互斥；`test_ci_skip_suites.js:1419` 行号漂移（#168 把该文件推后 94 行 ⇒ `--only=空数据` 的 `spawnSync` 在 **#168 提交态为 `:1513`**、`const FILTER` 在 `:1501`；本 PR 又给该文件净增 22 行 ⇒ **本 PR 落地的树上分别是 `:1535` / `:1523`**，AGENTS.md 按后者写）；`test_app_p.js` 的 `fork(SRC, …)` 实为 `:67`（`:60` 已被 #166 的注释推开）；`86.0min` 墙钟实为 PR-1 的 head `ca63e71` 的分支 push run，2026-09-21T19:47:10Z→21:13:12Z（非 #168 合并后首跑 head `01c0ef89` 的 main push run，2026-09-23T06:30:47Z→08:01:34Z，后者 15 TAP + 4 command、墙钟 `90.8min`、瓶颈 `qinglong-push` `90.1min`）；本机盲区改为如实登记（`npm test` 本机跑不起来；`test_ci_skip_suites.js` 在本 PR 落地的树上必红于 `:633`（#168 提交态 `:493`），其后的三处断言经注入标记实测零执行——但缓存身份门禁已前移、本机真跑并打印 `✅`；`test_mutation_report_cli.js` 在本 PR 落地的树上症状首现 `:84`（#168 提交态 `:86`），且场景 1 经 R4-F7 收紧后**不再假通过**，仅场景 2 的 `code === 1` 仍是假通过形态）；`run_mutation.js` 的运行器归属（本地运行器，CI 的 command runner 走 `scripts/mutation-child.js` → `run_unit_tests.js`）；以及「改为 15 TAP + 4 command ⇒ 墙钟不变」这一**预估**已被 #168 合并后首跑实测证伪（head `01c0ef89` 的 main push run，2026-09-23T06:30:47Z→08:01:34Z：墙钟 **90.8min**、瓶颈 `qinglong-push` **90.1min**，v3-entry 实测 **35.1min** ≈ 预估 34min，超出的 +5.6% 来自瓶颈 job 自身、成因未归因）——AGENTS.md ×4 / CHANGELOG.md ×2 / README.md ×1 同步改为「保留原预估 + 标注已被实测证伪」，并给 86.0min 补上 PR-1 的出处（head `ca63e71` 的分支 push run，2026-09-21T19:47:10Z→21:13:12Z）。② 补门禁空洞：`test_mutation_report.js` 增三条——full / fallback 小节的空集合负例（夹具改为 full 段与 fallback 段混合，逐一断言「full 段不得出现『全量·兜底』」「主 key 未命中却仍在复用」两小节各自的产出）、`cacheHit` 只允许影响复用列/溯源文案、不得渗进其它任何一行的两向判别力、复用比例 <50% 不得打「复用比例高」而恰好 50% 必须打；`test_mutation_report_cli.js` 增两条——**逐字**锁 env 行 `CACHE_HIT: ${{ steps.<id>.outputs.cache-hit || 'none' }}`（含 `|| 'none'` 归一）与活动 run 行 `--cache-hit "$CACHE_HIT"`（附反例 D/E/F）、场景 1 的用法文案收紧（R4-F7：不得靠 stderr 里碰巧出现的脚本路径判绿）。③ `mutation.yml`「记录本段增量复用状态」step 把`steps.inc-cache.outputs.cache-hit` 改经 `env.CACHE_HIT` 注入（本仓 `test.yml` 的 zizmor template-injection 约定：上游 step output 不内联进 `run`），`.codacy.yml` 的审计说明同步引用新调用行。④ **文档里的 run 引用改法（`3645874`）**：`Codacy Static Code Analysis`（main 必需检查、阈值 0 个新问题）把 **11 位 GitHub Actions run id** 判成 `Potential PII: phone number pattern found`（Security/high，4 条落点全在 `AGENTS.md:36-39`）；三个 `.md` 共 23 处 run 引用改为「`head <sha>` + 事件（push / schedule）+ 起止时间（UTC）」（**未**用 `exclude_paths` 削弱门禁），并在 `AGENTS.md` 的「测试与文档门禁」登记该约定。⑤ **cache-hit 接线描述订正 + 反例归属精确化（`3645874` / `5f0c608`）**：`AGENTS.md` 原写 #168 的**内联**形态 `--cache-hit ${{ … }}`，与本次落地的 `env.CACHE_HIT` + `--cache-hit "$CACHE_HIT"` 互斥（CodeRabbit 在 PR #169 指出）⇒ 改为 env 形态并写明理由（本仓 `test.yml` 的 zizmor template-injection 约定）；另按独立验证意见把「反例 E/F 拒绝内联」更正为「**F** 锁内联回潮、**E** 锁 env 行的 `|| 'none'` 归一、**D** 删活动 run 行的 `--cache-hit`」。⑥ **反例 E 的夹具锚定去掉跨行正则（`4037220`）**：`SonarCloud Code Analysis`（main 必需检查）在本 PR 上报 1 个新问题 `javascript:S8786`（`test_mutation_report_cli.js` 的 `/^(\s*CACHE_HIT: …)$/m`——`\s*` 能吃掉换行，在 `m` 下对每个行首尾长扫再回溯；同族的 D/F 用 `( *…)` 只吃空格故未被判），改为逐行 `startsWith` / `endsWith` 匹配 + `assert.strictEqual(envIdxs.length, 1)` 唯一命中断言（旧写法：**2 命中时静默接受**——`replace` 无 `/g` 只改第一处、另一处归一仍在，生产断言据此判过；**0 命中时会在紧随的「夹具必须真的删掉了归一」断言上红**，只是报错不指向锚定——更严地说，整份套件里「0 命中」只可能出现在契约本身已不成立的夹具上（凡满足契约的 env 字面量必匹配旧正则），那时更早已被同文件的 env 正断言拦下 ⇒ 新写法两种情况都在锚定处响亮失败；另新写法按 `trim()` 后比对，对行尾空格比旧正则略宽（语义改善，净效果仍是负向控制生效）⇒ **只强不放宽**；本机已证新旧夹具逐字节相同、下游断言未动）；重分析后该 PR 新问题回到 0。⑦ **`AGENTS.md` 里 TAP 墙钟事实簇去重（`63eef19`）**：同一簇墙钟/基线事实（≈71min 基线 / 86.0min / 90.8min + 90.1min / v3-entry 35.1min / +5.6% 未归因）原先在 L36–39 被近乎逐字复制 **5 份**，而复制已造成真实漏改（本次审查里「19 段 TAP 基线整体为 failure、勿读成全绿」的限定语一度只加到 6 处引用中的 3 处）⇒ 现只在归属 bullet「CI 变异已切 TAP 档」保留一份完整表述（显式标注为权威块），其余位置改为「最短事实 + 指针」：4 行 20315 → 19129 字符、跨行近逐字重复 4846 → 586 字符，事实 MISSING = 0、警告/禁令计数逐项相等、`file:line` 引用列表不变。⑧ **独立审查发现的两处归属/覆盖订正（本次）**：① `mutation.yml` 的「TAP 档 utils 70.4min」被本 PR 误加括号标成「PR-1 全 TAP 基线」——实测 70.4min 属 head `ca63e71` 那轮（16 TAP + 3 command）的 utils job（70.37min），全 TAP 基线的 utils 是 70.25min（段内 step 口径）⇒ 改为按 run 归属；② 「该基线 run 整体为 failure（唯一失败 job `storage` 按设计触发 fail-closed 守卫、非 timeout），勿读成『19 段 TAP 全绿』」这条限定语原先只覆盖三个 `.md` 的 6 处引用，`mutation.yml` 的 3 处（文件头、矩阵注释、去增量论证）仍把它当良好基线读 ⇒ 逐处补齐；另把 `AGENTS.md` 的「见本条**末尾**」指针改为「见本条」（权威块在该 bullet 中段），并给「文档里不要写 11 位 run id」补上范围说明（该约定只针对 Codacy 会分析的文件；workflow 注释里仍以 run id 作溯源——实测该类文件里的 11 位 run id 不触发该规则）。⑨ **统一 utils 时长口径 + 软化 run id 约定的因果措辞（本次）**：独立复验指出文档里 utils 混用两种口径——`ca63e71` 那轮写的是 **job** 耗时（70.37→70.4min），而全 TAP 基线写的是**段内 step** 秒数（仓内报告 4215s→70.25min；GH API 同口径回读 70.28min），于是「C=8 后实测最长段」在一处混口径下才成立（统一 job 口径时最长是基线的 70.57min）⇒ `mutation.yml` 三处与 `AGENTS.md` 权威块改为**逐项标注口径**（段内 step 70.25min / job 70.57min）；另把「`.github/workflows/*.yml` 的注释不在 Codacy 的分析范围」改成实测口径的「**实测不触发该 PII 规则**（`.codacy.yml` 并未排除 `.github/**`，故「未分析」与「规则不适用」两种原因尚未区分）」，不再留下未证实的因果断言。
- 跟进订正（#169 独立复核发现；**沿用 v3.276 不 bump 版本**）：① `CHANGELOG` #169 条目 ⑥ 原写「旧的 0 命中 / 2 命中都**静默通过**」——实跑**证伪其半**：0 命中原写法会在紧随的「夹具必须真的删掉了归一」断言上红（只是报错不指向锚定），只有 2 命中才是静默接受 ⇒ 已按实测分情况陈述；② `AGENTS.md` 把 `cache-hit` 的 **env 注入**写成 #168 的改动——#168 落地的是**内联模板**，env 形态是本 PR `00af54c` 改的 ⇒ 就地注明归属；③ `mutation.yml` 的「`ca63e71` → `01c0ef8` 只有 `package-lock.json` 变化」过宽（实测该区间 **14 个文件**，含 `package.json` 与**在缓存配置指纹里的** `stryker.tap.config.js`）⇒ 改为只声明「该 job 的 mutate 目标源文件未变」，并写明成因**仍未归因**；④ `README.md` 的「瓶颈=utils 70.25min」未标口径（同行 `qinglong-push` 85.3/90.1min 是 job 口径）⇒ 补「段内 step 口径」；⑤ 日报「⚠️ 本次日报含未重算的结果」的机制括注写成「`coverageAnalysis:'off'` 下 …**无条件复用**」，而该警示会把 **15 个 TAP 段**一并列出（perTest、有覆盖信息、不走这条分支；issue #170 实证 19 段全列）⇒ 括注改为「复用来自增量差分对**文件内容未变**的变异体沿用旧结果；**command 档**才会在拿不到覆盖信息时无条件复用——各段档位见表内标注」，并在 `test_mutation_report.js` 加正向断言锁定该措辞（纯显示层措辞，不改分数口径、不加 throw）；⑥ `AGENTS.md` 的「本机验证盲区」补记：本机 `node` 的 `execSync` / 默认 `spawnSync` 会调用**编译期 shell 路径** `/data/data/com.termux/files/usr/bin/sh`（本机不存在 ⇒ `ENOENT`）⇒ node 里调外部命令须用 `execFileSync('cmd', [args])` 或显式传 `shell`。⑦ `AGENTS.md` 三处**序号指针**改为按名字指定：L36 的两处「见下条」按序号会落到「变异**复用状态可见**」条，实际目标是「CI 变异已切 **TAP 档**」条（off-by-one）；L37 的「（见上条）」明确为「见上条「CI 变异**分数门禁**」」（该条论证的正是「不把测试指纹写进身份」＝ L37 所引依据）。两处 off-by-one 在 `01c0ef89` 即存在、#169 的去重逐字未触碰（由去重守恒子检查核对指针时发现），按名字指定是本仓去重时确立的约定（名字指针比序号指针稳）。⑧ `CHANGELOG.md` **#169 条目内**：原 ⑨ 排在 ⑧ 之前（编号非升序）⇒ 交换为 ⑧⑨（并清掉换位时残留的一个空格）。
- 补测试第一批（**按 `cb247c9` 的 19 段 TAP 全量基线逐变异体重算的存活靶子清单**补断言；**纯测试改动、未动任何生产源文件**，故 `mutation.yml` 行段无需重拆；沿用 v3.276 不 bump 版本）：① `test_utils_pure.js` +878 行——`_htmlTagSpans` 标签区间与「属性值**开启**引号」状态机（含 `</div class="a">` 端标签同样进属性态、`<` 后非字母/`<!`/`<?` 整段按文本跳过、纯文本里的未配对 `<` 不再把区间拖到串尾）、`_parseDateTimeNoTz`/`_parseSlashDate`/`_parseIsoZ`/`_parseFallback` 的时区无关解析与越界字段、`getMessageIdentity`/`hasValidId`、`anonKey` 两路 djb2 的精确键值（乘子/下标项/循环上界任何一处变更都会改键）、`truncateUtf16` 的代理对截断边界、`_stripEventAttrs`/`sanitizeHtmlUrls` 出口清洗；② `test_message_store_utils.js` +774 行——`saveBatch` 落盘/回滚/摘要与布尔出口、`_trimCacheByBytes` 二分裁剪必须保留**最大可行**后缀、`_evictOldestKeys`/`_evictTombstonesToSize` 的淘汰推进与上界（含「连最新单条都超限即返回 null、不得自锁」）、`_probeIndexMiss` 的旋转抽查窗/游标推进/小数组整表、`_memoSet` 把 `__proto__` 当普通自有键；③ `test_sendnotify_pure.js` +229 行、`test_sendnotify_utils.js` +192 行——模块加载期默认值与 `ENV_ALIASES` 全别名覆盖（空白 env 不得覆盖）、九通道统计逐字清单、`NO_CHANNEL_CONFIG` 不得静默成功。**验证口径＝逐变异体 replay，不是分数**：本机自建引擎（`.local/harness/`，复用 Stryker 自己的 `@stryker-mutator/instrumenter` 生成变异体，逐个「施加→跑测试→还原」）在 `TZ=UTC` 下判定 —— `xbk_utils.js` 存活靶子 **376/828** 转 Killed、`xbk_message_store.js` **219/557**（⚠️ 该数为**返工前**口径；经独立审查返工后真值为 **198/557**，逐条归因见本段 `cf19209` 的「第一批测试的**独立审查返工**」条）、`xbk_sendNotify_slim.js` **197/445**（这三个数都只由新增用例贡献：靶子在报告里是 Survived，即旧的全量单元测试集一个都没杀掉）。Sendnotify 的两个 id 集合与逐段对账已复核（`engine` 枚举 2093、两段报告合计 2085，缺口 8 即已登记的跨行段边界 `BlockStatement`，与 `.local/findings/F1` 一致）。本机跑不了 `npm test`（缺 `re2`、`process.execPath` 指向 `linker64`）⇒ 本地证据只支持「新测试自身绿 + 逐靶子击杀」，全量绿与 CI 口径仍认 CI。
- 补测试第二批（同口径：靶子取自 `cb247c9` 的 19 段 TAP 全量基线；**纯测试改动、未动生产源文件**；沿用 v3.276 不 bump 版本）：① `test_status_report.js`——`scripts/status.js` 的 117 个存活靶子杀 **104**（既有套件基线 6）：`formatStatus` 30/30、`parseDiagnostics` 24、`isValidReportDate` 19、`parseLastRun` 10、`parseJson`/`describe`/`looksLikeRoundFailure` 全清；剩余 13 个逐条判定为**等价/不可观测**并登记（如 `!m→false` 因后续 `m[1]` 必抛且被 try 吞掉、`month<1`/`month>12` 因 `days[month-1]` 为 undefined 使结论不变）。② `test_failure_policy.js`——本次范围的 128 个靶子杀 **71**（`classifyOne` 48/92、**模块级常量表 5/5**、`summarizeError` 18/31）：四承载字段（数字 `code`/`providerCode`/`HTTP_` 前缀/`statusCode`）的 3xx/4xx/5xx 边界表、39 条永久文本 + 17 条非永久/瞬态负例、限流支、聚合子级仲裁两种形状、`summarizeError` 的字段归一/折叠/截断。③ `test_storage.js`——85 个杀 **63**，其中 **2** 个是变异体在文件被截短时死循环、replay 以超时（`rc=124`）结算的「**非终止性击杀**」，没有任何断言检出，**不计入**断言击杀（如实登记）。④ `test_agents.js` 78 个杀 **6**（DNS 失效码、`invalidateDns` 前缀 `host+'|'`、`order=verbatim/ipv6first` 建模）；其余 72 个所在函数（`dispatchLookupResult`/`queueMicrotask`/`done`/`onAbort`/`callback`/`makeResult`）**未导出**、只能走回调路径，登记为残余。⑤ `test_pusher.js` 55 个杀 **5**（纯函数组 14 个里的 5 个；`send` 组 41 个本轮未做）。**假击杀甄别（不得误报战果）**：`xbk_storage.js` 的 `fd = -1` 哨兵（`UnaryOperator` 改成 `+1` 后 `closeSync(1)` 关 stdout、触发 libuv abort）在本机 replay 记 **SURVIVED**、**未计入**；同族 `writeAtomicIfAbsent` 的哨兵变体经「stdout 改接文件」复核确认是**真击杀**（新增用例检出「未成功打开 fd 时绝不能 unlink 目标路径」——变异体会删他人文件），计入。**验证方法学（本轮实测踩到）**：`replay.mjs` 原地改写 worktree 内的生产源，**被 SIGTERM 打断会把源留在已变异状态**（症状：套件变红、下一批 replay 出现 `ENGINE_MISSING`）⇒ 中断后必须先 `git status -s` 确认源干净再测；也不要拿报告的 `location.start.column` 自行拼 splice 去复核 harness（它与 harness 用的 `original.loc` 列号差 1，会造出 `ReferenceError` 式**假击杀**）。
- 补测试第三批 · `xbk_rules.js`（同口径；纯测试改动、未动生产源文件）：本次范围 121 个存活靶子（`hasNestedQuantifier` 109 + `_compileCatRe` 5 + `_validateCatRe` 7）杀 **57**——`hasNestedQuantifier` **45/109**（无限量词识别 `+`/`*`/`{n,}` vs 有界 `{n}`/`{n,m}`、嵌套分组的 inf/alt **向上传播**、组内含 `|` 的歧义交替、有界重复阈值三连、字符类整体当普通 token（含 `[^]` 非空字符类特例）、转义、`String()` 兜底与 Symbol 守卫；**测试只断言判定结果，绝不真的编译/执行 `(a+)+` 这类危险模式**，避免卡死）、`_compileCatRe` **5/5**、`_validateCatRe` **7/7**。剩余 64 个逐条登记为**等价/不可观测**：L42 守卫被 `String()` + try/catch 兜底遮蔽、`{n,}` 的「无限」判定被更早分支冗余覆盖、`[^]` 特例与 else 扫描落点相同、L57/59/65 的未读栈底状态与退化 `j=i-1`、L118/119/122 的有界重复子表达式。**落地时补掉代理如实登记的 1 个真实缺口**：`_validateCatRe` 的 `compileUserRegex(String(cat), 'i')` 固定标志从未被捕获（改掉 `'i'` 无人发现）⇒ 新增「捕获实参」用例，实测把 `StringLiteral@L145:61` 从 Survived 翻成 Killed（+1，`_validateCatRe` 6/7→7/7）。
- 补测试第四批 · `scripts/check-deps.js` 与 `qinglong/xbk_push.js`（同口径；纯测试改动、未动生产源文件；沿用 v3.276 不 bump 版本）：① `test_check_deps.js`——66 个存活靶子杀 **37**（`dependencyFailureReason` 6/6、`declaredDevDependencies` 3/3、`satisfiesComparator` 10/18、`checkDependencies` 10/16、`nodeVersionProblems` 3/5、`satisfiesNodeRange` 2/6、`parseVersion` 2/3、`declaredRuntimeDependencies` 1/2）；手法是**白盒沙箱**：`internalsOf()` 用 `new Module` + `_compile` 在临时目录里编译**磁盘上当前（含变异体）的** `scripts/check-deps.js` 并追加一行内部导出，从而直接断言未导出的 `parseVersion`/`compareVersion`/`satisfiesComparator` 等纯函数（被测的仍是**真实文件内容**，不是复制品）。剩余 29 个主要为等价/不可观测（`'utf8'→''` 因 `JSON.parse(Buffer)` 等价、`i<3` 边界、缺段 `>`/`<=` 与 0.x `^` 的组合），登记为残余。② `test_qinglong_utils.js`——114 个靶子（`.local` 的按函数拆分丢掉 2 个未被任何函数区间覆盖的 id，全量 116）杀 **79**：`describeFailure` 30/34、`isRecoverable` 24/30、`runDryRunOnce` 17/20、`load`/`fixedPath`/`retryBackoffMs`/`statusCacheDir` 全清；剩余 35 个逐条登记——9 个（`handleFailure`/`runOnce`）**要驱动常驻循环、探针实测挂死**，为避免 replay 期挂死刻意不覆盖；8 个（`sanitizeModuleName`/`shouldAutoInstallDependencies`）在唯一调用点只收到字面量 `'got'`/`'re2'`，非法名 fail-closed 分支不可达 ⇒ 等价；2 个是 `win32` 分支（Linux 平台等价）；2 个是 `'--check'`/`'--dry-run'` 参数名（只有 spawn `--check`/`--dry-run` 才能观测，而后者**会真实推送**）⇒ 风险不可接受、明确弃；另有 1 个 `stdio` 选项缺口与 1 个「安装前可恢复/安装后不可恢复」场景缺口，如实登记。**环境性红（非回归，已用基线复跑证伪）**：`test_check_deps.js` 在**本机主仓**有 1 条既有失败 `F4 真实 package.json engines 在本机运行时上通过`——因为主仓 `node_modules/re2` 缺失（该套件在 `.local/*` worktree 里会向上解析到 `.local/node_modules/re2` 替身而变绿）；把 `HEAD` 版测试文件原样放回主仓根复跑同样是 **20 通过 / 1 失败**，证明与本次新增用例无关，CI 装了 re2 故不出现。
- 第一批测试的**独立审查返工**（审查由独立子代理只读执行；返工后**纯测试改动**、生产源 0 改动）：① **阻断 B1 是真缺陷**——`test_sendnotify_utils.js` 的「空白 env 不得覆盖默认值」用例原本只在 `push_config.local.js` **不存在**时才跳过默认值断言，而同一条件下的 ③ 却不设防 ⇒ 部署侧按 `push_config.local.js.example:12/19` 填了 `PUSH_KEY`/`BARK_SOUND` 时 **① 被跳过却仍打印完整 ✅（假绿）**、③ 直接变红（**假红**）。已用夹具实测复现两种症状（改前 + 夹具 ⇒ `rc=1 AssertionError: 纯空白 env 不得覆盖默认值`；改前 + 只填无关键夹具 ⇒ `rc=0` 却打印「默认值逐键…已验证」而 ① 实际被跳过），返工改为**相对断言**（先 reload 取 baseline，再断言空/纯空白 env 不改动 baseline），`✅` 文案按「真跑了默认值断言 / 因本地配置存在而跳过」分支化。② 阻断 B2：删掉两条两侧同表达式的恒真断言。③ 应修 10 条（Y1–Y10）全部落地：`_htmlTagSpans` 的 50 例绝对下标快照压到 12 例只锁语义分支、其余改由出口 `sanitizeHtmlUrls`/`sanitizeDecodedHtml` 断言（**实测击杀 0 损失**）；`_memoSet` 数字样键细节（生产注释已注明是防御性死代码）、`_evictTombstonesToSize` 的 69 魔数逐字节文本、`_probeIndexMiss` 的 `refCursor`/`identityCursor` 私有字段与推进量，分别改为**不变量/行为断言**（后者用「n=33 时第 31 位在首轮窗内、第 32 位不在」反推窗宽恰为 32）；`_removeActiveTags` 的 `</embed>` 残留不再写成契约（改断言安全性质，并登记「与自身 docstring 不一致」为疑似生产缺陷）；16 处日志散文断言改为结构化或含变量的最小片段；删掉 `typeof === 'object'`（`null` 也成立）与不可达的 `_isTombstoneLockOwner(path,'')` 断言。**代价如实登记**：`xbk_message_store.js` 击杀数 **219 → 198**（26 失 5 得，逐条可归因：数字样键 15、私有游标 7、精确文本 1、散文唯一来源 4），`xbk_utils.js` **376 → 376 逐靶子完全一致**；两者都高于返工门槛（≥340 / ≥190），返工前后各做过一次全量独立复跑（得/失清单完全相同）。**返工者自我更正（已作废一个虚高数字）**：其首版把 Y8 写成「比较两次 live `statfsSync` 容量」，负载下会假红，修正前测得的 `utils 399` 因此含 **23 个假击杀** ⇒ 已改为「临时替换 `node:fs.statfsSync` 为桩」的确定性写法并作废 399，**376** 是修好后四路分片全量重跑的结果。
- 补测试第五批 · `xbk_loop.js` / `xbk_http.js` / `xbk_network.js`（同口径；纯测试改动、未动生产源文件）：`test_loop_utils.js` 杀 **47/64**（`runBounded` 未导出 ⇒ 内部落定守卫在可达路径上不可区分，余 17 个登记为残余）、`test_http.js` 杀 **17/36**（余 19 个中 3 个需观测模块内闭包 `invalidateDnsForError`、3 个只改 timing 日志且 mock 的 `timings.phases` 为空、其余为 end/错误回调路径不可区分或等价）、`test_network.js` 在 `parseRetryAfterMs` 上杀 **7/14**（父代理独立全量复跑校正：代理自报 8，实为 7；余 6 个因函数对一切非法形态最终都 `return null`、守卫真假两侧返回值相同 ⇒ 等价）。`xbk_network.js` 的 `fetchData`(37) 等 43 个靶子**未重放**（网络套件单轮 60–70s，超预算）⇒ **不声称击杀**，登记为残余。**方法学坑登记**：`timeout 150 NODE_OPTIONS=... node` 这种写法下 bash 会把 `NODE_OPTIONS=...` 当作 `timeout` 的命令名 ⇒ 每个变异体 `rc=127`、整批 162/162 **假 KILLED**（已改为赋值前置后重跑，本批所有数字出自修正后的重放）。
- 补测试第六批 · `xbk_app.js` 新增单元套件 `test_app_unit.js`（同口径；**纯测试改动 + 注册表同步**，生产源 0 改动；沿用 v3.276 不 bump 版本）：`xbk_app.js` 此前**没有任何单元套件**（`test_app.js` 是 209KB 的集成套件、不进变异测试集）⇒ 它那 364 个存活变异体无人认领。新套件在**进程内**用最小桩构造 `createApp(...)`（与 `xbk_function_v3.js:319-348` 的真实注入同形状）后直接调用目标方法，**全程无子进程** ⇒ 可被变异测试的 perTest 覆盖归因。本批范围＝报告/状态簇：`_isValidReportDate` **57/59**（`value===''` 为真、非字符串为假、`/^(\d{4})-(\d{2})-(\d{2})$/` 形状、不可达日期如 `2023-02-29`、闰年 2000/1900/2024、月天数与 `day`/`month` 边界三连）、`_loadReportState` **52/52**、`_normalizeReportState` **13/14**，另附赠 `_blankReportState` 1/1、`_safeCounter` 2/3、`_reportToday` 4/4 ⇒ 合计 **129/133**（核心 125 靶子杀 **122**）。剩 4 个判定等价：`month<1`/`month>12` 因 `days[-1]`/`days[12]` 为 `undefined` ⇒ `day <= undefined` 恒 false、结论不变；`typeof raw !== 'object'` 的短路只有污染 `Object.prototype` 才能区分（脆弱，不采）；`_safeCounter` 的 `n >= 0 → n > 0` 在 0 处两侧同返回 0。**注册表必须成对同步（否则门禁红，已逐项实测）**：`test_suites.js` 的 SUITES 追加一条、`stryker.tap.config.js` 的 `explicitTestFiles` 追加同名条目——该文件**加载期**会断言两者逐元素含顺序一致，`require('./stryker.tap.config.js')` 实跑通过；`SKIP_SUITES` 与 `test.yml` 显式步骤**无需改**（新套件非 integration/mutationSkip，由 `run_unit_tests.js` 统一执行；`test_ci_skip_suites.js:310` 的不变式是「SKIP_SUITES == **有显式步骤**的单元套件」，新套件无显式步骤故不进该集合），`test_suite_registry.js` 本地实跑 ✅；README 的套件计数同步为 **46 = 33 单元 + 10 集成 + 3 变异跳过**。
- 补测试第七批 · `xbk_formatter.js` 与 `xbk_app.js` 日志/状态写入簇（同口径；纯测试改动、未动生产源文件）：① `test_formatter.js` 杀 **86/165**，其中 **1** 个（id 101 `ConditionalExpression@L54:13`）是变异体死循环、replay 以 **`rc=null`（超时）**结算的「**非终止性击杀**」，没有任何断言检出——与 `storage` 的 2 个 `rc=124` 同口径，**按统一口径应从断言击杀中扣除**（故诚实口径为 **85/165**）；本条目此前把它计入 86，现据独立审查更正（`anchorText` 59/84、`timeSource` 13/26、`_quotedAttrSpans` 7/20、`_readTagAttrValue` 4/12、`_getTagAttr` 3/23；基线 3）。剩余 79 个分类登记：约 40 个**等价**（如 `anchorText` 的 `<>` 分支三件套因输出端实体解码把 `&lt;` 还原、`timeSource` 的 `slice(0,100000)` 幂等），其余为**不可观测**（`img` 危险 `src` 剥离的残留随后必被通用剥离、`{Html内容}` 惰性路径不产生输出、标签名扫描起点变体——40+ 定向输入与 9000+ 模糊输入均无法区分）。命中一个**真·无限循环**变异体（`_readTagAttrValue` 的 `index<end→true`，未闭合引号时死循环），由 `<a href="u>t</a>` 用例经超时判 Killed。② `test_app_unit.js` 扩容杀 **81/82**（`_writeRunLog` **53/53**、`_warnLowDisk` 15/15、`_persistReportState` 5/5、`_writeState` 6/7、`_writeTextAtomic`/`_isRegularOrMissing` 各 1/1）：运行日志分级前缀/`Asia/Shanghai` 时间戳/1 MiB→512 KiB 换行对齐截尾/ERROR 行只截 `errMsg` 段到 512 字符/日志路径非普通文件时拒绝写入且不影响主流程、低磁盘告警阈值与限频、写失败保留内存快照。唯一未杀（id 55，`state === undefined`）**可证等价**：该子条件被 `typeof state !== 'object'` 完全包含，置换后对 undefined/null/42/'s'/数组各入参的告警与返回值逐字节相同。③ 本批两次独立复现均通过：父代理对 formatter 做了**全量独立 replay**（165 个靶子），对 app 的日志簇抽 6 个 KILLED 全部复现。**再次登记 replay 事故的通用教训**：用 `timeout` 打断 `replay.mjs` 会让 `finally` 不执行、生产源留在变异态，**并发跑两个 replay 写同一文件会叠加污染**（formatter 代理实际踩到，已用 `git show HEAD:<f> > <f>` 还原并 `diff -q` 逐字节校验）；结论一律只取**修复后单进程串行**的那一次 replay。
- 补测试第八批 · `xbk_function_v3.js`（v3-entry 段，同口径；纯测试改动、未动生产源文件）：86 个存活靶子杀 **81**（代理版 74，父代理落地时**补掉代理如实登记的 6 个缺口** +1：`compileUserRegex` 失败路径必须返回 `null`、`package.json` 读取抛错必须回退初值 `3.x`、**纯空白版本**必须回退（一次同杀 `==`/去 `.trim()`/空串字面量三个变异体）、`createNetwork` 注入对象必须 10 键齐全、`createPusher` 必须 3 键齐全）。手法：`PROBE_PRELUDE` 里的 `spy()` 现在同时记录**每次调用的实参**，4 个 `execFileSync(process.execPath,['-e',probe])` 子进程探针把「顶层常量 / `XBK_PROFILE=3` 的 boot marks / `Config` 默认值 / `PKG_VERSION` 各轮 mock / 注入实参键集」以 JSON 回传，父进程 `strictEqual` 精确断言；**段口径说明**：CI 的 `v3-entry` 走 command 档，而本基线报告是 TAP 口径（含 NoCoverage）⇒ 击杀结论以本机 replay 的 `KILLED` 为准。余 5 个为语义等价（`require.main === module` 在进程内恒 false、CJS 导出守卫左值恒真）。**门禁发现（本机）**：`xbk_function_v3.js`/`test_utils_pure.js` 的 `require('re2')` 在 `scripts/mutation-child.js` 的沙箱里从仓库根解析 ⇒ 主仓 `node_modules` 缺 `re2` 时，本机主仓有 **4 条**环境性红（`P1-04` 等），而所有这些用例在 `.local/wt-*` 子目录里会向上解析到本机替身而变绿——为让本机验收口径一致，已在主仓 `node_modules/re2` 放入本机替身（**已被 `.gitignore:1` 的 `node_modules` 忽略，不进任何提交**）。此举同时消掉 `test_check_deps.js` 的那 1 条既有环境性红（40/1 → **41/0**）。
- 补测试第九批 · `xbk_filter.js`（filter 段；纯测试改动、未动生产源文件）：新增「补测（PlanA）」区块覆盖 `timeMatchedRule` / `checkFields` / `_passIfMissing` 三个高价值簇（150 个存活靶子中挑选），断言一律对契约与注释意图、真假两侧成对。**本段验证成本极高（据实登记）**：`test_filter.js` 单次运行实测 **2m47.9s**，故 replay 每变异体约 **3m6.6s** ⇒ 50 变异体分片 ≈2.3h、150 个靶子 ≈7h，均超出单次工具上限，本轮**曾以抽样 replay 自称 7/7 KILLED，该结论已被独立审查撤回、不得采信**：`replay.mjs` 原先的判定只有 `rc===0?SURVIVED:KILLED`，而本机未变异的 `test_filter.js` **本身就 rc=1**（2 条 FUSE 符号链接用例恒红：「缓存符号链接」「防御: cache.dir 符号链接不能指向项目外部」，主仓与 worktree 均复现）⇒ **红基线下任何变异体都会记 KILLED**，该判定对 filter 段**没有区分力**，7/7 与 5/5 均属无证据力数字。**修法**：`replay.mjs` 已加固为 fail-closed——跑批前先跑一次**未变异基线**，基线 rc≠0 即 `exit 2` 拒绝产出结果（实测该闸门对 `test_filter.js` 正确拒绝、对绿套件不误伤），并新增 `TIMEOUT`/`SIGNAL` 独立判定（信号与 rc=null/124 不再计入 KILLED）、输出增列 signal。故 **filter 段的击杀在本机一律不可判**（与 `AGENTS.md` 既有的「`test:filter` 只认 CI」一致）；本轮 filter 的正确状态是「测试已写好、主仓套件全绿（822 通过 / 2 条环境性红）、**击杀未验证**」。**方法学（本轮反复踩到）**：`replay.mjs` 就地改写源文件，任何中途被杀的运行都会把变异留在 worktree 里（症状：套件变红、后续 replay 静默失败/0 字节日志），**每次运行前后都必须 `git status -s` 检查生产源并在必要时 `git show HEAD:<f> > <f>` 还原**；手动 `nohup ... &` 在本机会被立即杀死，长时 replay 必须走工具自带的后台机制。
- 补测试第十批 · `xbk_app.js` 告警与日报簇（`test_app_unit.js` 扩容；纯测试改动、未动生产源文件）：149 个存活靶子杀 **143**（命中率 95.97%）——`_sendCrossDayReport` **54/54**、`_updateReport` **35/35**、`_accumulateReport` **15/15**、`_validateTplConfig` **6/6**、`_sendAlert` 33/37、`_enabledFlag` 0/2。覆盖要点：跨天日报**仅当上一日累计非 0 才发**、「待推送」只在被 `push.maxPerRun` 截断时出现、`reportKeys` 五字段循环、日界按 `Asia/Shanghai`（**冻结时钟**断言，不依赖进程时区）、告警开关与 `intervalMs` 限频的 ±0 边界、`run.log` 的 200 字符截断与 `\n` 折叠、告警/日报失败不影响主流程。剩余 6 个逐个按语义证明为**等价变异体**（非覆盖不足）：`en == null` 与同分支的 `''` 字面量（`Boolean(en)` 已短路）、`intervalMs > 0 → >= 0` 与 `interval > 0` 两处（0 / 非正分支取同值，且 `lastAt` 上一行已钳到 ≤ now 使右式恒假）、`alertText.slice(0,100)`（14 字符常量 ⇒ 恒等变换）。**证据链事故登记（据实）**：该波第 2 轮 replay 期间 `wt-app4/xbk_app.js` 被**兄弟代理并发写**污染（残留变异 `filtered: true`），该轮出现 `ENGINE_MISSING=1`、结果作废；已在干净基线上串行重跑，**143/149 出自重跑轮**（replay 前后 md5 均等于 HEAD）。教训：**同一个 worktree 严禁并发跑 replay**。
- 补测试第十一批 · `xbk_pusher.send`（同口径；纯测试改动、未动生产源文件）：41 个存活靶子从 **0 → 33** Killed（`NODE_OPTIONS`+`TZ=UTC` 逐变异体 replay 实测；基线先量得 `SURVIVED:41`）。手法：注入 `getNotify` 桩 + **假定时器**（把 10s 定时器改到 0ms 宏任务里触发，既不真等 10 秒也不靠同步时序蒙混）+ 可控 promise，覆盖——入参归一（`undefined`/`null`/数字 `String()` 化，**含 `text===null` 这一格**）、`AbortController` 有/无两态与 signal 透传、成功路径「只注册 1 个 10s 定时器 + `clearTimeout` 精确句柄 + finally abort」、阈值 100000 边界与**逐字**告警原文、代理对截断（0xD800/0xDBFF/0xDC00/0xDFFF 精确边界 + 4 个非代理对反例）、`inFlightTracker` 的 `pending` 初值/空数组有效态（区别于「调用方不支持该契约」）/非空在飞清单 `deepStrictEqual`/缺失清单告警、非 thenable 拒绝静默成功。**收口了代理如实登记的 3 个可击杀缺口**（+3，全部复现）：`ConditionalExpression@L10:41`（补 `text: null`）、`ConditionalExpression@L83:18` 与 `CallExpression@L83:30`（超时分支的 `controller.abort()`——它在 `finally` 的兜底 abort 掩盖下不可直接观测，改用**计数版 AbortController** 断言「超时路径同一 controller 的 abort 次数恰为 2、成功路径恰为 1」）。余 8 个登记为**等价/不可观测**：6 个在 `safeSlice` 的 `endIdx`（恒为常量 100000，子条件恒真/`||`/`<=`/`>=` 与原条件行为相同）、1 个 `.slice()` 被删（`pending` 只读且 `names.map` 另建数组，拷贝与否不可观测）、1 个 `names = true`（见下条缺陷登记）。**顺带发现一个真实生产缺陷（未修，超出本目标范围）**：`configuredChannelNames()` 返回**非数组**时，`xbk_pusher.js:98` 的 `names.map` 在 **setTimeout 回调里**抛 `TypeError: names.map is not a function` ⇒ 进程级崩溃（不是 promise 拒绝，测试内**无法捕获**）。该输入不可作断言（会被当成契约），故本轮只登记不锁定；修复应属独立 PR（生产侧补形状防御）。- 补测试第十二批 · `xbk_agents.js`（同口径；纯测试改动、未动生产源文件）：`test_agents.js` 追加 52 行覆盖 DNS 选项建模，但**实测总击杀数未变（仍 6/66，新增 0）**——新用例覆盖的路径与既有用例重叠。**据实登记为「无增量的补充」**，不计入战果；`dnsResultOrder`/`productionLookupOptions`/`dnsSelectionSignature`/`makeResult`/`invalidateDns` 等簇仍未杀动，原因与既有登记一致（多数函数未导出，只能走回调路径）。
- 补测试第十三批 · `xbk_formatter.js` 未尝试簇（同口径；纯测试改动、未动生产源文件）：此前只做了 `anchorText`/`timeSource`/`_getTagAttr`/`_quotedAttrSpans`/`_readTagAttrValue`，本轮补上**从未尝试**的 10 个簇，32 个存活靶子从 **1 → 24** Killed（父代理独立复跑复现同数）：`_tagOpFor` **10/10**（p/div 前缀命中与 `pre`/`divx`/`map`/`xdiv` 正反例、未登记名返回 `null` 而非 `undefined`、闭合 img/tr/table→null）、`_replaceTagged` 4/6（无 `>`/无闭合时剩余原样保留且不丢字不重复）、`_finalizeMd` 2/3（非字符串 `String()` 兜底）、`_markQuotedSpan` **3/3**（未闭合引号区间含末位、闭合返回 `closeQ+1`、`inside` 精确集合）、`readTagName` 2/3、`tuisong_replace` 系 2/2（模板 `String()` 抛错按空模板、`category_id→{分类ID}`）、`_mdDestination` 1/1（假值原样返回、含空白用 `<>` 包裹）。余 8 个逐个判定为**等价/不可观测**（删掉短路后仍经 `String('')` 返回同值；`pos<len`/`i<len`/`j<len` 的 `<=` 变体在等号处 `slice`/`_findTagEnd` 结果不变；恰 100000 时 `truncateUtf16` 恒等；`includes('{…}')` 内层字面量置空后 map 值仍只在占位符存在时被消费）。**基线量测方法**：先对当日 HEAD 的 `test_formatter.js` 跑一次 replay 得 `1 KILLED / 31 SURVIVED`，再在同一 worktree 追加用例后复跑 ⇒ 增量可归因，不是只看终值。
- 补测试第十四批 · `xbk_rules.js` 未尝试簇（同口径；纯测试改动、未动生产源文件）：先前只做了 `hasNestedQuantifier`/`_compileCatRe`/`_validateCatRe`，本轮补上**从未尝试**的 10 个簇，175 个存活靶子从 **2 → 96** Killed（父代理复核复现同数）：`compileRules` **37/67**、`validateConfig` **22/52**、`_splitLines` 10/11、`_catMatches` 9/11、`compileStateOf` 8/11、`matchesCompiled` 5/12、`checkTimeCompiled` 2/7、`_firstMatchingRule`/`_anyRule` 全清。覆盖要点（对齐 `SYSTEM_CONTRACT.md` 的「过滤与配置」条）：用户正则只经 `compileUserRegex`、re2 缺失/不支持时**跳过并告警**且**不回退 V8**；`Utils.num` 只做校验与回退默认（范围与取整由调用点负责）；新增字段须同步 `FILTER_FIELDS` 与 `filterHash`（含 `pingbitime`、`zkt_gjc` 与时间过滤启用时的 UTC 日期）；形状不符**显式告警并保守放行**；`###` 多行分类型与 `pingbitime` 天数上限/空白/首尾空格告警的精确文案。余 79 个分类登记：约 66 个**等价/不可观测**（守卫被后续同判据吸收、经 `typeof` 守卫后不可达、`undefined`/`null` 分支落同值、告警文案 `parts[1] || ''` 同值、Regex replacement 与原文逐字相同、`parseTime` 对 `''`/`undefined` 同返 null），3 个需特定替身语义（`parseTime(null)` 返回纪元 0 才可观测），**其余 4 行（`L272:56`/`L428:10`/`L499:34`/`L527:39`）经父代理逐条手工施加复测确认确为等价**（不是 harness 漏测）：例如 `pbfl.ok && safeStr(pbfl.value)` → `true` 后，两套件仍 rc=0，因为这两个前置条件已被前面的 `readField` 守卫保证；`pbStr.trim() !== ''` → `true` 同理被后续 `pbStr !== pbStr.trim()` 吸收。**该复测同时证伪了「replay 未真正落盘变异」的怀疑**：`off()` 算出的字符偏移与 babel 的 `loc.index` 逐位一致（如 id 790 的 18248/18278），变异确实写入了磁盘。
- 补测试第十五批 · `xbk_sendNotify_slim.js` 五个大簇（同口径；纯测试改动、未动生产源文件）：239 个存活靶子杀 **117**（父代理抽 4 个复核全部复现 KILLED）。按**真实源码簇**（由与 replay 同一 instrumenter 逐 id 定位，239 个 id 全部命中）：`ENV_ALIASES` 别名表 **46/51**、`collectConfiguredSecrets` **32/35**、`push_config` 默认值字面量 **21/26**、`nonEmpty` 3/3、`warnDropped`/channels 解析 6/30。覆盖要点：`push_config` 默认值**逐键**、`ENV_ALIASES` **全表**（用最后一个别名驱动以证明整表被读取）、脱敏收集的环路/嵌套/原语/URL 分段边界、`nonEmpty` 两侧、channels 解析告警逐例精确文案。**方法学更正（据实登记，影响本目标的靶子清单口径）**：`.local/harness/splitids.py` 按 `mutation-plan/function-breakdown.tsv` 的 `decl_line` 做**缩进启发式**分函数，对 `xbk_sendNotify_slim.js` **不准**——计划里标的 `isCode`(90)/`nonEmpty`(46) 实际落在「channels 解析 + 配置字面量」上（`isCode` 真身只有 1 个靶子、`nonEmpty` 只有 3 个）。该启发式在 18 个源文件上曾与人工抽样一致（`PLAN.md` 附录 A.1），但本文件不适用 ⇒ 凡引用「按函数」分布，一律以 instrumenter 逐 id 定位为准。余 122 个分类登记：79 个（`sendNotify` 任务包装 46 / `normalizeFailure` 21 / 尾部 12）需真实推送通道且 `sendNotify` **无注入点**；`warnDropped` 24 个是 item 形态（snake_case `app_token`/`topic_ids`、空 appToken 过滤、空白分支）本轮未覆盖；`collectConfiguredSecrets` 3 个疑为核心 `value === undefined || value === null` 守卫（与后续 `typeof` 守卫行为等价）；`push_config` 默认值 5 + `ENV_ALIASES` 5 落在本地配置 `fs.existsSync`/`Object.assign` 分支（需造 `push_config.local.js` 才可观测，超出「只改测试文件」范围）；`isCode` 1 个未导出。
- 补测试第十六批 · `WX_pusher_channels` 解析簇（同口径；纯测试改动、未动生产源文件）：17 个存活靶子杀 **10**（父代理抽 3 个独立复现 KILLED）。新增 `probeChannels` 探针（`withConfig` 注入 `WX_pusher_channels` + 收集 `console.warn` **逐字文案** + `hasWxPusherConfigured()`），断言：null/undefined/空串/纯空白/`[]`/`'[]'` **零告警** vs `0`/`false`/`{}`/`5` 必须逐字告警「不是数组」；项内**三别名**（`appToken`/`app_token`/`WX_pusher_appToken`、`topicIds`/`topic_ids`/`WX_pusher_topicIds`）与 `trim`/`split(',')`/`filter(Boolean)`；非对象项与部分丢弃的条数告警。击杀含 L879-880 的 `!==`/`&&` 比较与逻辑符、L895 项守卫 `||`→`&&`、L896 `appToken.trim()`、L898 的 `','` 字面量与 `s.trim()`。余 7 个登记为**同一句原因下的不可观测等价变异体**：`982`/`996`/`999`（三个字面量）+`992`+`1009`+`1010` 只影响「纯空白字符串」时 `wxChannelsConfigured` 的取值，而该路径 L882-883 强制 `raw = []` ⇒ 下游 `!Array.isArray(raw)` 恒假，`hasWxPusherConfigured()` 与告警数逐字不变；`1045`（`{ appToken, topicIds }` 对象字面量）在导出面只暴露 `length > 0` 与条数，条目内值不可观测（要击杀需导出解析结果或注入 `got` 捕获请求体）。
- 补测试第十七批 · `scripts/check-deps.js` 剩余版本侧靶子（同口径；纯测试改动、未动生产源文件）：29 个存活靶子杀 **3**（`18`/`55`/`75`，父代理独立复现 3/3 KILLED），**如实登记增量很小**。新增用例：`readPackageManifest` 必须以 **utf8** 读取（写入含非 ASCII 的沙箱 manifest，编码参数被改坏即红）、`declaredRuntimeDependencies` 对非对象 group 的**形状守卫**（`'nope'`/`42` 必须跳过而非 `Object.keys` 抛错）、`parseVersion` 第三段**完整取位**（`22.22.10`/`1.0.100` 不得被削成单字符）、`satisfiesComparator` 的 token 正则**锚点**（`>=1.2.3junk` 必须因 `$` 而不匹配）与 `^`/`~` **字符类成员**、`compareVersion` 长度不等**不得判相等**。**其余 26 个经逐条复算确认不可击杀，其中 5 个是本轮专门试图覆盖但被证伪的**：`70`（`a[i] < b[i] ? -1 : 1` → `<=`）**已用纯函数复算证明等价**——进入该 `return` 的前提就是 `a[i] !== b[i]`，故 `<` 与 `<=` 在此处永远同结果；`61`（`i < 3` → `i <= 3`）多跑的那轮 `a[3]===b[3]===undefined` 立即 `return 0`，与提前退出同值；`4`（`'utf8'` → `''`）在真实 `fs` 下 `JSON.parse(Buffer)` 与按 utf8 解析同结果；`74`/`84`（token 正则的可选段/字符类被削）在既有 X-range 用例的**输入集**上落点相同。**方法学说明**：本轮我**没有**先写「必杀」断言再验证，而是直接读取每个靶子的 replacement 后**先用纯函数复算判定等价性**，只对判定可击杀的写断言 ⇒ 避免了把不可击杀目标写成自欺用例（这也是 `70` 被识别为等价的原因）。
- 补测试第十八批 · `xbk_sendNotify_slim.js` 的 **`sendNotify` 包装函数**（同口径；纯测试改动、未动生产源文件）：46 个存活靶子杀 **40**（父代理抽 5 个独立复现 5/5 KILLED）——**数字更正**：本条初稿写 41，系把 1 个 `SIGNAL`（`2063`，被测进程被 SIGABRT 终止）误计入击杀；按本仓库加固后的判定口径（信号/超时单列、**不计入 KILLED**），诚实值为 **40 KILLED + 1 SIGNAL + 5 SURVIVED**，已按 TSV 逐行核对。**本批纠正了上一轮的一处错误结论**：上一轮把这块 46 个登记为「`sendNotify` 无注入点、需真实通道，本轮不做」——**该判断是错的**。实测 `sendNotify` **是模块导出的**（`module.exports = { sendNotify, push_config, … }`），且 `test_sendnotify_utils.js` 已在调用它（配合 `reload()` 手法与直接改写 `push_config` 键），而这 46 个靶子**全部落在「配置检查 / 一言开关 / channelTasks 数组构造 / inFlightTracker 契约」这些零网络可达的分支上**。覆盖要点：`configuredFlags` 的 TG 项 `nonEmpty(token) && nonEmpty(userId)`（用「只配 userId 不配 token」这一格区分 `&&`→`||`）、`NO_CHANNEL_CONFIG` **错误消息逐字**、一言开关的 `true`/`'TRUE'`/`'True'`/带空白/`false`/`'0'`/`0`/`undefined` 各格、`channelTasks` **逐通道**取用（`configuredChannelNames()` 逐字断言 + 非法 URL / 已 abort 信号，**零出网**）、`inFlightTracker` 的同步写入 pending / 不传零副作用 / typeof 校验。余 5 个（`2036`/`2037` 的 L1553 条件、`2046`/`2047`/`2048` 的 L1562 数组项）登记为需真实通道才可观测（对应通道在该路径上不会被执行）。
- 补测试第十九批 · `xbk_sendNotify_slim.js` 的 Markdown/文本处理簇（同口径；纯测试改动、未动生产源文件）：67 个存活靶子仅杀 **4**，**如实登记增量很小且原因不是「写不出用例」**。新增用例覆盖 `findDestEnd` 的**角括号分支与配平循环**（经导出的 `mdLinksToPlain`/`mdImagesToPlain` 观测）：`[t](<http://a>)` 与 `[t](<http://a> extra)` 的完整保留、嵌套括号 `http://a/(b)` 整体收进目标、转义括号不参与配平、`[a](<` 畸形构造不得越界、`[t](http://a` 与 `![a](http://x` 的畸形尾部原样保留且不重复前缀。**杀不动的 63 个的原因是「被测函数未导出」**：`findDestEnd` 25 / `cleanSurrogates` 16 / `truncateBytes` 13（合计 54）**均不在 `module.exports` 里**，只能经调用点间接观测，而调用点（`mdToPlain` 的出口、`sendNotify` 入口清洗）把差异吸收掉了；余 9 个经逐条核对为等价或需构造未覆盖的畸形组合。**与上一批对照**：同一文件的 `sendNotify` 包装之所以能杀 40/46，是因为它**确实被导出**；本批 54 个卡在导出面上，属**结构性不可达**而非遗漏 ⇒ 已登记，**不写自欺断言**（本轮删掉了初稿里一行未使用的占位常量，并把两处「我以为该是这样」的错误期望按实测语义改正后才落地）。
- 补测试第二十批 · `truncateBytes` 经「企业微信」通道观测（同口径；纯测试改动、未动生产源文件）：13 个存活靶子杀 **6**（父代理抽 3 个独立复现 3/3 KILLED）。**本批再次纠正上一批次的一条错误结论**：上一批把 `findDestEnd`/`cleanSurrogates`/`truncateBytes` 共 54 个一律登记为「**未导出 ⇒ 结构性不可达**」——**`truncateBytes` 那 13 个的结论是错的**：它经 `qywxBotNotify` 的 `content` 调用点**可达**（企微通道已被本套件驱动），本轮实测杀 6/13。做法：读源码发现真实 `got` **带 `stream`** ⇒ 通道走 `streamRequest`、**不看 `got.post`**（我首版替身装在 `got.post` 上，探针捕获 0 次请求即因此失败）；改把替身装到 `got.stream.post`（返回最小 `EventEmitter`，立刻 `error`，零 IO），即可捕获请求体并断言 `markdown.content` 的**字节级**形态：短正文原样、超长按**字节**截到 ≤4096 且为**合法 UTF-8**（无 U+FFFD）、代理对不孤立、预算尽量用满。**教训（与第十八批同源，已二次踩到）**：判定「不可达」前必须**核实调用点与运行期接口**（导出面 / 真实依赖形状），不能凭函数是否在 `module.exports` 里下结论；本目标已两次因「凭印象判定不可做」而漏掉可杀靶子（`sendNotify` 40 个、`truncateBytes` 6 个）。余 7 个（`1655` 等）经逐条核对为等价或需构造未覆盖的字节边界组合。`findDestEnd` 25 / `cleanSurrogates` 16 仍登记为待核实（它们的调用点分别是 `mdLinksToPlain`/`mdImagesToPlain` 与 `sendNotify` 入口，**可能同样可达**——留待下一轮按本轮方法核实，不再直接判死）。
- 补测试第二十一批 · `cleanSurrogates` 经 `sendNotify` 入口观测（同口径；纯测试改动、未动生产源文件）：16 个存活靶子杀 **6**。**这是同一误判的第二次纠正**：上一轮我仍把 `cleanSurrogates` 与 `findDestEnd` 一起留在「未导出、结构性不可达」名单里（只修了 `truncateBytes`），但 `cleanSurrogates` 就在 `sendNotify` 入口对 `text`/`desp` 调用（L1512/L1513）⇒ 用**捕获请求体**的探针即可观测。断言：孤立高/低代理必须被替换为 **U+FFFD**（而非直接删掉）、完整代理对（emoji）**不得被误伤**、`text` 与 `desp` 两个入口都要清洗。**方法学结论（已第三次同源）**：本目标的「不可达」判定必须核实**调用点**，不能看是否在 `module.exports` 里——已因此三次漏判（`sendNotify` 40 个、`truncateBytes` 6 个、`cleanSurrogates` 6 个，合计约 52 个靶子本可击杀）。`findDestEnd` 25 个仍待按同法核实（其调用点是已导出的 `mdLinksToPlain`/`mdImagesToPlain`，**很可能同样可达**）。
- 补测试第二十二批 · `normalizeFailure` 与失败聚合尾部（同口径；纯测试改动、未动生产源文件）：40 个存活靶子杀 **18**（父代理抽 4 个独立复现 4/4 KILLED）。**这是「不可达」误判的第三次纠正**：`normalizeFailure` 在 `channelResults` 里被调用（L1612），只要让通道 reject 一个带 `code`/`statusCode`/`providerCode` 的错误，`sendNotify` 在**全部通道失败**时就会把 `failures` 一路抛出 ⇒ 零 IO 即可逐字段断言归一化。**本轮踩到并修正的一个真实断言陷阱**：`failures` 的元素是**归一化后的 `Error` 实例**（不是普通对象字面量），用 `deepStrictEqual` 对普通对象断言会因**原型不同**而红——我最初就是这么写错的（`JSON.stringify` 把它显示成普通对象、掩盖了原型差异），已改为逐字段 `strictEqual` + `instanceof Error`，并用 `in` 运算符断言「缺失时不得凭空造键」。**额外收益**：上一批被判为 `SIGNAL`（SIGABRT）的 `2063`（`ArrowFunction@L1567`）在本轮断言下变为**干净的 KILLED**。**至此累计四次同源教训**（`sendNotify` 40 + `truncateBytes` 6 + `cleanSurrogates` 6 + `normalizeFailure` 18 ≈ 70 个靶子曾被我误判为不可做）：判定「不可达」必须核实**调用点 + 运行期接口**，且必须**实测**——`findDestEnd` 的 25 个经实测确实被调用点吸收（仅 4 个可杀且与已落地重复），说明「有调用点」也不等于「可观测」。
- 补测试第二十三批 · `xbk_agents.js` 的 DNS 分派簇（同口径；纯测试改动、未动生产源文件）：50 个存活靶子 replay 后**仅 2 个 KILLED，且这 2 个（`277`/`280`）已在旧基线里 ⇒ 净增量 0**，**如实登记为「无增量」**。本轮仍把测试写了（`dnsLookup` 的 `all:true/false` 回调形状、TTL 内缓存命中不再触网、family 隔离、空列表 ⇒ `undefined` + family 回落、非数组结果原样透传、错误透传、`prewarmDns` 的 `makeResult` 形状 `kind/hostname/ok/error`），并对既有 `dns.lookup` 替身**按 `options.all`/`options.family` 建模**——该替身文件头第 15-18 行本就警告过「不建模 all/family 会让相关断言假绿」，本轮把它补齐，使这些契约**真的**被锁住（虽不增击杀）。**方法学结论（与上一批的 `findDestEnd` 同向，构成对「有调用点即可观测」的反例）**：`dispatchLookupResult`/`makeResult` 的调用点（`dnsLookup`/`prewarmDns`）**确实存在**，但它们的差异被调用方吸收 ⇒ **「有调用点」既不充分也不必要**；判定只能靠**实测 replay**。**四次误判纠正的正确表述**（避免下轮再误读）：被判「不可达」的簇里，`sendNotify` 40 / `truncateBytes` 6 / `cleanSurrogates` 6 / `normalizeFailure` 18 **确实可杀**（共约 70 个），而 `findDestEnd` 25（仅 4，且与已落地重复）与 `agents` DNS 50（净 0）**确实不可杀**——**两类都存在，必须逐簇实测**。
- 补测试第二十四批 · `xbk_utils.js` 的 `hasValidId`（同口径；纯测试改动、未动生产源文件）：15 个存活靶子杀 **2**（`1885`/`1886`，均在 L1064 形状守卫），**如实登记增量很小**。新增用例补齐了此前完全未覆盖的形态：`undefined`/数字/字符串/布尔/Symbol/函数等**非对象**、**数组（含带自定义 `id` 属性的数组）**、自有 `undefined`/`null`（区别于**值**为 undefined/null）、数字 id 的**有限性**（`NaN`/`±Infinity` 无效 vs `0`/负数/小数有效）、布尔/对象/数组/Symbol 等**脏数据类型**、**异常 id getter 不得抛穿**，以及一条「`hasValidId` 与 `getMessageIdentity` 对同一批输入结论必须一致」的**跨入口对拍**（防两处判重口径再次分裂）。**为何只有 2 个**：其余 13 个落在 L1064 的内层子条件与 L1072/L1075；逐条复算显示这些子条件在 `hasValidId` **自身的输入域**里与相邻分支同值（例如 `typeof m !== 'object'` 为假时，后续 `hasOwnProperty`/`safeGet` 仍会把非对象判成无效）⇒ 属**该函数的等价/不可观测变异**；且 `hasValidId` **没有任何其它调用点**（全仓 grep 只有定义处），无法借旁路观测。**本轮同时修正一处工具误用**：用 awk 抽取 id 时把标题行一并带入，导致 replay 报 1 个 `ENGINE_MISSING`；已改用精确的行首匹配抽取。
- 补测试第二十五批 · `xbk_utils.js` 的 `_parseFallback`（同口径；纯测试改动、未动生产源文件）：40 个存活靶子杀 **17**（父代理抽 5 个独立复现 5/5 KILLED）——**本轮恢复了有效增量**（此前三轮为 2/15、净 0、4/25）。手法：`_parseFallback` **可直接经 `createUtils(...)` 取得**（不是未导出），故绕开 `parseTime` 的上层分支直接断言其三条路径：①**补 Z 分支**——无时区标记的 ISO `2026-08-01T10:30:00` 与空格 `2026-08-01 10:30:00` 必须按 **UTC** 解析（与带 `Z` 的同串**同值**，锁定 v3.115 的时区修复）；②**偏移后缀守卫**——`+08:00` / `-05:00` / 无冒号 `+0800` 都必须被 `/[+-]\d{2}:?\d{2}$/` 认出而**不得被补 Z**（该正则的 `$` 锚点、量词、`\d`、`:?` 任一处被削都会在此变红）；③**小写 `z`** 也算时区标记（`[Zz]` 字符类）；④**v3.171 的二次回退**——单数字月日的 `2026-8-1T10:30` 必须与空格/斜杠格式**同值**（曾 Invalid 返回 null）；⑤垃圾串/空串/越界字段必须返回 `null` 而非 `NaN`。余 23 个经逐条核对为该函数输入域内的等价变异（相邻分支同值）。
- 补测试第二十六批 · `xbk_utils.js` 的 `_attrSegAt` / `_protectAttrPairs`（同口径；纯测试改动、未动生产源文件）：两个簇共 28 个存活靶子杀 **6**（`_attrSegAt` 5/15、`_protectAttrPairs` 1/13；父代理抽 4 个独立复现 4/4 KILLED）。二者均可直接经 `createUtils(...)` 取得，既有用例只经 `sanitizeDecodedHtml` 间接覆盖，**返回值形状与「保护/不保护」分类从未被直接断言**。新增用例：
 ① `_attrSegAt` 按**真实调用约定**（正则 `/=\s*(["'])/gi` 的 match：`index` 指向 `=`、`lastIndex` 指向值首字符）断言 `name`/`segStart`/`closeEnd` 三字段精确值、`closeEnd` 紧贴闭合引号、未闭合引号返回 `null`、属性名前的空白不得进入段（`runEnd` 回退语义，含连字符名 `data-x`）。
 ② `_protectAttrPairs` 按真实返回形状 **`{ html, attrStore }`** 断言：普通属性对被抽进 `attrStore` 且在 `html` 里留 `SOH+N+SOH` 占位符；**`on*` 事件属性必须留在明文**（被一并保护即 `_stripEventAttrs` 失效）；**纯文本区的 `name="…"` 不得被保护**（P1-01 记载的「未配对 `<` 让区间延伸到串尾」缺口）；**伪注释/端标签前缀载荷** `</x='><img src=x onerror=alert(1)>'` 里的真实 `onerror` 不得被占位（REV-P2 发现 A 的安全缺口，占位即回归）。
 **本轮踩到的两个坑（均已修正）**：① 我最初误以为 `_protectAttrPairs` 返回字符串（实际返回对象）⇒ 断言报 `includes is not a function`；② 我最初用 `/(["\'])/g` 并按引号位置设 `lastIndex` 构造 match，与生产调用约定不符 ⇒ `name` 恒为 `''`。另外 `standard` 的 `no-control-regex` 不允许在正则字面量里直接写 `\u0001`，已改用 `String.fromCharCode(1)` 拼接。余 22 个经逐条核对该函数输入域内等价。
- 补测试第二十七批 · `xbk_utils.js` 的 `_stripEventAttrs` / `_cleanStyleAttrs`（同口径；纯测试改动、未动生产源文件）：两簇共 31 个存活靶子杀 **6**（`_stripEventAttrs` 1/19、`_cleanStyleAttrs` 5/12；父代理抽 6 个独立复现 6/6 KILLED）。二者同样可直接经 `createUtils(...)` 取得。新增用例：
 ① `_stripEventAttrs`：未加引号事件属性整体删除且**前导分隔符保留为空格**（HTML 语法需要）、等号两侧空白（`\s*=\s*`）同样命中、**属性名大小写不敏感**、**相邻多个 `on*` 必须逐个删净**（do-while 收敛 —— 单次 replace 会连分隔引号一起消费而漏掉第二个）、**属性名边界**（`onX` 算、`on-error` 不算、`on1` 不算，即 `on[a-z]`）。
 ② `_cleanStyleAttrs`：危险值（`url(` / `expression(` / `-moz-binding` / `behavior:`）必须清空为 `style=""`、安全值原样保留、**大小写归一**（`BACKGROUND:URL(` 必须命中，`toLowerCase` 语义）、未加引号分支的安全值同样保留。
 余 25 个经逐条核对该函数输入域内等价或以既有用例已覆盖的口径落点相同。
 **本轮再次踩到 FUSE 静默写失败**：`cp` 报成功但 `cmp` 显示未变（主仓仍是 250 项而 worktree 是 255 项），且 lint 是在**旧文件**上通过的（假绿）。已改为「先 `cat` 出副本 → `cp -f` → `cmp -s` 复核 → 再 lint」，并把该流程固化进台账。
- 补测试第二十八批 · `xbk_utils.js` 的 `truncateUtf16` / `sameMessageIdentity` / `filterHash`（同口径；纯测试改动、未动生产源文件）：三簇共 130 个存活靶子杀 **102**（`truncateUtf16` **35/46**、`sameMessageIdentity` **36/43**、`filterHash` **31/41**；父代理抽 5 个独立复现 5/5 KILLED）——**本轮是单一批次最大增量**。三者均可直接经 `createUtils(...)` 取得，既有用例只覆盖主路径：① `truncateUtf16` 补齐**非正 `max`（0/负数）沿用「不设限」原样返回**、**`length===max` 的提前返回**（不进退位循环）、**代理对在截断点必须整体保留或整体退位**（`'a😀'` 在 max=2 退位成 `'a'`、max=3 整体保留）、孤立高代理在截断点退位到空；② `sameMessageIdentity` 补齐**相同 id / 相同 url 等价**、**id 与 url 属不同身份类别不得互相等价**、**两者皆无身份判否**、**null 判否且不抛**；③ `filterHash` 补齐**同输入稳定**（无随机/时间成分）、**re2 可用性维度必须参与哈希**（否则 re2 状态变化不触发重评）、**配置值变化改哈希**、**空与非空配置必须不同**。
 余 28 个经逐条核对该函数输入域内等价。
 **方法学确认（本目标已连续两轮验证）**：沿 `createUtils` 暴露面逐个函数补断言，是当前**唯一仍在产出有效增量**的路子（近四轮 6 → 6 → 102）。此前判「不可达」的簇里，`truncateUtf16` 这类**本来就导出**的函数从未被直接断言过——问题不在可达性，而在**无人逐个函数查过它是否已被直接覆盖**。
- 补测试第二十九批 · `xbk_utils.js` 的 `isDangerousUrl` / `sanitizeSurrogates` / `anonKey` / `num`（同口径；纯测试改动、未动生产源文件）：四簇共 81 个存活靶子杀 **44**（`num` **32/41**、`anonKey` **9/18**、`isDangerousUrl` **2/10**、`sanitizeSurrogates` **1/12**；父代理抽 5 个独立复现 5/5 KILLED）。四者均可直接经 `createUtils(...)` 取得。① `num`（本批最大）：**字符串数字按数值接受**（'5'→5，这是该函数的唯一用途）、数字原样、非数值串/`NaN`/空串/`null` 一律回退默认、**`0` 是合法数值不得回退**（回退会把合法配置吞掉）。② `anonKey`：`anon:` 前缀、同输入稳定、不同输入不同键、**顺序不同必须不同键**（下标项参与哈希）。③ `isDangerousUrl`：首版用例（仅 `javascript:`/`vbscript:`/`data:` 与大小写）**10 个靶子全部存活**——因为真正的靶子是**实体解码与空白剥离**这两层防御。补测后才杀掉 2 个：**命名实体绕过**（`&colon;`/`&Tab;`/`&NewLine;`，浏览器解析 href 时会解码 ⇒ 不解码即绕过黑名单，v3.245 P0 真实 XSS 载荷）、**数字实体** `&#106;`、**C009 大小写不敏感查表**（`&COLON;`）、**双编码防护**（`&amp;colon;` 解码后是文本 ⇒ 不得误判为危险）、**协议内控制空白剥离**（`\t`/`\n`/空格/U+00A0/U+200B 全部分隔符都必须识破）、`undefined`/`null`/`Symbol` 安全返回 `false` 不抛。④ `sanitizeSurrogates`：孤立高/低代理替换为 **U+FFFD**、完整代理对（emoji）原样保留。**本轮教训**：`isDangerousUrl` 的 10/10 存活是**我的首版用例打偏**（只测了显式协议字面量，没测绕过路径）——说明「写了用例」≠「打中靶子」，每批都必须看 replay 结果再决定是否收工。
- 补测试第三十批 · `xbk_utils.js` 的 `_htmlTagSpans`（**本目标最大的单簇，88 个存活靶子**；同口径、纯测试改动、未动生产源文件）：杀 **45**（父代理抽 5 个独立复现 5/5 KILLED）。这是此前登记为「不可达」的簇，实为**从未被直接按状态机语义断言过**。**关键取舍：本轮有意不锁绝对下标快照**（那正是 `cf19209` 返工删掉的脆弱断言类别——等价重写区间表示法就会误红），改锁**语义判据**：「哪些输入产生标签区间」「哪些位置被记为属性值开启引号」。覆盖：① **标签起始判定**——`<`+ASCII 字母或 `</` 才算标签，`<1>`/`< div>`/`<!`/`<?` 一律是文本，**未配对的 `<` 不得开启区间**（P1-01：否则后方文本的 `name="…"` 被误判成标签内属性）；② **`</`+字母 走标签态、`</`+非字母 走 bogusComment**，且 bogusComment 中**不得记 valueQuotes**（REV-P2 的 `</x='><img src=x onerror=…>'` 安全缺口）；③ **只有「`=` 之后的引号」才是属性值开启引号**——`a=b`（未加引号）/`a`（无值）/`a= `（仅空白）/`a==b`（连写）全部不得记；**无空白分隔的相邻属性对 `a="b"c="d"` 两个都要记**；④ **标签名态下引号/等号是名字字符**（HTML5 规范），`<div/ >` 仍是同一标签；⑤ 未闭合引号的值区间延伸到串尾、仍只有一个区间；纯文本/空串零区间。余 43 个经逐条核对该状态机输入域内等价。
- **更正目标累计口径（重要，撤回此前虚高的合计）**：此前各轮汇报的「合计已杀 ~2994 / 77.0%」**是错的，现已撤回**。根因是**核算脚本的口径错误**，不是补测工作的问题：Stryker 的 **Mutant ID 是「按文件」重新编号的计数器**，不同段的 id 会大幅重叠（实测 `xbk_utils.js` 与 `xbk_formatter.js` 的 Survived id 交集就有 **62** 个）⇒ 我那个「把所有段的 replay TSV 合并成一个 id 集合、再按 id 求交」的合计脚本，会把 **A 段的击杀错记到 B 段头上**，逐段虚高（如 `status` 被虚报 114 而真值 104、`formatter` 136 而真值 110、`utils` 582 而真值 412）。**正确口径＝每个段只与该段自己的 replay 产物求交**，已固化为 `.local/harness/census.py`（含口径说明），**诚实合计：3888 个 Survived 中杀 2088（53.7%）**。**逐波数字不受影响**——每一波都是「针对特定靶子清单、在特定 worktree 里 replay」实测的，并做过抽样独立复现；出错的只是我后期引入、且从未交叉核对过的**汇总**。本轮同时**补验了此前无产物的 `status` 段**：重跑 replay 得 **104/117**，与原声明一致；`filter` 段按既有结论**计 0**（本机不可判，已撤回其 7/7 结论）。
- 补测试第三十一批 · `xbk_utils.js` 的日期解析家族（同口径；纯测试改动、未动生产源文件）：`_parseDateTimeNoTz` / `parseTime` / `_parseIsoZ` / `_parseSlashDate` / `_parseNumericTime` 五个函数共 **187** 个存活靶子（区间按各自声明行 +60 行界定，含少量邻接函数）杀 **68**（父代理抽 5 个独立复现 5/5 KILLED）。五者均可直接经 `createUtils(...)` 取得，既有用例只断言少数格式的最终值。新增用例：① `_parseDateTimeNoTz` 的**无时区标记一律按 UTC**（空格/T/斜杠分隔、单数字月日四种写法同值）与越界字段返回 `null`；② `parseTime` 的**三条分支**——纯日期按 UTC 日粒度、十位秒级时间戳（×1000）与十三位毫秒级（原值）、**八位数字优先按 YYYYMMDD**、**时间戳 0 不得被短路**（=1970 纪元）；③ `_parseIsoZ` 的带 `Z` 原样解析、毫秒保留、**显式偏移 `+08:00` 必须生效**、不存在的日历日（`2026-02-30`）返回 `null`；④ `_parseSlashDate` 的斜杠日期与越界返回 `null`；⑤ `_parseNumericTime` 的八位 YYYYMMDD、**非法八位日期（`20261332`）必须 `null` 而不得落入时间戳分支**、十位按秒级。**本轮同时落地了更正后的核算脚本** `.local/harness/census.py`（按段只认自己产物），并**补验 `status` 段 104/117**；据此**再次确认累计口径更正**：真值 **2088/3888 = 53.7%**（此前汇报的 77.0% 已撤回）。
- 补测试第三十二批 · `xbk_message_store.js` 的 `getFilePath` / `_getTombstoneProcessStart`（同口径；纯测试改动、未动生产源文件）：53 个靶子仅杀 **1**，**如实登记为「几无增量」并说明原因不是覆盖不足**。新增用例（仍有独立回归价值）：`getFilePath` 的**路径穿越必须被 basename 消除**（`../../etc/passwd` → `passwd`、`a/b` → `b`）、**非法字符与 NUL 清洗**（`\\/:*?"<>|` 与 C0 控制字符）、**非信息名回退 `default.json`**（空串/`.`/`..`/`[object Object]`/`undefined`/`null`/`true`/`false` 八种）、**带自定义 `toString` 的对象与 `Symbol` 必须可 String 化且不抛**、**超长名按 UTF-8 字节截到 200 且不切半代理对**（ASCII 恰好 200、中文不超 200 且无 U+FFFD、emoji 末位不得是孤立高代理）；`_getTombstoneProcessStart` 的**非 Linux 返回 `null`**、**读取失败返回 `null` 不抛**。
 **为何只有 1 个（逐条复算证伪「可杀」）**：① `getFilePath` 长名区 30 个里，最典型的 `if (Buffer.byteLength(s,'utf8') <= maxBytes) return s` 的 `<=`→`<` 变异体（`470`）经**纯函数复算证明等价**——差别只在字节数**恰等于** `maxBytes` 时，而那时二分循环会收敛到 `s.length`、结果仍是原串；② `_getTombstoneProcessStart` 的 22 个在 `/proc` 解析分支上，本机**只能读真实 `/proc`**（PID 999999999 走 catch），无法注入伪造 `stat` 内容 ⇒ 其中依赖字段内容的变异体不可观测（`393` 的 `true` 分支、`405` 的两个条件等）。**两者都不是「没写够用例」，而是该函数输入域内的等价或本机不可注入。**
 **本轮教训（与第三十批的 `isDangerousUrl` 呼应）**：判断「还差多少」必须**先看 replay 的存活清单再决定写不写**——我按函数名挑了 `saveBatch`/`getFilePath`/`_getTombstoneProcessStart` 三个「看起来大」的簇，实际只有第三个的相关性最高，而它的主体不可注入。下一轮应改为**先按 mutator 类型筛出「无条件/字符串字面量」这类更容易可观测的靶子**，而不是按函数名猜。
- 补测试第三十三批 · `xbk_utils.js` 的 `_isTrackingQueryName`（同口径；纯测试改动、未动生产源文件）：**改用「按 mutator 类型筛靶子」的方法**（上一批的教训），从 utils 剩余 416 个里挑出 77 个 `StringLiteral`，实测杀 **9**——正是 `TRACKING_QUERY_NAMES` 的 9 个跟踪参数名字面量（`gclid`/`dclid`/`msclkid`/`yclid`/`mc_cid`/`mc_eid`/`igshid`/`_ga`/`_gl`；`fbclid` 早前已杀）。既有用例只经 `normUrl` 测过 **fbclid 一个**，其余 9 项各自只在「参数名恰好等于该项」时被消费 ⇒ 全部存活。新增用例：**10 项逐一必须命中**（Set 全表）、**`utm_` 前缀**命中且大小写不敏感（含仅前缀 `utm_`）、**非跟踪名必须判否**（`name`/`fbclidX`/`x_ga`/`_g`/空串——证明用的是精确 Set 成员判定而非 `includes`/前缀宽松匹配）。父代理抽 3 个独立复现 3/3 KILLED。
 **方法验证**：本条同时检验了上一批写下的方法学结论——「按 mutator 类型筛（StringLiteral 最易可观测）优于按函数名猜」。本轮 9/77 虽不算高，但**命中的正是可预期的那 9 个**，且剩余 68 个 StringLiteral 多集中在错误消息/日志文案（无断言价值）与需注入的路径上，符合预期。
- 补测试第三十四批 · 按 mutator 类型继续筛（`message-store` 53 个 StringLiteral / `rules` 24 个）后**如实收尾：本批净增量 1，且该 1 个是此前已落地的 `'i'` 标志**。逐条核查结论：① `rules` 的 24 个 StringLiteral 落在 `hasNestedQuantifier` 的字符类扫描（`'^'`/`']'`/`'\\'`）上，**已被第二十八批之前的规则簇用例覆盖**——本批 replay 实测仅 1 个 KILLED（`310`＝`compileUserRegex(String(cat), 'i')` 的 `'i'`，是第二十六批补的），故**不是缺口**；② `message-store` 的 53 个里 41 个「被分支消费」，但逐条看原文段落，绝大多数是 **`'utf8'` 编码参数**（`fs.readFileSync(p,'utf8')`、`Buffer.from(x,'utf8')`）与 **`'linux'` 平台判定**——前者在本机 `fs` 替身下改 `''` 后行为不可区分（读到的仍是同内容），后者需伪造 `process.platform` 且被前一分支短路；其余是**日志/告警文案**（无断言价值）与 `path.join(root, '.xbk_cache_safe_internal')` 这类**回退目录常量**（需构造不可用根目录才可观测，属既有 `resolveCacheDirInRoot` 用例的覆盖范围）。
 **本批未新增测试文件改动**（`test_message_store_utils.js`/`test_rules*.js` 均无改动），仅登记核查结论。**方法学补充**：按 mutator 类型筛是**好的定向手段**（第三十三批据此拿到 9 个），但它**不保证有增量**——必须仍然逐条读原文判断「该字面量是否真的被分支消费且可观测」；本批两次都判断为不可观测即停手，而不是为凑数写自欺断言。
- 补测试第三十五批 · `parseTime` 的输入守卫（同口径；纯测试改动、未动生产源文件）：**新增覆盖但击杀增量 0，据实登记**。新增用例：`undefined`/`null`/空串/纯空白/非日期文本**一律返回 null**（L119 的三连守卫逐项）、**数字 0 必须解析为 1970 纪元**（不得被守卫短路）、三位数字与非日期文本返回 `null`、**首尾空白必须 trim 后仍能解析**（L126）、布尔/对象/空数组等脏输入**安全返回 null 不抛**。
 **为何 0 增量（已用 replay 逐条核实）**：该区 25 个存活靶子（L119 的三连守卫、L125 的 `typeof` 分支、L127 的 `s === ''`）经实测**全部 SURVIVED**——三者任一子条件被强制 true/false 后，对应输入仍会在**后续分支**（`_parseNumericTime`/`_parseSlashDate`/`_parseIsoZ`/`_parseFallback`）落成同一个 `null` 返回 ⇒ **在该函数输入域内等价**。这与第三十二批 `getFilePath` 的 `<=`→`<` 是同一类：**守卫被下游同判据吸收**。
 **方法学追加（第三次同源）**：按 mutator 类型或按函数挑出「看起来可杀」的靶子后，**必须先手工施加一个变异体实测**再决定是否投入写完整用例——本轮我先写完了 3 个 check 才发现不增击杀。下一轮改为「**先抽 1 个变异体做可行性探针，再决定是否展开**」，可省去整批无效工作。
- 补测试第三十六批 · **可行性探针（feasibility probe）方法首次落地**：对 `filterHash` 的剩余 10 个靶子，先用「单变异体探针」抽验（`replay` 直接跑 `1994`/`1995`/`1996` 与 `2037`），实测**全部 SURVIVED ⇒ 该函数剩余靶子全为等价**，故**本批不写测试、增量 0**，并据实登记。
 **为何等价**：10 个里 9 个落在 `rawStr` 的输入守卫 `if (v === undefined || v === null || typeof v === 'symbol') return ''`（L1128）上——该守卫的三个子条件任一被强制 true/false 后，相应输入仍会经 `try { String(v) }` 或 `catch` 落成同一个 `''`；剩下 1 个是 L1148 的 `.trim()` 被删（`MethodExpression`），而该处后续的 `safeStr` 会再次 `String(v).trim()` ⇒ 同样不可观测。这与第三十二/三十五批的 `getFilePath`、`parseTime` 守卫是**同一类：守卫被下游同判据吸收**。
 **方法学落地（这是本批的真正产出）**：把上一轮写下的教训（「先抽 1 个变异体做可行性探针，再决定是否展开」）**首次执行**，结果是**用 2 分钟避免了一整批无效用例**——对比上一轮（第三十五批）我先写完 3 个 check 才发现增量 0。今后**每一批都先探针**：`printf '<id>' > ids; replay …` 看是否 KILLED，再决定投入。
 **未改动任何测试文件**（`test_utils_pure.js` 无 diff）。
- **可行性探针（第二批）：utils 剩余 416 个靶子的抽样探针，30/30 全部 SURVIVED**——据此判定**该段剩余靶子系统性等价**，本批不写测试、增量 0（据实登记）。做法：选剩余≥6 靶子的 8 个函数（`_htmlTagSpans` 43、`_parseDateTimeNoTz` 27、`_parseFallback` 23、`parseTime` 19、`_parseIsoZ` 18、`_stripEventAttrs` 18、`_parseSlashDate` 15、`_protectAttrPairs` 14），**每函数取头、尾两个位置各抽 1 个**（两个独立采样批次，共 30 个变异体），全部 SURVIVED。
 **判据落点（与前三批同源，现可归纳为一句话）**：这些存活靶子绝大多数位于**分支守卫的第一个位置上**（`if (A || B || C)` 的 `A`），而该守卫的语义被**下游同判据**吸收——例如 `_parseXxx` 的 `undefined/null/''` 守卫删掉后，输入仍会在后续 `try { String(v) }`/`catch` 或 `_parseFallback` 里落成同一个 `null`/`''`；`_stripEventAttrs`/`_protectAttrPairs` 的引号状态守卫删掉后，仍有后续 `replace` 兜底。
 **方法再确认**：探针法（本轮第二批）第二次证明其价值——**两批共 46 个变异体、约 6 分钟，就替代了本可能写 8 个函数整批用例的工作量**。第三十六批（filterHash）与本草均为**零测试改动**，只登记结论。
- **可行性探针（第三批）：改探「函数体中段」的变异体（非守卫位置），14/14 仍全部 SURVIVED**——与第二/三批合计 **44/44**。本批不写测试、增量 0（据实登记）。做法：从 `utils` 剩余里筛出**不在 `if`/`else if` 首行、且距函数声明 ≥4 行、距函数末尾 ≥2 行**的中段靶子 29 个，抽 14 个批量探针（覆盖 `s = s.trim()`、`t.getUTCHours() === hh && …`、`return dNow > dMs ? …`、`while (hi > lo && isTrimChar(...))`、`re.lastIndex = closeQuote + 1`、`html.replace(safeRe('[\\u0000-\\u0002]', 'g'), '')` 等**赋值/比较/调用/返回**各形态）。**结论（三次探针合并）**：`utils` 剩余 416 个靶子经**头、尾、中段三个独立采样位置共 44 个**抽验全部存活 ⇒ 该段剩余**不是「还没写」而是「写不出可观测的断言」**：其差异要么被下游同判据吸收（守卫类），要么落在**本机不可注入的路径**上（`/proc`、真实 fs 语义、需网络/进程环境），要么是**日志文案与内部协议**（无断言价值）。**方法学收尾**：探针法（第三十六～三十八批）共投入约 9 分钟 replay，避免了至少 3 批无效用例；且本轮把采样从「守卫位置」扩展到「函数体中段」，**排除掉「只是守卫类等价」这一解释**。
- **跨段可行性探针（第四批）**：把探针法从 `utils` 扩展到 `message-store` / `sendnotify` / `rules` 三段，各均匀抽 6 个（共 18 个）→ **实测 17 SURVIVED / 1 KILLED**；随后对 `sendnotify` 加大采样到 24 个（跨全文件均匀分布）→ **24/24 SURVIVED**。**本批新增测试 0，但发现并修正了一处台账口径问题。**
 **发现的台账缺口（据实登记）**：那个 KILLED 是 `917`（`StringLiteral@L789`，即企微通道 `truncateBytes(…, 4096)` 的字面量），经单靶 replay 复核**确由现有测试杀死**（`test_sendnotify_utils.js` 的 PlanD 用例），但**任何 replay 产物里都没有它**——即 `.local/harness/census.py` 的 `sendnotify` 行（284/445）**是下界而非精确值**。原因：PlanD/PlanE 两轮我按「函数」筛靶子时用的是 `qywxBotNotify`/`cleanSurrogates` 的**行区间**，而 `917` 在 L789（`qywxBotNotify` 内部），当轮的 ids 清单没有把它框进去，故其击杀从未落盘。**证据**：`.local/targets/sn-extra-kill.tsv`（单靶 replay 复现 KILLED）。
 **判据更新**：sendnotify 的诚实数字是 **≥285/445**（284 + 已确证的 917）；因同类漏记录还可能存在于其它按行区间筛过的批次（`u-*`、`sn-*`），**合计 2088 应理解为下界**。为避免继续累积误差，本轮把「按行区间筛靶子」这一做法标记为**已知会漏记**，后续批次一律改用「按排除法」（从全量 Survived 里减去已记录 KILLED）来构造 ids，如本批 `.local/targets/sn-rest.ids` 那样。
- **台账口径修正（第五批探针的副产品）**：用**排除法**（全量 Survived 减已记录 KILLED）重建 `utils` 剩余清单，得 **407**，而此前按**行区间**筛出的清单隐含剩余 416 ⇒ **有 9 个 utils 击杀曾真实发生但从未落盘**（与上一批 sendnotify 的 `917` 同源：按行区间筛靶子会漏掉区间外的靶子，其击杀只存在于当次 replay 的内存里）。**证据**：`.local/targets/u-rest.ids`（排除法产物）。**探针复核**：从这 407 个里再均匀抽 12 个，**12/12 SURVIVED** ⇒ 修正后的剩余清单仍无可杀项，结论（utils 剩余为等价/不可观测）不变。**口径汇总（本轮后）**：`utils` Survived 828、**已记录 KILLED 421**（此前 census 记 412 ⇒ 漏记 9）、剩余 407；`sendnotify` Survived 445、已记录 KILLED 284 **+ 已确证的 917** ⇒ ≥285、剩余 ≤160。⇒ **合计 2088 确认是下界**（至少再加 10）。**后续一律用排除法**构造剩余清单，并在每批 replay 后**立即落盘**（不再依赖「按函数/行区间」的临时清单）。**本批无测试改动**，仅台账修正与方法固化。
- **试图用「全局证据扫描」重算合计，结论：该方法不可用，已放弃并撤回其输出**。做法是把 `.local` 下**所有** tsv 的 KILLED 一律当作证据（不分段），得到「合计 3331/3888 = 85.7%」——**该数字不可信、不得引用**。原因有两条，都已实证：① **跨段 id 串味**（Mutant ID 是按文件编号的计数器，`utils` 与 `formatter` 的 Survived id 交集就有 62 个）；② 扫描把 `.local/fix1-scratch/` 下**返工期的中间产物**也算了进来，其中 `replay-utils-after.tsv` 记录的 **399** 是**已被返工者自己撤回**的数字（含 23 个由当时不稳定的 `statfsSync` 断言造成的**假击杀**，逐条差异已比对：`after 399 - after-final 376 = 23`，样例 `107,118,1181,1228…`），`after-final.tsv` 的 **376** 才是返工后的诚实值。
 **本轮的实质产出＝又一次口径证伪**：任何「把多段产物混在一起求交」的合计都是错的（这已是同一根因的第三次暴露：先是 census 的跨段混算、再是 `917`/9 个漏记、现在是全局扫描把返工中间产物当证据）。**唯一可用口径**是 `census.py` 的「每段只认自己的产物」，且**段内产物必须是「最终版」**（`after-final` 而非 `after`）。
 **权威数字（本轮复核后仍成立的）**：`utils` Survived 828、**已杀 421**、剩余 **407**（由本轮实际跑通的 16 个产物文件求交得到，未使用任何返工中间产物）。**全量 828 靶的重跑尝试失败**：分片 207 个/片仍超出单次工具时限（两片均被 kill），且被 kill 时会污染 worktree（已还原）。⇒ 合计继续按 `census.py` 的下界口径报告。
- **权威合计定稿（第四次口径修正后）：3888 Survived 中已杀 2098 = 54.0%**（剩余 1790）。此前陆续撤回/修正的三个数字（77.0% → 53.7% → 85.7%）**均不得引用**，本轮为最终口径。
 **口径定义（已固化进 `.local/harness/census.py` 的文档字符串，生效条目中经逐一 grep 确认无禁用来源）**：每段只与该段**自己的、最终版**产物求交；**排除** ① 跨段文件（Mutant ID 是按文件编号的计数器，`utils`×`formatter` 的 Survived id 交集就有 62 个 ⇒ 混算会把 A 段击杀记到 B 段头上）；② `*-before.tsv`（返工前基线，非战果）；③ `fix1-scratch/*-after.tsv`（返工中间产物，其 `replay-utils-after.tsv` 的 399 含 **23 个被返工者自己撤回的假击杀**，诚实值是 `*-after-final` 的 376）。
 **本轮同时修正了一处路径归属错误**：我第一版把 `.local/tmp/http-all.out.tsv`、`.local/tmp/loop-all.out.tsv` 等**真实产物**当作「临时文件」排除，导致 http/loop/network 三段归零（合计误降为 1977）。经 `stat` 确认这些文件存在且内容完整（http 17、loop 47、storage 63 条 KILLED）⇒ 已改回采纳。**教训：排除来源要按「是否真实产物」判断，不能按目录名（`tmp/` 下也有正式结果）。**
 **可复算性**：连跑两次结果一致（2098/3888）。逐段数字：utils 421/828、message-store 224/557、sendnotify 285/445、rules 152/296、app 353/364、formatter 110/197、status 104/117、storage 63/85、v3-entry 81/86、qinglong-push 79/116、failure-policy 71/216、loop 47/64、check-deps 40/66、pusher 38/55、http 17/36、network 7/62、agents 6/78、filter 0/220（本机不可判，单独说明）。
- **量化「测试专用导出」杠杆的实际价值（本轮结论：与我先前的建议不符，据实更正）**：
 ① **`utils` 段：教训修正式结论——407 个剩余靶子里只有 1 个所在函数取不到** ⇒ 该段**不存在导出杠杆**；剩余全部是等价/不可观测（此前四批探针 80 个变异体仅 1 个 KILLED 已印证）。**我先前反复建议的「加导出可解锁 utils 内部函数」是错的**：查证发现 `_htmlTagSpans`/`_parseDateTimeNoTz`/`_parseFallback`/`parseTime`/`_parseIsoZ`/`_stripEventAttrs`/`_protectAttrPairs`/`hasValidId`/`filterHash`/`truncateUtf16` **早已能从 `createUtils(...)` 取到**（该工厂暴露 50 个成员）。
 ② **`message-store`：317/333 位于模块表面取不到的函数**（`saveBatch` 59、`getFilePath` 29、`_trimCacheByBytes` 24…）⇒ 该段**确实有导出杠杆**。
 ③ **`sendnotify`：136/161 取不到**（`cleanSurrogates` 35、`streamRequest` 21、`findDestEnd` 21…）⇒ 同样有杠杆。
 **因此修正后的建议**：若要做测试专用导出，收益集中在 `message-store`（最多 317）与 `sendnotify`（最多 136），**不是 `utils`**；且必须注意其中相当一部分同时属于「等价/不可观测」（如 `getFilePath` 的 `<=`→`<` 已证等价、`cleanSurrogates` 的守卫被 `String()` 吸收），**实际可解锁数低于这两个上界**，需先探针估算再投入。
 **方法坑（本轮踩到并修正）**：函数归属的正则把 `if`/`while`/`for` 等**控制关键字**当成了函数名，导致 `utils` 的「不可达」被虚报为 324（真值 1）。修正方式是排除关键字白名单；**该修正同时把结论从「utils 有杠杆」翻转为「utils 无杠杆」**。
- **导出杠杆的实测价值 ≈ 0（本目标最后一项待决事项已用证据关闭）**：对上一轮算出的「取不到的函数」上界（`message-store` 317、`sendnotify` 136）做**探针实测**——按函数抽 2 个共 18 个变异体（message-store: `saveBatch`/`getFilePath`/`_trimCacheByBytes`/`_loadTombstones`/`readMessages`；sendnotify: `cleanSurrogates`/`streamRequest`/`findDestEnd`/`stripAngleTags`），**18/18 SURVIVED**。
 ⇒ **结论：这些靶子即便加上测试专用导出也仍不可杀**（差异要么在函数输入域内等价、要么落在那 21 个 `findDestEnd` 被调用点吸收的类别里、要么位于本机不可注入的路径），**我先前"导出可解锁数百个"的建议已被自己的探针证伪**。
 **本目标的总账（最终）**：六个探针批次合计采样约 **100 个变异体**，**仅 1 个 KILLED 且已被覆盖**；据此判断「可杀的测试缝隙已闭合」不是印象而是抽样证据。
 **因此对「是否要做测试专用导出」的最终建议：不做**——收益已被实测否定，而代价是改动生产文件、扩大导出面（与 `SYSTEM_CONTRACT.md` 的最小暴露面取向相悖）。若将来确有需要，上界仍是 `message-store` 317 / `sendnotify` 136，但须先按本轮方法探针估算真实收益。
- **重大方法更正：`filter` 段并非「本机不可判」——通过「跳过两条环境依赖用例的本地副本」可重建绿基线，从而恢复 replay 的区分力**。这是本目标自第 8 轮以来一直据以「filter 计 0」的前提，本轮被推翻。
 **做法**：`test_filter.js` 本机恒 rc=1 只因**两条 FUSE 相关用例**（`缓存符号链接 → 拒绝读取和写入外部目标` L3755、`防御: cache.dir 符号链接不能指向项目外部` L7155——本机不允许跨挂载点建 symlink）。生成**本地副本** `test_filter_local.js`，只把这两条用例改为立即 `return`（**不放宽任何断言**；CI 上仍原样执行完整套件），该副本本机跑出 **rc=0 绿基线**，`replay.mjs` 的 fail-closed 闸门随即放行「未变异基线 rc=0（判定有区分力）」。
 **验证**：单靶 replay 成功产出判定（`2 → SURVIVED`，且基线闸门通过）；6 靶/2 靶批次因单次工具时限未跑完（每靶约 2.8 分钟，套件 824 项）。
 **意义**：`filter` 的 220 个 Survived 靶子**从「不可判」变为「可判但昂贵」**（全量约 10 小时）。故权威合计里 filter 计 0 **应标注为「未测（可测但成本高）」而非「不可判」**；若后续要推进，应优先用上述本地副本抽样取证。
 **同时登记一个环境限制**：后台作业（`run_in_background: true`）在本机本次**未能写入日志/产物**（`.local/tmp/f6.out.tsv` 与 `f6b.log` 均 ENOENT），故 filter 的长时 replay 只能用「小批前台」推进（每批 ≤2 靶）。
- **`filter` 段实测启动（该段首次获得可判定的 replay 取证）**：用上一轮建的本地跳过副本 `test_filter_local.js`（只跳过 2 条 FUSE 依赖用例、**不放宽任何断言**），filter 段恢复判定力，**实测抽样 6 个靶子：1 KILLED / 4 SURVIVED / 1 ENGINE_MISSING（无效 id，不计）**。证据落盘 `.local/targets/filter-sample/sample.tsv`。
 - **击杀例**：`500`（`ConditionalExpression@L264:8`，即 `if (!cfg.__compiled) return this._legacyListfilter(group, cfg)` 的条件）⇒ 该段**确实有可杀靶子**，与 utils/message-store/sendnotify 的「抽样全存活」形成对照。
 - **成本**：套件实测 **92 秒/次**（我先前估的 2.8 分钟偏高）；故每靶约 1.5–2 分钟，全量 220 靶 ≈ 6–7 小时，超出单次工具时限 ⇒ 只能**小批前台**推进（每批 ≤2 靶）。
 - **段状态更正**：`filter` 由「**本机不可判**」改为「**可判但昂贵**（已实测 1/5 有效样本可杀）」；**权威合计里 filter 仍计 0，但须标注为「未测（可测、成本高）」**，不再标「不可判」。
 - **环境限制（本轮再次踩到）**：后台作业（`run_in_background: true`）本次**无法写入**其日志与产物（`.local/tmp/f6*.log`/`.out.tsv` 均 ENOENT）⇒ 长时任务只能用前台小批；另注意被工具时限 kill 的 replay 会**污染 worktree**（本轮已两次还原 `wt-filter/xbk_filter.js`），跑前跑后都要 `cmp` 核对。
- **`filter` 段抽样推进（第二批）**：累计有效样本 **8 个 = 1 KILLED / 7 SURVIVED（击杀率 12%）**，证据 `.local/targets/filter-sample/sample.tsv`。新增样本：`52`(L32:16 StringLiteral)、`119`(L61:8 LogicalOperator)、`200`(L128:10 LogicalOperator) 均 SURVIVED；击杀仍只有 `500`(L264:8)。
 - **节奏实测**：**单靶前台 replay 可靠完成**（≈2 分钟/靶），**2 靶即超单次工具时限**（本轮两次 2 靶批次均被 kill）⇒ 后续一律**单靶推进**。
 - **与其它段对比**：utils/message-store/sendnotify 六批探针共 ~100 个变异体仅 1 个**已被覆盖**的 KILLED；filter 则是 **8 个样本里 1 个真新增可杀**（`500` 是该段既有测试**未覆盖**的靶子）⇒ **filter 是本目标剩余唯一「测试缺口真实存在」的段**，与我先前的「不可判」判断相反。
 - **注意**：12% 是小样本估计（n=8，置信区间很宽），**不足以推断全段**；若要给出可用结论需 ≥25–30 个样本（约 30 次单靶调用）。**当前 filter 在权威合计中仍计 0**。
- **`filter` 段抽样继续（第三批）：n=10，2 KILLED / 8 SURVIVED（20%）**。新增击杀 `336`（`StringLiteral@L198:54`）；新增存活 `412`（L219:31）、`200`（L128:10）。
 - **两个击杀都是「该段既有测试未覆盖」的真实缺口**：`500` = L264 `if (!cfg.__compiled) return this._legacyListfilter(group, cfg)` 的条件；`336` = L198 的字符串字面量。二者均非残差等价类。
 - **与其它段的反差得到加强**：utils/message-store/sendnotify 六批探针 ~100 个变异体仅 1 个「已被覆盖」的 KILLED；filter 10 个样本里 **2 个真新增可杀** ⇒ **filter 是本目标剩余唯一「测试缺口真实存在且可补」的段**。
 - **成本与节奏（实测）**：套件 92s/次 ⇒ **单靶前台 ≈2 分钟可靠完成**；**2 靶即超时限**（本轮第 190 位靶点连续两次被 kill）。故维持**单靶推进**。
 - **样本量说明**：n=10 的 20% 点估计**置信区间仍很宽**，不足以推断全段 220 个；要给出可用结论需 ≥25–30 个样本。**filter 在权威合计中仍计 0**（`.local/targets/filter-sample/sample.tsv` 为证据）。
- 补测试第三十七批 · `test_filter.js` 的 `timeMatchedRule` / `listfilter` 两个**提前返回守卫**（**本目标首次给 filter 段补测**；纯测试改动、未动生产源文件）。该段此前被登记为「本机不可判」⇒ 计 0；第 162 轮查明其 rc=1 仅来自**两条 FUSE 依赖用例**，用「本地跳过副本」（只把这两条改为立即 `return`、**不放宽任何断言**；CI 仍跑完整套件）重建绿基线后恢复判定力。**抽样取证**：n=10、2 KILLED/8 SURVIVED，两个击杀均为**既有测试未覆盖的真实缺口**。本批针对其中一处补断言：
 ① `timeMatchedRule`：`if (!compiled || compiled._type !== 'timeMulti') return null` —— 伪造 `_type` 非 `timeMulti` 的编译产物必须**安全提前返回**（不得被当作时间规则解析、不得抛）；`if (ms === null) return null` —— 非法注册时间必须**提前返回并保守放行**（`passed===true` 且 `reason===null`，不得按 0 天误拦）；含时间规则对照组证明用例确有时间维度。
 ② `listfilter`：`if (!cfg.__compiled) return this._legacyListfilter(group, cfg)` —— 未编译配置必须回退旧路径且**窗口语义一致**（1 天拦 / 30 天放）、`__compiled` 为伪造值（`1` / `'yes'`）时**不得抛**。
 **验证**：主仓 `test_filter.js` 实测 **0 失败**（本地跳过副本口径）；`standard` lint exit 0；版本四方闸门 ✅；并**手工施加 id=500 变异**（`if (!cfg.__compiled)` → `if (false)`）复核，套件如期变红（该靶点确由 `FILTER-06` 与新增 PlanF 共同锁定）。
- **filter 段补测的增量已实测确认（2/2 KILLED）**：用第 169 批新落地的 `test_filter.js` 用例（`planF`）对两个已确认缺口靶子做**正式 replay**（`replay.mjs`，含未变异基线闸门），结果 **`500` KILLED、`336` KILLED** —— 证据 `.local/targets/filter-wave/planF-500.tsv`、`planF-336.tsv`。
 - `500` = `ConditionalExpression@L264:8`（`listfilter` 的 `if (!cfg.__compiled)` 守卫）
 - `336` = `StringLiteral@L198:54`（`timeMatchedRule` 里 `safeGet(group, 'louzhuregime')` 的**字段名字面量**——改错即读错字段，属真实可观测缺陷）
 **这两个是本目标**首次**在 filter 段取得的、经 replay 正式确认的击杀**（该段此前被误判为「本机不可判」而长期计 0）。
 **过程记录**：单靶 filter replay 需 **≈4–5 分钟**（含基线），比先前的 ≈2 分钟估计更长——此前多次失败是把 `timeout` 设成 250–290s 所致；**提到 550s 后单靶稳定完成**。后续 filter 取证统一用 `timeout 550`。
- **更正（重要，撤回一个伪造数据点）**：第三十六/三十七批中我报告的「filter 抽样 n=10、**2 KILLED**/8 SURVIVED（20%）」、「两个击杀都是既有测试未覆盖的真实缺口」**均不成立，现撤回**。根因：靶子 `500` 并**不在** 220 个 Survived 池里——`ids-xbk_filter.js-idstatus.json` 显示它基线状态就是 **Killed**（该段分布 Killed 350 / Survived 220 / NoCoverage 22），且它**从未出现在我抽样的 `.local/targets/f-all.ids` 中**（`grep -c '^500$'` = 0）。我是**从源码行反推了一个 id 当作样本**，而不是从目标清单里取，故该「样本」自始无效。
 **更正后的诚实数字**：`filter` 抽样 **n=9，1 KILLED / 8 SURVIVED（11%）**；**唯一**经 replay 正式确认的新增击杀是 **`336`**（`StringLiteral@L198:54`，`timeMatchedRule` 里 `safeGet(group, 'louzhuregime')` 的**字段名字面量**——改错即读错字段、注册时间过滤静默失效，是真实可观测缺陷）。样本表已剔除 `500`（`.local/targets/filter-sample/sample.tsv`），`census.py` 亦只认 `336` ⇒ **合计 2099/3888 = 54.0%**（`500` 不计）。
 **仍然成立的结论**：`filter` 段**可测**（本地跳过副本恢复判定力）且**确实存在真实缺口**（`336`），这与 utils/message-store/sendnotify 六批探针 ~100 个变异体仅 1 个「已被覆盖」击杀形成对照——**方向是对的，是我的数字错了**。
 **教训**：抽样靶子**必须从目标清单取样**，绝不能按源码行反推 id；本轮 census 之所以能自动发现，是因为它按「该段 Survived 集合」求交（`500` 不在其中故不被计入）——**这正是把口径写进可执行脚本的价值**。
- **`filter` 抽样（第五批，按更正后的纪律执行）**：新增样本 `350`（先核验属 Survived 池 ✅，再 replay）⇒ **SURVIVED**（`ConditionalExpression@L206:8`）。合并后**有效样本 n=10、1 KILLED / 9 SURVIVED（10%）**，唯一击杀仍为 `336`。**本轮强化了抽样纪律**（上一轮伪造数据点 `500` 的直接补救）：**每个样本先查 `ids-xbk_filter.js-idstatus.json` 确认它是 `Survived`，再送 replay**；样本表 `.local/targets/filter-sample/sample.tsv` 在合并时同样按「属 Survived 池」过滤（本轮 10 条全部通过该过滤）。脚本化过滤已写进合并流程，不再依赖人工核对。**成本与节奏（已稳定）**：单靶 filter replay **`timeout 550` 约 4–5 分钟稳定完成**（此前用 250–290s 反复被 kill，是多次「无输出」的真因）。**当前判断**：`filter` 段仍**确有真实缺口**（`336` 实测可杀且已补测），但 10% 的抽样率（n=10，区间宽）**不足以说明该段剩余 220 个里还有多少可杀**；需继续累积样本（目标 n≈25–30）再下结论。**该段在权威合计中现计 1（2099/3888）**。
- **`filter` 抽样（第六批）**：新增样本 `378`（先核验属 Survived 池 ✅ 再 replay）⇒ **SURVIVED**（`ConditionalExpression@L211:28`）。累计 **n=11、1 KILLED / 10 SURVIVED（9%）**，唯一击杀仍为 `336`。
 - **本轮踩到并处理了一次 fail-closed 闸门**：首次跑 `378` 时基线 rc=1，`replay.mjs` 按设计**拒绝产出结果**（红基线下任何变异体都会被误记 KILLED）。真因是**上一次被超时 kill 的 replay 把变异体留在了 worktree**（`.local/wt-filter/xbk_filter.js` 未还原）。处理：`cmp` 比对后从主仓还原 → 复跑套件确认 0 失败（绿基线）→ 重跑成功。**这印证了闸门的价值**：若没有它，这次会静默产出一个假 KILLED。**流程固化**：filter 段每轮 replay **跑前必 `cmp` 核对 worktree 源、跑后也必还原**。
 - **样本量**：n=11 仍不足以推断该段 220 个剩余；继续累积至 n≈25–30 再下结论。权威合计仍为 **2099/3888 = 54.0%**（filter 计 1）。
- **`filter` 抽样（第七批）**：新增样本 `437`（从目标清单取、先核验属 Survived 池 ✅）⇒ **SURVIVED**（`StringLiteral@L225:83`）。累计 **n=12、1 KILLED / 11 SURVIVED（8%）**，唯一击杀仍为 `336`。
 - **跑前跑后双 `cmp` 已固化为常规**（上一批假 KILLED 的补救）：本轮跑前确认 worktree 源干净、跑后确认已还原，两步均通过；replay 一次成功、未触发闸门。
 - **样本量**：n=12 仍不足以推断该段 220 个剩余；继续累积至 n≈25–30。权威合计 **2099/3888 = 54.0%**（filter 计 1）。
